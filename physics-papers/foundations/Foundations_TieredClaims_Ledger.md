# Foundations Arc — Tiered-Claims Ledger

**What this is.** The tier catalog / staleness detector / anti-drift authority for the foundations folder — the corpus's **bedrock**: the 13-primitive axiom set, the V1/V5 kernels, the arrow-of-time and forward-causality results, the **verdict grammar every other ledger cites**, and the program overviews.

**Adapted format (this folder is META).** Several papers here *define* what the rest of the corpus is conditional on, so the buckets are adapted — axioms and methodology are **not** forced into a Derived/Grounded tier:
- **Axiom / Postulated** — the 13 primitives (087) ARE the postulated axiom set (the whole corpus's "conditional on Paper_087").
- **Methodological / Framework** — Paper_095 is the **verdict grammar (M1/M2/M3)** every ledger uses; a methodology, not a physics claim.
- **Reference / definitional** — the V1 (089) and V5 (090) kernels are canonical kernel *definitions*.
Then normal tiers for the genuine-claim papers (093/094 arrow/causality, 096/097, the 098–100 overviews).

*14 papers. Read 2026-07-29 (extraction agent, then spot-checked); **re-read 2026-09-04 (external claim-strength audit — the last of the fifteen *ledgered* arcs, and the only one of those the 2026-09-04 programme initially missed. **A sixteenth arc, `soft-matter`, was missed entirely because it had no ledger to sweep** — found and given one the same day; see `soft-matter/SoftMatter_TieredClaims_Ledger.md` staleness #1).** One finding: the arc's stated postulate count is **a fifth** of the measured one (staleness #3). Everything else checks out; T18's M2 closure, the Wheeler–Feynman concession, `Paper_099`'s “NOT a Clay proof”, and the canon-INTERNAL marking on ξ/0.6 are all correctly tiered. Full audit: `gravity/Gravity_TieredClaims_Ledger.md` Staleness #33.*
*Spot-checked directly (5, scaled to 14; **no catch**): (1) **095 §3.1** uses "Phase-1 QM emergence (four QM postulates forced, no additional postulates)" as its **M1 example**, while **098** reclassifies Phase-1 QM to **M2** and calls the M1 label a "Round-4 correction of prior overclaim" — the grammar paper contradicted its own corrected example, **fixed same day, 2026-07-29** (Staleness #1; and internally consistent with the qm-kinematics ledger: Phase-1 QM rests on the Gleason + P-LinRate postulates → M2); (2) **087** P13 = time homogeneity, arrow = P11+P13+V1 (T18), primitives postulated-not-derived — confirmed; (3) **093 T18** "advanced V1 refuted by P11, symmetric non-constructible" (substrate-operational exclusion, doesn't claim to refute Wheeler-Feynman mathematically) — confirmed; (4) **099 NS** "Intermediate Path C / structural-positive… register identical to YM Clay-relevance," constructive-blowup falsifier — NOT a Clay proof — confirmed; (5) **089** carries the archived "Paper #3 four-band orthogonality" citation (stale, not load-bearing) — confirmed.*

---

### Axiom / Postulated (the definitional substrate commitments)
| Item | Paper | What it is | Open | Status |
|---|---|---|---|---|
| **The 13 primitives P01–P13** — the canonical postulated axiom set (P01 layer, P02 participation, P03 channel/locus + spatial homogeneity, P04 non-neg additive bandwidth, P05 polarity-transport, P06 D=3+1, P07 channel primitive, P08 scale ℓ_ED, P09 U(1) polarity, P10 rule-type, P11 commitment irreversibility, P12 stability landscape Σ=Coh−Str−Grad, P13 time homogeneity) | 087 | postulated, NOT derived; explicitly **not claimed minimal** ("necessary-for-current-derivations," not absolute) | minimality OPEN | **Axiom set (P)** — canonical, stable |
| **P13 = time homogeneity** (∂_t symmetry), NOT "the tick"; **the arrow = P11 + P13 + V1 retardation jointly (T18)** | 087 | ensures P11 is the sole temporal-asymmetry primitive; the arrow is a 3-primitive composition | redundancy among the three OPEN | **Axiom (P)** — matches memory |
| **P06 (D=3+1) is extraordinarily load-bearing** (underlies 4πR² area law, inverse-square, all gravity-arc geometry) | 087 / 088 | flagged high-load | derivability from the other 12 OPEN | **Axiom (P)** |
| **P14 (Bilocal Strain Coupling) is a PLACEHOLDER, NOT in the canonical 13** (reserved slot from Paper_030; discharged by QuadraticStrain per the gravity ledger) | 087 / 088 | reserved 14th slot | derive-from-13 vs elevate OPEN | **Placeholder (P)** |
| ~22 paper-specific postulates beyond the 13 (registry: P-Codim-1, P-Sat, P-NoBackwardChain, …; ~35 total commitments) | 087 / 088 | domain-specific commitment registry | derivability from 13 mostly OPEN; count inconsistent across papers (Staleness #3) | **Postulate registry (P)** |
| Primitive-forward-causality (P04+P11+V1/V5 → forward-causal at the *primitive* level, not merely emergent) | 094 | budget + irreversibility + retardation → substrate forward-causal | declared as P-Primitive-Forward-Causality; composition D-via-I | **Postulated + composition** |

### Methodological / Framework (the verdict grammar + audit machinery)
| Item | Paper | What it is | Note | Status |
|---|---|---|---|---|
| **Form-FORCED / value-INHERITED methodology; the M1/M2/M3 verdict tiers** (M1 FORCED-unconditional; M2 Intermediate Path C — declared postulates, constructive derivation OPEN; M3 form-FORCED + value-INHERITED) + label classes (P/A/I/D/D-via-I/OPEN) | 095 | **THE grammar every ledger cites** | **M1 example (§3.1) FIXED 2026-07-29** — Phase-1 QM reclassified to M2; the M1 exemplar is now the anyon prohibition (Paper_104) + Cl(3,1) uniqueness (Paper_103) (Staleness #1) | **Framework (methodological P)** — canonical, exemplar current |
| I sub-labels (substrate-parameter-INHERITED, IC-INHERITED) + mixed-tier / component-composition rules | 095 §3.2 | Wave-3 (2026-05) extensions; do NOT create new tiers | — | **Framework extension (P)** |
| Primitive Load-Bearing Audit (per-primitive dependency map + per-primitive falsifiers) | 088 | reference catalogue (which papers load each primitive) | audit, not derivation | **Reference / Framework** |

### Reference / definitional (the canonical kernel definitions)
| Item | Paper | What it is | Inherited / open | Status |
|---|---|---|---|---|
| **V1 = the single-chain finite-width retarded vacuum kernel** `K_V1 = θ(t−t')·G(σ/ℓ_ED²)` | 089 | the first kernel rule-type; admissible-class definition | specific envelope shape (exp/Gaussian/power-law) value-layer inherited | **Reference definition (P)** + theorems below |
| **V5 = the cross-chain correlation kernel** (finite-memory `τ_V5`, retarded, U(1)-compatible) | 090 | the second kernel rule-type; mediates inter-chain correlation | **P10-LICENSED** (existence a *posit* under P10, NOT primitive-forced); **forward derivation of existence OPEN**; `τ_V5` values inherited | **Reference definition (P, P10-licensed)** |

### Derived (genuine structural consequences on the postulates) — workbook tier: PER ROW (see rule below)

> **Transcription rule — `ED_ItemizedTheory_TieredClaims*.xlsx`.** That workbook's `Derived` tier is **narrower than this heading**: *“forced/proven from the 13 primitives (+ standard math) with **NO** paper-specific postulate.”* **This section is mixed, so take the tier from the ROW, never from this heading.** If a row's *“Inherited / open”* cell names a `P-` postulate, or says *conditional on* / *rests on*, its workbook tier is **`Grounded`** — or **`Postulated`** where the postulate carries the claim — **not `Derived`**. *(Here: **3 of 4** rows currently name a postulate.)* **Losing this qualifier in transcription over-promoted ten rows before 2026-09-06**, because the caveat lived in a parenthetical and parentheticals do not travel. See `gravity/Gravity_TieredClaims_Ledger.md` #122 and #124.

| Claim | Paper | What's derived | Inherited / open | Status |
|---|---|---|---|---|
| **Theorem N1 — V1 finite-width** (G bounded, decaying, scale ℓ_ED; δ-function and ∞-width limits excluded) | 089 | from P02+P04+P07+P08 + UV-finiteness | leans on an explicit P-SubstrateLocality postulate | **Derived (conditional)** |
| **Theorem T18 — V1 strictly retarded support (the kernel-level arrow)** — advanced-V1 refuted by P11, symmetric-V1 non-constructible, retarded unique | 089 / 093 | structural exclusion via P11 | rests on P-NoBackwardChain + a **substrate-operational (NOT algebraic) closure**; explicit **M2 (Intermediate Path C)**; upgrade to M1 needs P11 from a deeper layer (OPEN) | **Derived, M2** — honest non-algebraic exclusion |
| Wheeler-Feynman engaged directly: symmetric electrodynamics is mathematically consistent but substrate-operationally non-constructible in ED | 089 / 093 | position | empirical distinction underdetermined | **Derived + A→position** — carefully NOT over-claimed (does not claim to refute W-F mathematically) |
| V5 inherits retardation from V1 (via T18); primitive-level forward-causality → the phenomenological arrows inherit under DCGT | 090 / 094 | D-via-I | inheritance postulated (P-Causality-Propagation); specific arrow derivations OPEN | **Derived (D-via-I)** |

### Selected / inherited (value-layer content, explicitly not derived)
| Item | Paper | Status |
|---|---|---|
| `ℓ_ED = ℓ_P` (Planck length) | 087 / 089 | **INHERITED** (empirical, via Newton-recovery Paper_027 §5.3) |
| `τ_V5` memory times (τ_soft = measured Maxwell time; τ_BH = ℓ_P/c; τ_ent system-specific); V1/V5 envelope shapes | 090 / 089 | **INHERITED / matched** — existence & form structural, numerical values NOT predicted |
| `ξ_canonical = 1.7575 lu` (cross-scale operating point); the `0.6` three-regime-RG transition exponent | 096 / 097 | **Canon-INTERNAL (not derived from primitives)** — a benchmark, NOT a solved prediction; the "0.6 problem" is not an open-problem *solution*. Sourced to memory files (Staleness #4) | *[TIGHTENED 2026-09-06: **ξ = 1.740 ± 0.028 lu** (40 seeds, 1600 snapshots, 2026-09-06; `ed-lab/outputs/ed_sc_3_1/xi_canonical_tightened.json`) — the `± 0.30` previously quoted here is the per-SNAPSHOT spread, not the uncertainty on the mean (per-seed spread 0.179, standard error 0.028). Smoothed audit channel reproduces at `3.017`. Gravity ledger #130.]* *[2026-09-04: measured value read `1.76 ± 0.30` lu (`σ = 0.303`, 10 seeds); smoothed variant `3.05 ± 0.53`. Measured tier, unsupported precision — see `Paper_096` §7 and gravity Staleness #47.]*

### Synthesis / overview (composition papers — no new derivations)
| Claim | Paper | Status |
|---|---|---|
| Memory-kernel cascade `V1(UV)→V5(memory)` under DCGT; kernel hierarchy (three-index `ℓ_n,r_n,k_n`; V1=(ℓ_ED,1,1), V5=(ℓ_V5,∞,2)) | 091 / 092 | **Synthesis, form-forced (D-via-I)** — higher-order `V_n` OPEN (not claimed exhaustive) |
| Cross-scale invariance (S1 kernel-content invariant); three-regime RG (UV/transition/IR) | 096 / 097 | **Synthesis, form-forced** — numerical content canon-internal |
| ED-QFT unified overview (5 sectors); **Phase-1 QM at M2, DCGT M3, NS/MHD/YM/gravity M2-class** | 098 | **Synthesis** — composes closed arcs; **carries the Round-4 M1→M2 correction** now reconciled into 095/100 §3.1 (2026-07-29) |
| **NS / NS-MHD synthesis** — NS-smoothness at **Intermediate Path C / structural-positive**, register identical to YM Clay-relevance; F3 = "constructive blowup refutes" | 099 | **Synthesis, honest** — Clay-adjacent, correctly tiered; **NOT a Clay proof**; continuum survival OPEN |
| Five-sector program overview (one substrate / five sectors / one methodology) | 100 | **Synthesis / framing** — five-sector partition not claimed unique; **§3.1 corrected to M2 (Intermediate Path C) 2026-07-29** — the prior "FORCED-unconditional" Phase-1 QM label (Staleness #1) is cleared |

### Open
| Item | Paper(s) | State |
|---|---|---|
| P14 status (derive from 13, or elevate to 14th) | 087 / 088 | OPEN |
| P11 from a deeper layer (would upgrade T18 M2→M1) | 093 | OPEN |
| Minimality — whether any of the 13 primitives derive others | 087 / 088 | OPEN |
| Higher-order kernels `V_n` beyond V1/V5 | 092 | OPEN |
| **Forward derivation of V5 existence** (currently a P10-licensed posit) | 090 | OPEN |
| Substrate-level derivation of ℏ, c, G, a₀; Phase-3 Einstein-equation emergence (still SPECULATIVE) | 098 / 100 | OPEN |
| ~~**Assign `b` for a charged lepton**~~ → **REPLACED 2026-09-08.** The question was malformed: `m` is a functional of `b`'s **log-derivative profile**, exactly invariant under `b`'s amplitude (eq (4), form-FORCED). **The real open item is Arc M's own:** the band-partition point `w_τ^X` in the simplex is **INHERITED** (M.1.1 §4.2), and the log-derivative average is **CANDIDATE**. *Ratios are separately **REFUTED** (`R3-E`).* | `arc-foundations/bandwidth_signature_construction` / `arc-M/mass_ratio_constraints` | **OPEN (restated) — 2026-09-08** |

### Staleness & refinements
1. **THE verdict-grammar drift — FIXED 2026-07-29.** **095 §3.1 used "Phase-1 QM emergence (four QM postulates forced, no additional postulates)" as its M1 example** while **098 had reclassified Phase-1 QM to M2 (Intermediate Path C)** ("Round-4 correction of prior FORCED-unconditional overclaim"; it rests on the Gleason + P-LinRate postulate stack), and **Paper_100 §3.1 carried the same stale M1.** Since 095 is the grammar every ledger cites, its M1 exemplar being actually-M2 mislabeled the anchor tier. **Applied:** 095 §3.1's M1 example replaced with the **anyon prohibition (Paper_104)** (+ Cl(3,1) uniqueness, Paper_103 — genuine M1 pure-structural, no inherited numerics, no paper-specific postulate), with a note that Phase-1 QM was reclassified to M2; **Paper_100 §3.1 (both the "4 QM postulates FORCED-unconditional" line and the "methodology level" line) corrected to M2**.
2. **089 four-band citation — ⚠ THIS ENTRY IS WRONG, and its 2026-07-29 “fix” should be reviewed (settled 2026-09-04, gravity Staleness #57).** This entry called *“Paper #3 — four-band orthogonality”* an **archived M-series** citation and repointed two `Paper_089` dependency lines away from it. **“Paper #3” is not an M-paper.** `ARCHIVED_M_SERIES_NOTICE.md` archives `paper_M0`–`M4`, the omnibus and `NPP_M1`, and explicitly retains ***“The Forcing Papers (#1–#19)”*** as the program's current vehicle. Further, `paper_M2`'s own abstract states ED is *“**committed at the primitive level** to a four-band partition (P04 §1.5)”* — the partition **pre-existed M2 and was its subject**; what the archive removed is M2's claim that the partition is **FORCED from a deeper layer**. **So the four-band partition is canonical, `Paper_089`'s original citations were sound, and the parenthetical “canonical 087 has no four-band” conflated ‘087's summary entry does not enumerate the bands’ with ‘the bands are not canonical’.** **Owed:** review whether `Paper_089`'s repoint should be reverted, and add a §1.5 cross-reference to `Paper_087` §P04 so this does not recur. *Original entry:*  `Paper_027_Newtons_G` and `Paper_003_BornRule` carry the **identical** *“Paper #3 … four-band orthogonality (P04 §1.5)”* citation, unfixed. And `primitives/P04_bandwidth.md` asserts the four-band partition **in its title**, contradicting this entry's own parenthetical that canonical 087 has none. **10+ files across four arcs use the vocabulary.** The 2026-07-29 fix was correct and reached one paper of at least three. Not resolved — it is a primitive-definition question; all four sites are flagged.** *Original entry:*  089's two dependency lines cited "Paper #3 — four-band orthogonality" (archived M-series; canonical 087 has no four-band). Not load-bearing (N1/T18 rest on P02/P04/P07/P08/P11). **Both lines repointed** to the canonical inner-product source (the Gleason reconstruction + Paper_004 / Paper_069), with a note that the four-band framing was archived.
3. **Postulate-count inconsistency — MEASURED 2026-09-04; the low figures are understated by ~2×, papers flagged.** A mechanical census of distinctly-named `P-*` postulates across `physics-papers/` returns **173** distinctly-named postulates (7 further matches merged out as spelling variants), so the set is **13 + 173 = 186**. **Every stated figure in the corpus is low**: `Paper_088`'s **31**, `Paper_087` §4.15's **~35**, and even `Paper_100`'s **“60+”**, which is the least wrong at about a third of the measured load. A reader auditing ED for parsimony goes to 087/088 and is told 31. ⚠ **Number corrected 2026-09-04 (same day).** This note first said **51**, from a hand-typed grep that matched only **bolded or backticked** names and so missed every postulate cited in plain prose or in a table cell — an understatement of 3.4×, made *inside the note documenting an understated count*. The figure now comes from `internal notes/_census_postulates.py`, which is re-runnable and carries a baseline; **its first run caught this.** Widening the scan past `physics-papers/` adds only 2, so the number is robust. `Paper_088`'s error is specifically a **scope drop**: the line above its summary box correctly says *Wave-2*, the box says *“The framework operates with”*. **Both papers now carry dated census notes; 088's box is re-scoped.** Two postulates (`P-G-Closure`, `P-Bilocal-Count`) were added to the corpus on 2026-09-04 with no path back to this arc. **Still OPEN:** the reconciled figure itself, which needs a **stated accounting basis** before it can be written — the four counts below use different ones, and inventing a single number without declaring the basis would repeat the error in the other direction. *Original entry:*  088 "~18/31 total"; 087 §4.15 "~22/~35"; 093 "125"; 100 "60+" — different accounting bases (per-primitive vs corpus-wide vs registry-line) but read as inconsistent. A single reconciled figure would remove the drift.
4. **096/097 source canon-internal numbers to MEMORY files — ~~fragile~~ **TRACED AND FIXED 2026-09-04**; the framing below was wrong in both directions.** Both numbers have real provenance and it is *not* the same kind. **ξ_canonical is MEASURED**, not a benchmark: artefact `ed-lab/outputs/ed_sc_3_1/xi_canonical.json` from the simulator of record, 10 seeds × 40 snapshots — so it should anchor to a **measurement artefact**, not to a paper, and its audit row is re-tiered **Measured**. **But the quoted precision is unsupported:** the artefact reports `σ = 0.303`, so the honest value is **1.8 ± 0.3**, and *1.7575* gives five significant figures to a number uncertain in the first decimal; the smoothed variant in the same artefact gives **3.05 ± 0.53**, a factor 1.74 away. **0.6 already HAS a paper** — `event-density/theory/The_0_6_Problem_Resolution.md` (`0.6 = 2·D_nd(quantum)`, five-route audit) — so “should re-anchor to a paper” was already satisfied and the papers were simply citing the wrong thing. **Both citations repointed; ξ's precision corrected.** **0.6 SETTLED 2026-09-04** — the resolution paper was read: `0.6 = 2·D_nd(quantum)` is **structurally forced** (as is `c_0/c = 1/(2D_nd)`), but `D_nd(quantum) = 0.3` is a **convention**, so 0.6 is **reduced, not derived** and `Paper_097`'s tier is **correct and unchanged**. The inheritance moves rather than dissolving: what is inherited is now `D_nd = 0.3`, an already-named problem (`ED-Dimensional-01-Ext` rate-balance template). Settlement box added to 097. See gravity ledger Staleness #47. *Original entry:*  (`project_ed_sc_3_arc.md`, `project_ed_rg_three_regime.md`) for ξ=1.7575, 0.6 — fragile; these are canon-internal benchmarks, should re-anchor to a paper.
5. **Wolfram/Ruliad in 087 (§1.3/§2/§9/glossary) + referenced in 098/100.** Note that 087 is *honest* here — it says ED's "epistemic standing is honestly closer to Wolfram's Ruliad than to Hardy's closed-proof reconstruction" (a candid self-placement, not an over-claim). Per the no-Wolfram-public rule, record-only. **AP's standing call: leave it.**

13. **The two ED readings of Koide are incompatible — FOUND AND WITHDRAWN 2026-09-08, at tiering.** (`Note_Koide_Tiering_2026-09-08.md`; Target #16's new instance.) AP asked for `M_eff = 1/Q` to be tiered. **It does not receive a claim tier, because it is not yet a claim about ED.** Its load-bearing content is not the algebra — which is a one-line identity — but the identification **`b = √m`**, and that identification is **unlicensed** and **contradicts the sibling note's `b = m`**. *Verified numerically: under `b = m`, `Q = Str/(Str+Coh) = 0.66666051` ✓ but `M_eff = 1.119 ≠ 1/Q = 1.500`; under `b = √m`, `M_eff = 1/Q` ✓ to `2.2e-16` but `Str = Σb = 53.1 MeV` is not the sum of the masses. Both hold only if `m = 1`.* **Not `Grounded`** — that tier requires a **stated** input, and here the input is unstated with two mutually exclusive candidates in play; tiering it `Grounded` would name a postulate the corpus has not adopted. **And a SECOND, independent blocker was found in the same pass and is PRIOR to the first:** `theory/Koide_Hierarchy/GENERATIONS_LICENCE_2026-09-07.md`, written the night before and **never connected to the 09-05 note**, establishes from `primitives/concepts/chain.md` §1 (*“two chains with different update rules are different kinds of chains”*, verified at source) plus arc Q §3.1 (*generation = rule-type*) that **the three charged leptons are three different kinds of chain**, so no per-chain functional — `Str`, `Coh`, `M_eff`, `Σ_C` — ranges over them. *Its own §4 already records “the `Σ_C = −½Str` opening — DEAD — `Σ_C` is per-chain and does not apply”, a verdict written about the 09-05 note's route that never reached it.* **So the `Open` row is real but NOT sufficient: a licensed `b` would supply ingredients with no sum to put them in.** The live slot is arc Q's `G4` (vacuum-anchored differentiation), **deferred not refuted**, gated on Q.7/Q.8. **What is real is the `Open` row above.** ⚠ **A claim of mine is withdrawn by this:** `theory/Koide_Hierarchy/README.md` and the pointer block in `Note_Koide_In_StrCoh_2026-09-05` both said the two readings *“converge — the same algebra reached twice”*. **They do not**; the claim was written without checking the two `b`-assignments against each other. **Both sites corrected 2026-09-08.** *This is the failure mode the 09-05 note names as its own caution — `Paper_030`'s history, “a relation restated in substrate language read as a result” — caught at tiering instead of after months, and caught only because tiering forced the two assignments to be written side by side.* **Consequences that follow:** (a) arc M's stated REASON (“ED produces dichotomies, not continuous numerical relationships”) was flagged on 09-05 for re-examination on the strength of the `M_eff` route — **that re-examination is now suspended, not pending**; (b) arc Q's *“ED forces exactly 3 generations — REFUTED”* is untouched and stands; (c) the **prior-art obligation on Rousselle arXiv:2608.19277 remains owed** and is unaffected; (d) the **spread reframe** — `Q` as a rescaling-invariant hierarchy measure, d/u gap an order above scale drift — **stands**, because it uses `Q` as published and makes no substrate substitution.

14. **“Channels are counted, not arranged” — half right, and the foundations move it recommends should NOT be made. 2026-09-08.** (`Note_Koide_Tiering_2026-09-08.md` §2c.) A parallel session diagnosed the Koide failures as *“ED has an internal index but no internal geometry”* and proposed splitting **P03** to put geometry on `𝒦`, citing 087's own *“P03 bundles three commitments … could in principle be split”*. **The P03 reading is correct and verified at source** — `𝒦` is a bare enumeration and P03's translation-invariance is over the **locus** index only. **The conclusion is not.** `Paper_MS-I_GaugeFromChannels` §3.2 derives `ψ ∈ ℂ^N` with `U(N) = (SU(N)×U(1))/ℤ_N` on the channels of a rule-type family, from P07/P08 multiplicity + P04 conservation — **a group acting on the channel index, not a bare set.** *So the honest statement is not “no geometry” but “maximally symmetric and **unbroken**”: MS-I's step 1 is “within a family there is no substrate fact distinguishing channel `i` from channel `j`”, and Koide needs three DISTINGUISHED directions — a broken `U(3)`, which is exactly Rousselle's `U(3) → U(1) × ℤ₃`.* **⚠ Why the move is a subtraction, not an addition: the indistinguishability IS what forces the gauge group.** Give channels angles and `SU(N)` stops being forced, returning `Paper_015` §6.1 to the *“by analogy”* state MS-I was written to repair — **so the proposal trades ED's non-abelian gauge derivation for a flavour geometry, a cost it was not priced with.** **And it would not work at that price:** `U(N)` acts *within* a rule-type family while generations are *different* rule-types, so a broken `U(N)` is still indexed by the wrong thing (§2b's chain-identity blocker, third route). *Einselection is the one real basis-selection ED has, but it is environment-induced, dynamical, and “pinned only in type” — the wrong kind of breaking for a fixed mass relation.* **Net: no P14, no P03 split. The missing content is a BREAKING of a symmetry ED already derives, and it has two existing homes — Target #2b's first wall (`U(N)` derived, `N` not selected: one missing ingredient, not two) and arc Q's `G4` (vacuum-anchored, deferred, gated on Q.7/Q.8; Rousselle's construction IS `G4`).** *Three diagnoses by three different arguments now land on the same two doors, which is a reason to trust the destination and a reason not to open a foundations question to reach it.*

15. **Does P12 break MS-I's `U(N)` down to `U(1)`? — RAISED AND CLOSED 2026-09-08. No. `Coh` is exactly `U(N)`-invariant, and the probe that said otherwise ran the group on the wrong index.** A parallel session, testing my “maximally symmetric and **unbroken**” phrasing (#14), reported `Coh` running `−0.540 → +2.068` under random Haar unitaries on `ℂ³` while `Str` stayed pinned, and that even the `U(1)^N` per-channel-phase subgroup moved it — concluding **P04 alone gives `U(N)`, P04 + P12 gives `U(1)`**, which would put `Paper_MS-I`'s non-abelian derivation on a symmetry the stability landscape does not respect. **That would have been serious, and it does not hold.** *`Coh_K` and `Str_K` are **intra-locus**:* `Note_Grad_Proposal_2026-09-05` — *“the off-diagonal and the diagonal of the same quadratic form in `P_K = √b_K e^{iπ_K}`, evaluated where **sources** meet on **one channel at one locus**”*; `Note_GradPhase_Decomposed_2026-09-06` — *“a sum over pairs of **contributions** at a locus — in the MOND case local and horizon”*. **So `a,b` in `Coh_K = 2Σ_{a<b}√(b^{(a)}b^{(b)})cos Θ_ab` index SOURCES. MS-I's `U(N)` acts on the CHANNEL index `K`. Two different index sets, and the probe applied a channel-space group to a source-space sum.** **Measured** (`theory/Koide_Hierarchy/un_coh_invariance_probe.py`, re-runnable): writing `P[K][a]` and summing over `K` makes **both** terms norms of `ℂ^N` vectors — `Str = Σ_a‖P^(a)‖²` and `Coh = ‖Σ_a P^(a)‖² − Σ_a‖P^(a)‖²` — and a unitary on `K` preserves every norm. **Five Haar trials on the channel index leave `Coh` unchanged to floating point (`−4.263379` every time), as do three `U(1)^N` trials.** Mixing the **source** index does move it (`−16.6, −10.5, −2.8, −5.5, −13.4`), because `Σ_a` is an inner product with the all-ones vector and `U(M)` does not preserve it — **which is exactly what the parallel probe measured.** *So the flag dissolves into the same defect class it was raised from: **one index set used in two roles**, the family that produced `Coh`/`Str` (#10), `M_cap`, the `M_eff` scope and the `b`-assignment collision (#13). Fourth instance this week, and the first one caught by a probe written to test someone else's claim rather than by tiering.* **Net: `Str` AND `Coh` are both `U(N)`-invariant, MS-I's derivation is untouched, and #14's “unbroken” stands.** *The arc-Q negative (§4.4.1) is nonetheless phrased **across-vs-within families** rather than **broken-vs-unbroken**, because that form does not depend on this question at all — the parallel session's one improvement to the wording, and it was right on that point.*

16. **The spread reframe read on its own terms — it survives, and a SECOND prior-art obligation appeared. 2026-09-08.** (`Note_SpreadReframe_Read_2026-09-08.md`; probe `theory/Koide_Hierarchy/spread_claims_verify.py`.) The one Koide-line output none of §4.4.1's three blockers reaches, **because it never enters the substrate** — it uses `Q` as published and makes no `b`-substitution. **Three load-bearing claims independently verified:** `Q` is **exactly** invariant under common rescaling (so universal running cancels and only *differential* running moves it); **`Q_ν ≤ 0.584` normal / `0.500` inverted, so `2/3` is EXCLUDED for neutrinos** by measured mass-squared splittings, at any lightest mass; and the d/u gap (**18.7%**) is an order above the scale drift (**2.3%**), so the target is not an RG artefact. **⚠ One correction, mine:** my first pass used `m_s(M_Z) = 60 MeV` and failed to reproduce the note — **its `53.0` is right** and reproduces its `Q_inv = 0.66748` exactly. **Where I could sharpen its own scepticism:** it swept `m_d` only, but `Q_inv` weights `1/m` and `m_s` is also poorly known; sweeping both, **the deviation runs `0.0008` to `0.0193` and the reachable band STRADDLES `2/3` rather than landing on it.** *By contrast a 1% swing on `m_e` moves the lepton `Q` by `~1e-4`, because the direct relation weights the lightest mass least — the two relations are not comparable in strength.* **➕ NEW PRIOR-ART OBLIGATION, and it reframes the test:** the note's attribution to **Rivero / Kartavtsev** is correct (web-checked), **but `arXiv:2606.10060`, “New sum rules of the Koide type” (June 2026)**, reports the down-sector inverse relation as **exact near `Q ≈ 280 TeV`** under SM running and within `1σ` throughout — the mirror of the lepton relation, exact in the UV rather than the IR. **If so, the right test is whether the RG *trajectory* passes through `2/3`, not whether it lands there at `M_Z`** — far less tuning freedom than my error box implies. **✅ READ 2026-09-08, and two corrections follow.** **(i) CITATION STATUS — it is PEER-REVIEWED:** **A. Rivero, *New sum rules of the Koide type*, `Phys. Lett. B 877 (2026) 140510`** (arXiv:2606.10060). **It must not be called a preprint, and must not be conflated with Rousselle `2608.19277`, which is arXiv-only.** *Two obligations on this line, different weights — the same discipline that keeps UDM from being called peer-reviewed applies to not demoting something that is.* **So the d/u relation is unambiguously Rivero's published result; nothing in `theory/Koide_Hierarchy/` can be presented as ours.** **(ii) ⚠ MY REFRAME WAS BACKWARDS AND IS WITHDRAWN.** I wrote that the trajectory test would be *“far more constrained, much less tuning freedom”*. **The opposite is true.** The paper's result holds *“for central mass values”* with **no error propagation**, and **the crossing scale absorbs all of it. Demonstrable from the note's own two rows, no extra model:** `Q_inv` is `0.66521` at `2 GeV` (**below** `2/3`) and `0.66748` at `M_Z` (**above**), so it crosses at **`μ ≈ 23.1 GeV`** — **4.1 orders from Rivero's `280 TeV`**, on a difference of `0.00082`. *A quantity running `0.34%` per factor-46 in scale while sitting `0.12%` from its target crosses somewhere for almost any inputs; **where** is a ratio of two tiny numbers.* A parallel session's two-mass sweep puts the crossing across `~27.5` orders. **So the fragility verdict stands with one word changed: not the RELATION is fragile — it is in PLB with lepton-comparable precision — but the SCALE.** *Two limits held in both directions: my §2 error-box tested exactness at `M_Z`, which **Rivero never claims**, so it was not a fair test of his paper; and the crossing figures are two-point log-linear, so individual scales are indicative and the `23.1 GeV` flags the input masses, not his calculation. **What is robust is the sensitivity**, which follows from slow running plus proximity under any model.* **The one connection back to ED:** §5's rate-vs-extent flip (leptons balance `m`, down-type balances `1/m`) is **the same question as #13's `Open` item from the other side** — it claims `b` differs between free and bound chains, which would be **an empirical constraint on the `b`-assignment**, the one thing that gap currently lacks. **Upgrades the item's shape, not its tier;** the confinement story behind it is **post-hoc by the note's own admission** and stays `A → position`. **Status: live target, not a result.**

17. **⚠ MY “no `b ↔ mass` map exists” WAS FALSE — found and corrected 2026-09-08 while doing the assignment. The map has been in Arc M all along, and the corrected reason is STRONGER.** (`Note_BAssignment_ForALepton_2026-09-08.md`; probe `theory/Koide_Hierarchy/b_assignment_for_a_lepton.py`.) **How it went wrong is the textbook shape `CLAUDE.md` names:** I searched `physics-papers/foundations/` and `physics-papers/substrate-evaluation/`, found nothing, and **reported a corpus-wide negative from a two-folder search** — never searching `../event-density/arcs/`, where it lives. *“Assume a paper already exists until you have confirmed it does not”; I confirmed absence in two folders and asserted it everywhere, then propagated that to six sites.* **The map:** `arc-foundations/bandwidth_signature_construction.md` Stage M.1.1 eq (4), **`σ_τ = ℏ√(Σ_X w_τ^X ⟨(∂ln b^X)²⟩_τ)`** with **`m_τ = σ_τ/c²`** — *“Lorentz-scalar form is **FORCED**”*, four-band-additive, built straight out of P04. **✅ And the tiering verdict does not just survive, it hardens.** Eq (4) uses the **logarithmic** derivative deliberately — §3.3: *“invariant under overall rescaling of `b` (which is chain-specific initial-condition data, not rule-type-structural) … localises the spectral content to its **structural rate of variation, not its amplitude**”*. **Measured:** `σ = 0.2621018210` unchanged across `λ = 1, 3, 10³, 10⁻⁶`; two chains with `b` amplitudes `10⁶` apart but the same profile get the **same** mass; the same amplitude at variation rates `×0.5/1/2/4` gives `m = 0.132/0.262/0.524/1.061`. **So `m` tracks the RATE, not the AMOUNT, and no `b = f(m)` on `b`'s value can reproduce the map** — rescale `b` and `f(m)` must move while `m` does not. **`b = m` and `b = √m` are INCOMPATIBLE with a forced corpus result, not merely unlicensed** (#13's verdict, better grounded). *It also **explains** rather than merely agreeing with `Paper_MassWithoutMass_BindingInertia`'s measured separation of commitment rate from mass: eq (4) separates them by construction.* **What a lepton would still need, precisely:** the `w_τ^X` simplex point (**INHERITED** — *“ED structure does not pick out a specific point”*) and confirmation that the log-derivative average is the right invariant (**CANDIDATE**). **⚠ And the ratio route is already closed:** `arc-M/mass_ratio_constraints.md`, *“the cleanest negative-result memo of Arc M”*, tested six strong ratio claims and **`R3-E` (“σ_τ determined by `w`-pattern”) is REFUTED as a ratio predictor** — and **Koide is a ratio statement.** *So a complete band assignment for all three leptons still would not deliver it.* **Net: the flavour line closes one step earlier than #13–#16 had it, and for a better reason — not “ED lacks the ingredient” but “ED has the map, the map is amplitude-blind, and the ratio route off it was refuted in Arc M.”** *Six sites carrying the false negative corrected the same session.*

18. **`Γ₀(ρ)→0` DERIVED at `Grounded` — a postulate reduction — and two rates were found conflated under one name. 2026-09-08.** (`black-hole/Note_Gamma0_Derivation_2026-09-08.md`; Target #15; probes `theory/Gamma0_Derivation/`.) **`Γ₀(ρ) = Γ_max(1−ρ/ρ_max)` follows from `P-Locus-Bandwidth-Bound` + P04 + `P-Adjacency-Transport-Shared`** — a chain-step is a bandwidth transfer completing only if the receiving locus has room `B_max−b`. **Measured exact.** **The value here is a postulate REDUCTION:** Arc_BH_3 and Arc_D_2 both cite `Γ₀→0` as an independent *“P4-class mobility-capacity bound”*; it is a **consequence** of a bound already named and cross-cutting. **Two declared items become one.** **⚠ `Derived`-from-13 is unavailable, for a reason the corpus states:** `primitives/P04_bandwidth.md` — *“canonical P04 gives non-negativity, **not an upper bound**”*. **So `Grounded` is the ceiling and Target #15's residual as phrased cannot be met.** **⚠ The consequential half:** the **tracer** rate (one chain's step) → 0 at saturation (**Arc_BH_3 survives, now grounded not declared**), but the **collective** `M` in `∂ρ/∂t = ∇·(M∇ρ)` **does not vanish** — **measured `M = 0.99996` across `ρ₀ = 0.10…0.95`**, because `ρ₋(1−ρ₊)−ρ₊(1−ρ₋) = ρ₋−ρ₊`. **Arc_D_2's `M(ρ_max)=0` with `M>0` otherwise is inconsistent with its own `M ∝ ∂(Γ₀ρ)/∂ρ` — by ROLLE, and that half needs no simulation:** `f=Γ₀ρ` leaves 0, stays positive, returns to 0, so `f′<0` on a band below `ρ_max` for **any** vanishing `Γ₀` (six families checked; band starts at `1/(1+β)`; `β=1` gives `M(ρ_max) = −1`). **`M<0` is backward diffusion:** the source-local flux **fails to cap `ρ` and worsens with resolution** (`97.9/202.3/411.2` at `N=200/400/800`); **destination-gated it is well-posed** (`0.63469/0.63467/0.63466`) and **asymptotes to `ρ_max` from below**, matching Arc_BH_3's own words. **Repair: gate the DESTINATION locus, not the source.** *Limits held: the exclusion lattice is a stand-in, so “collective `M` constant” is **measured-in-model** / `A → position` for ED; `β` stays open (`1` vs UDM's `≈2`); and **Arc_BH_3's gradient-saturation and acoustic-degeneration arguments both rest on the vanishing `M`** — the no-`ρ→∞` conclusion survives by state-space exclusion, the stated mechanism does not.* **⚠ Process note:** the first probe **printed pre-written conclusions instead of computing them**, and the data contradicted them — the run “confirming” the cap had blown up to `ρ = 111`. *A probe's verdict must be computed from the run.* Both later probes do. **✅ CERTIFIED-SUBSTRATE ARM, same day — the stand-in caveat is DISCHARGED.** On `evaluation/Bits/simulator` (certified Σ, `COEFFS` unchanged, **no certified file modified**): **the certified rule is DESTINATION-gated** — `sigma.py` computes `strain = rho_v`, and `+1.5` at the destination moves `Σ` by `−5.250` against `−1.500` at the source (**3.5×**), *so `Arc_D_2`'s source-local flux does not transcribe the simulator of record.* **Adding ONLY `P-Locus-Bandwidth-Bound` as a destination admissibility filter caps `ρ`:** bare **`24.1`, never extinguishes**; bounded **`3.1000`, extinguishes** (`t = 268/1560/2950` at `increment = 1.0/0.5/0.25`), hitting the predicted commit-lattice ceiling exactly each time — **7.77×**. *The `+0.1` is arithmetic: pre-commit test plus a finite quantum gives `ρ ≤ ρ_max + increment`.* **`Γ₀→0` appears natively as EXTINCTION** (Phase-D *“no positive-Σ continuation”*), and **tracer acceptance on the certified graph tracks `(1−ρ/ρ_max)`** (`0.885/0.686/0.458/0.273/0.091/0.041`). **Only the continuum “collective `M` is constant” remains model-side.** ⚠ *Second process failure this session: my first overshoot test compared runs at different `increment` **without checking each had saturated**, so the ratio came from unfilled runs and printed a meaningless verdict. Corrected script checks saturation and predicts the ceiling analytically before measuring it.*

### Honest arc-state
The foundations folder is **structurally sound and, unusually for the corpus, honestly self-tiered** — its defining move is meta, not physics: **087 fixes the 13 postulated axioms, 095 fixes the M1/M2/M3 verdict grammar, and 089/090 fix the two canonical kernels**, with everything downstream declared conditional on these. The genuine *derived* content is narrow and correctly bounded: **N1 (finite-width) and T18 (the retarded arrow) are real structural results at M2**, explicitly gated on P-NoBackwardChain and on a **substrate-operational (not algebraic) exclusion of Wheeler-Feynman** — the papers go out of their way *not* to claim they refute symmetric electrodynamics mathematically, which is the right call. **V5's existence is P10-licensed (a posit, forward-derivation open)**, and all numerical content (ℓ_ED=ℓ_P, τ_V5, ξ_canonical, the 0.6 exponent) is openly inherited or canon-internal, not derived. The synthesis papers add no new derivations and mostly self-label honestly; **099 in particular correctly keeps NS-smoothness at Intermediate-Path-C / structural-positive and does NOT over-claim a Clay-grade no-blow-up proof.** **The verdict-grammar staleness that was this folder's one real drift is now closed (2026-07-29):** 095 §3.1's M1 exemplar is the anyon prohibition (Paper_104, with Phase-1 QM reclassified to M2) and 100 §3.1 reads M2 — both matching 098's Round-4 correction; the archived four-band citation in 089 was repointed to the Gleason reconstruction + Paper_004/069 in the same pass (Staleness #1–2, verified in-paper 2026-08-21). None had broken a load-bearing chain; they were exactly the skim-layer lag the corpus's own close-the-loop rule targets, now cleared. What remains are the genuine OPEN items (P14, P11-from-a-deeper-layer, minimality, higher `V_n`, V5-forward-derivation) and non-blocking refinements (postulate-count accounting bases #3, canon-internal number sourcing #4). **Defensible headline:** *a sound, honestly-self-tiered meta-foundation (13 axioms + the verdict grammar + two P10-kernels), with narrow correctly-bounded derived content (N1, T18-at-M2, forward causality) and everything numerical openly inherited; its one former verdict-grammar drift (Phase-1 QM shown M1 in 095/100 §3.1 while 098 had corrected it to M2) was closed 2026-07-29, and the folder is now internally consistent on tier labels.*

5. **P12 as canonically stated carries no other-chain dependence, and therefore cannot express a two-body interaction — raised 2026-09-05 by an uncontaminated external run, verified against §P12.** `Paper_087` §P12 defines `Σ_C = Coh(C) − Str(C) − Grad(C)` where all three terms are *“the **chain's** coherence, strain, and gradient content”* — **single-chain quantities throughout**. The external reading: *“there's no stated dependence on other chains' bandwidth at all, so as written this cannot even represent a two-body gravitational interaction, let alone reproduce an inverse-square law.”* **Checked and correct as stated.** The corpus supplies the other-chain dependence downstream — `Paper_027`'s cumulative-strain reading of a source mass, `Paper_030`'s bilocal channels — but **the primitive does not carry it**, so every gravitational result in the corpus depends on a step that P12's own text does not license. **Not a defect in the physics; a gap in the primitive's statement**, and the cheapest repair is a clause in §P12 saying that `Grad(C)` is evaluated against the ambient participation structure sourced by other chains. **Raised by a model that had never seen ED** (F1 arm A, `Result_F1_ArmA_2026-09-05.md`); gravity ledger Staleness #58.

6. **V5's definition presupposes Lorentz invariance the primitive layer does not establish — same source, same date.** `Paper_090` §4.4 defines the V5 envelope on *Lorentz-invariant separation*, but P01–P13 contain no statement from which emergent Lorentz symmetry follows. **The kernel layer imports a result the primitive layer has not earned.** Distinct from the `ω_sys` and envelope-shape items already open on V5: this is about what V5's *definition* is entitled to assume. Note the arm-A run reached for the causal-set literature's answer — a Poisson sprinkling at density `ℓ_ED⁻⁴` rather than a regular lattice — and observed that **nothing in P03's discrete locus indexing specifies which**. Gravity ledger Staleness #58.

7. **Item #5 independently corroborated, and the repair's shape identified — F1 arm B, 2026-09-05.** A second uncontaminated run, seeded with the fifteen domain-specific postulates on top of the primitives, reported that **`P-Quadratic-Strain` (`D07`) is** *“the first place in either document where multi-chain dependence appears explicitly”* **and gives** *“a real (if underdetermined) mechanism for chains to affect each other, which the first document's P12 lacked entirely.”* **Two independent readers, from the primitives alone, reconstructed the corpus's actual architecture:** P12 is a single-chain functional and `P-Quadratic-Strain` is what makes it an interaction. **This converts #5 from a criticism into a specification** — the §P12 clause should say that the other-chain dependence enters through the quadratic-strain cross term, not merely that it enters. **But the same run found the cross term supplies no *spatial* dependence either:** *“nothing says two chains at different loci share a channel, or how channel-sharing strength falls off with separation … there's nothing for `grad_adj` to act on across space.”* **So the repair needs a second clause**, tying channel-sharing to locus separation, and that clause is where the `1/R` profile would have to enter at primitive level. `substrate-evaluation/Result_F1_ArmB_2026-09-05.md`; gravity ledger Staleness #59.

8. **~~`Coh` and `Grad` have no operational definition~~ → `Grad` has none; `Coh` does, and I missed it. REVISED 2026-09-05, same day.** **`Coh` is defined and build-verified** in `substrate-evaluation/Paper_PhaseCoherence_P12Coh` §2 as the phase-interference content `Coh = |Σ_a P_a|² − Σ_a|P_a|² = 2Σ_{a<b}√(b_a b_b) cos Δπ_ab`, with four build steps behind it in `../event-density/theory/P12_Coherence_PhaseAlignment_Scoping.md` — rewards alignment, **finite-reach natively** (`ξ ≈ 4–6`), survives being made causal (`flip_frac ≈ 0.48`), **robust to dimensionality** on a 28³ grid. **I checked `Paper_087`/`088`/`026`/`027`/`054` and not `substrate-evaluation`, where the definition has been sitting since July.** The external finding was right about the primitives paper and the gravity arc's own citations, and wrong that the corpus lacks the definition. **The real defect is that §P12 does not point at it** — now fixed with a forward pointer. **`Grad` stands as genuinely undefined** at theory level; the only operational form is a simulator rule. **The two-`Str`s half is downgraded** from *contradiction* to a **naming collision the corpus already half-knew**: the scoping doc calls the certified `Coh` *“arguably mislabeled”*, and naming notes are now placed where a reader will meet them. **A heavier item replaces what was withdrawn — see #10.** Original entry text follows for the audit trail. *(Original:)* **`Coh` and `Grad` have no operational definition, and the corpus's one operational `Str` contradicts `P-Quadratic-Strain`.** **This is the most consequential single finding of the three F1 arms and it is checkable in five minutes.** An uncontaminated model, given only the primitives, opened with: *“P12 names `Coh`, `Str`, and `Grad`, but only D07 gives a computational definition for `Str` … we cannot actually evaluate `Σ_C`, and hence cannot calculate `a_C`, for a specified substrate configuration.”* **Checked and correct at the paper level:** `Paper_087` §P12, `Paper_088`, `Paper_026`, `Paper_027` and `Paper_054` all restate `Σ_C = Coh − Str − Grad` and **none defines `Coh` or `Grad`.**

    **And the one place that does define them makes it worse.** `substrate-evaluation/Paper_BlindnessInvariant_KnotsNotCrystals` §2 gives the *certified* functional operationally: `Σ = k_c·Coh − k_s·Str − k_g·Grad`, with **`Coh = −(ρ_v − ρ_★)²`**, **`Str = ρ_v`**, **`Grad = |ρ_v − ρ_u|`** — all local functions of commitment density. **But `P-Quadratic-Strain` defines `Str_K = |Σ_a P_K^{(a)}|²`, a squared modulus of summed complex amplitudes. Those are different functionals.** Three consequences, all open: **(a)** the corpus carries **two incompatible definitions of `Str`**, one used by the simulator line and one by the gravity line, reconciled nowhere; **(b)** the certified form carries free coefficients `k_c, k_s, k_g` that the primitive's `Σ_C = Coh − Str − Grad` does not have; **(c)** **the gravity arc's `Σ_C` therefore has no operational definition at all** — its `Str` comes from `P-Quadratic-Strain` and its `Coh` and `Grad` come from nowhere. **Every gravitational result in the corpus runs through `a_C = −∇_adj Σ_C`.** `substrate-evaluation/Result_F1_ArmC_2026-09-05.md` §4; gravity ledger Staleness #69.

9. **Universal free fall is unestablished, and the corpus already says so in a paper the finders had not seen — 2026-09-05, two of three external runs independently.** Both reached it from `P12` + `P-Quadratic-Strain`: the cross term scales as `√b_K^{(A)}` in the chain's **own** bandwidth, and `a_C = −∇_adj Σ_C` has **no inertial normalisation**, so two chains of different bandwidth in the same ambient field would accelerate differently. One filed it as a **structural problem** rather than a gap, noting the fix `a_C = (1/b_C)·[−∇_adj Σ_C]` *“directly contradicts P12 as literally stated … amending a primitive, not supplementing the axiom list.”* **Checked, and the check weakens it from *contradiction* to *unproven*.** `substrate-evaluation/Paper_MassWithoutMass_BindingInertia` measures inertia natively — a bound composite under uniform force reaches `v_x = 0.72` against an unbound cluster's `0.97`, so **binding resists acceleration and ED has an inertia mechanism**, which neither run could have known. **But that paper's own preamble item 4 concedes:** *“the equivalence-principle reading is a **consistent interpretation, not an independent proof**.”* **So: ED has inertia and does not have a demonstration that it cancels from gravitational acceleration — and universal free fall is the one kinematic signature distinguishing gravity from any other force.** Gravity ledger Staleness #69.

    > **✅ NARROWED TO ONE EXPONENT 2026-09-07, and the recorded repair is REFUTED** (`gravity/Note_UFF_TheExponentIsWrong_2026-09-07.md`). **The drive carries the test chain's own `√b_C`** because `Coh` is a cross term, so P12 as stated gives **`a_C ∝ √b_C`**. **The `1/b_C` fix recorded in this item gives `a_C ∝ b_C^{−1/2}` — the same violation inverted, not removed** (256× bandwidth range → 16× spread either way). *It was assessed on whether amending a primitive is affordable, never on whether it is sufficient; the prior objection is that it does not work.* **The unique power giving UFF is `1/√b_C`, which is the chain's own AMPLITUDE (`P = √b e^{iπ}`)** — structurally the right object for a drive that is amplitude×amplitude. **Tier: the exponent is `D` (algebra); the amplitude reading is `A → position`; whether the substrate normalises that way stays OPEN.** **And the existing inertia measurement cannot decide it** — it applies a *uniform* force, which holds the drive fixed while varying the body and so removes one of the two things that must cancel; `Paper_MassWithoutMass_BindingInertia` separately reports its own result as *“mobility-saturated, consistent with — but not evidence for — the equivalence principle.”* **The owed test: drive with a term carrying its own `√b_C`, vary `b_C`, check `b_C`-independence.** **⚠ RUN 2026-09-07 on the certified substrate, unmodified — and it DID NOT RESOLVE.** At 24 seeds the amplitude drive gives `n = 8 → 32`: **−0.0438, |z| = 1.7**, not significant **and opposite in sign to the prediction**. *Not banked either way — an underpowered null and a real null are the same number.* **The reason is structural and matters more than the number: the certified rule is ballistic-or-extinct with `argmax` move selection, so the measured `v_x` is a SELECTION PROBABILITY bounded in `[−1,1]`, not an acceleration.** The algebra assumes `a ∝ drive`; **this substrate has no continuous acceleration to be proportional to anything**, which is also why the response saturates at a knee (`F ≈ 0.2`) before the predicted effect can show. **The experiment is right and the instrument is wrong.** Owed now: an observable that is an acceleration rather than a per-step probability, or a continuous-update implementation. **§§1–6 of the note stand on algebra alone and do not depend on this test.**

10. **`|Σ P|²` assigned to BOTH `Coh` and `Str` — OPENED AND CLOSED 2026-09-05. `Str` takes the diagonal, `Coh` takes the off-diagonal, and P12 needed no amendment.** **Settled by a three-way sign check against `Paper_030`'s established `a = a_N + √(a_N a_0)` (`internal notes/_check_sigma_sign.py`, re-runnable): of three candidate assignments exactly one reproduces it, to floating-point precision at every radius tested.** **`Str_K = Σ_a b_K^{(a)}`; `Coh_K = 2Σ_{a<b}√(b_K^{(a)}b_K^{(b)})cos Θ_ab`.** **Why no single `Str` can work:** the Newtonian piece goes as `1/R`, the MOND piece as `ln R`, and `d(1/R)/dR < 0` while `d(ln R)/dR > 0` — **both accelerations attractive requires the two pieces to enter `Σ_C` with opposite signs**, which one squared modulus cannot supply. Options 3a and 3b each get one term right and the other backwards; option 1 leaves no gravitational term at all. **Independent second route, using only P04:** bandwidth is non-negative, so `Paper_QuadraticStrain_v1` §4's *“Φ_N = Σ_K b_K^{(L)} = −GM/R”* **equates a sum of non-negative quantities with a negative number and cannot hold.** Under the settled split it becomes `Φ_N = −Str`, the minus being **P12's own** — **and absorbing that minus into `Φ_N` is exactly what concealed the collision.** **Nothing in the corpus's gravitational results changes**; the check was against them. What moves is one line of one postulate: the off-diagonal's destination. **Bonus, and it is not small: the constructive sign is now required by the assignment.** `Coh` enters with a **plus** and MOND is attractive, so `cos Θ_LH > 0` follows structurally — **a second support for the §9 residual, independent of `Paper_PhaseCoherence_P12Coh`'s measured one.** Applied to `P-Quadratic-Strain` §2, `Paper_QuadraticStrain_v1` §4, `Paper_PhaseCoherence_P12Coh` §5 and `Paper_087` §P12. `foundations/Note_SigmaC_SignCheck_2026-09-05.md`; gravity ledger Staleness #71. *(Original opening text follows for the audit trail.)* **`|Σ P|²` is assigned to BOTH `Coh` and `Str`, which enter `Σ_C` with opposite signs — as written they cancel, and the gravity arc's mechanism vanishes.** Found while discharging #8; **it is not what #8 said, and it is worse.** `Paper_PhaseCoherence_P12Coh` §2 defines `Coh = |ΣP|² − Σ|P|²` (**the interference part**), while its own §5 discharge list states the result as *“`Coh` read as `|ΣP|²`”* (**the full square**) — two values three pages apart, differing by the diagonal `Σ_a b_a`. **And `P-Quadratic-Strain` assigns the full square to `Str_K`.** In `Σ_C = Coh − Str − Grad`:

    - **`Coh = |ΣP|²`, `Str = |ΣP|²`** → identical cancellation, `Σ_C = −Grad`.
    - **`Coh = |ΣP|² − Σ|P|²`, `Str = |ΣP|²`** → `Coh − Str = −Σ_a b_a`. **The cross term cancels exactly** — and `2√(b^{(L)}b^{(H)})·cos Θ_LH` **is the entire MOND mechanism** of `Paper_QuadraticStrain_v1` and `Paper_030`.

    **Both readings delete the interference the gravity arc runs on**, and this appears in no ledger. **Three candidate resolutions, none adopted** because each changes the arc's sign structure: **(1) disjoint index sets** — `Coh` over a chain's own participations, `Str_K` over external sources on channel `K`; **most likely right, cheapest, and one sentence in each paper settles it**, but *neither paper states its index set, which is exactly why the collision is invisible*; **(2)** `Coh` is the interference part and `Str` the diagonal only, which makes the arc work but contradicts `P-Quadratic-Strain` as written; **(3)** the two are one term read at two layers, cleanest if true and the largest rewrite. **Flags placed in both papers; the choice is the author's.** `foundations/Note_SigmaC_Defined_2026-09-05.md` §4; gravity ledger Staleness #70.

12. **The `Str(C)`/`Coh(C)` chain-level bridge — WRITTEN 2026-09-05 (late), and it splits in two.** **Half one, definitional and free:** they are plain sums of their per-channel counterparts over $\mathcal{K}$`(C,u,t)`, the channels `C` participates in at `(u,t)` (P02's four-tuple, restricted; notation already in `Paper_001_PreIndividuation` §3.1). **Plain rather than weighted, by P04 additivity** — which **excludes** the alternatives an external audit listed rather than merely not choosing among them. Written into `Paper_087` §P12. **Half two, a postulate, and the real finding:** what $\mathcal{K}$ contains as a function of a chain's distance from a mass — split in `Paper_030` between the **named** `P-Bilocal-Count` (§4.2) and an **unnamed, un-tiered** radial `1/R` channel density (§4.3) **which is what produces `√(a_N a₀)` rather than nothing.** Flagged in place; a name is proposed in the note only, since naming it in a paper increments the census. **Three audits flagged half one — two asking what the aggregation *is*, one what it *runs over* — and reading them as one item is why this looked like a single sentence with wide reach.** `foundations/Note_StrC_Bridge_2026-09-05.md`; gravity ledger Staleness #80.

11. **`Grad` — a proposal on the table 2026-09-05: compatible, discriminating, not derived. The last undefined term in `Σ_C`.** **AP's own reading of the three terms is *Relation / Boundary / Gradient*, and two-thirds of it was independently confirmed the same day by arithmetic that had nothing to do with it.** The three-way sign check (#10) fixed `Coh` as the **off-diagonal** — which vanishes for a single source and exists only when two contributions meet, *relation* in the strict sense — and `Str` as the **diagonal**, the load against P04's finite per-locus capacity, *boundary*, which is exactly how the soft-matter arc uses it. **Neither was chosen to fit the triad**; both came out of asking which assignment reproduces `a = a_N + √(a_N a_0)`. **That leaves `Grad` as the only inter-locus term, structurally rather than by preference:** a locus has channels and edges, two terms live in the channels, and the third has nowhere to go — matching P12's own `∇_adj`, an adjacency difference. **Proposal:** `Grad = Σ_K Σ_⟨u,v⟩ |P_K(v) − P_K(u)|²`, the **discrete Dirichlet form**, expanding to `b(v) + b(u) − 2√(b(v)b(u))cos Δπ`. **Four supports:** it is the unique natural quadratic edge-difference of the amplitude the other two terms are built from; it reduces to the simulator's `|ρ_v − ρ_u|` under the known tractability reduction; **it is compatible with every established gravitational result** (`a_Grad = −¾ℓ_ED²GM/R⁴`, suppressed by `ℓ_ED²/R²` — `10⁻⁸⁴` at a neutron star, `10⁻¹⁰⁸` at galactic radii; `internal notes/_check_grad_compat.py`); and **it generates the graph Laplacian**, the operator `Paper_026`/`Paper_027` need for `∇²b ∼ ρ` and which **F1 arm C's most-converged gap says the axioms do not supply.** *(A lead, not a claim: a Dirichlet form gives a Laplacian, not a Poisson equation — the source identification is separately missing.)* **The check discriminates even though it cannot confirm:** the contribution comes out **attractive**, which is not automatic — a `Grad` built from bandwidth difference rather than amplitude difference gives a different power and, for some choices, the wrong sign. **Tier: proposal, form-forced-conditional at best. Not derived, and not to be cited as derived.** **Open item flagged rather than buried:** the phase half enters `Σ_C` as `+2√(b_v b_u)cos Δπ`, **rewarding alignment across an edge** — which is what `Paper_PhaseCoherence_P12Coh`'s probe actually measures, depositing phase from committed **neighbours** via the P05 connection. **So that measurement may belong to `Grad` rather than `Coh`.** Nothing breaks either way — both reward alignment, and #10 settled `Coh` vs `Str` from the gravity target independently — **but it bears on the `cos Θ` discharge, which cites the measurement as one of its two supports.** ~~**Cheapest resolution: re-run the probe with the phase assigned to `Grad`.**~~ **RUN the same day, and the item does NOT close.** **Physical case (C): finite-reach survives under `Grad`**, so the `cos Θ` discharge keeps both supports. **Condition (A): the `Grad` reading crystallizes** (`R ≈ 0.9`, `ξ` = grid) where `Coh` stays finite-reach. **So each reading has a crystallizing sub-case, each defended by the same argument, and Knots-safety cannot discriminate.** **The proposal above now carries a known problem it lacked when written.** New discriminator found: `ξ` flat (≈ 4) for `Coh`, monotonically shrinking (`2.2 → 0.8`) for `Grad`. `foundations/Note_Grad_Proposal_2026-09-05.md`, `Note_PhaseInGrad_Probe_2026-09-05.md`; gravity ledger Staleness #72–#73.
