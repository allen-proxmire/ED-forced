# Constants-Inherited — Tiered-Claims Ledger (+ the constants list)

**What this is.** The tier catalog for the one-paper `constants-inherited` folder (`Constants_Ledger_and_G_Dimension.md`). Adapted: alongside the paper's tiered *claims*, it **lists the fundamental constants themselves** — each as its own row under a `Constant` tier — so the sortable master carries "which constant, present when, derived or inherited." (Per AP's request, 2026-07-29.)

**The paper's own honest tiers** (its §6): the constants ledger is *grounded*; `G=c³ℓ_P²/ħ` + `[G_d]=m^d` is *solid*; `G_d=c³ℓ_P^{d−1}/ħ` (c-power dimension-blind) is *derived*; the dimension-signature-is-holographic reading is *grounded synthesis*; the becoming/memory/extent + G-as-first-shadow framings are *interpretive* (not theorems). This folder is **mainly assertion/synthesis** — and that is stated as such.

*Currency: re-read 2026-09-04 (external audit). This was the only arc ledger of fifteen carrying **no stated read date**, and it had missed the same-day `Paper_027` work — **P-G-Closure**, the multi-use parsimony defence, and the **F-P** result — all of which is now folded into the `G` row below. The `G_d = c³ℓ_P^{d−1}/ħ` generalisation was checked dimensionally and is sound. See `gravity/Gravity_TieredClaims_Ledger.md` Staleness #26.*

---

### The constants (each listed; tier = `Constant`)
**Event-scale — present at the first events (dimensionful, = 1 in natural units → the beginning carries *units, not numbers*):**
| Constant | Source | What it is | Status |
|---|---|---|---|
| **c** — the rate of becoming (tick→hop conversion) | Paper_012 (P-RB-1) | present as soon as one event influences a neighbor | **Constant (event-scale)** |
| **ħ** — the action of one commitment (the grain of memory) | Paper_087 | the action-cost of laying down one record (P11 forbids erasure) | **Constant (event-scale)** |
| **ℓ_P = ℓ_ED** — the grain (size of one hop) | Paper_087 (P08) / 089 | `ℓ_ED=ℓ_P` set empirically via Newton-recovery (Paper_027 §5.3) | **Constant (event-scale)** |

**Derived combination (no new input):**
| Constant | Source | What it is | Status |
|---|---|---|---|
| **G = c³ℓ_P²/ħ** | Paper_027 | a *combination*, not a **second** independent input — **but not therefore explained.** *(Corrected 2026-09-04; previously read “adds no information”.)* `ℓ_ED` is itself fixed by matching `G` (§5.3, row above), so the two carry **one** piece of information and which is called the input is a labelling choice. The equality — coefficient exactly 1 rather than `G ∼ c³ℓ²/ħ` — requires the labelled postulate **P-G-Closure** (`Paper_027` §2, named 2026-09-04). **The real parsimony is multi-use:** `ℓ_ED` is one postulated scale spent in the holographic count (025), the BH area law and entropy coefficient (043, BH_Thermal2Pi) and the UV cutoff (111), so a rival needs `G` *and* a grain *and* a cutoff. **F-P run 2026-09-04:** all three companion roles **inherit** `ℓ_ED` from §5.3 — one determination, three uses, no independent confirmation; **absent from the entire Standard Model**; the "first shadow" at the mass↔geometry seam — least-fundamental yet first-emergent | **Constant (derived-combination)** — `G_d=c³ℓ_P^{d−1}/ħ`; c-power dimension-BLIND; the dimension lives in `ℓ_P^{d−1}` = the holographic area-element |

**Inherited / late (need the cosmos or matter to have accumulated — the universe's "personality"):**
| Constant | Source | Status |
|---|---|---|
| **a₀ = cH₀/(2π)** (MOND scale) | Paper_029 | **Constant (inherited/late)** — needs a Hubble horizon; so late it *evolves*, a₀(z) |
| **Λ** (cosmological constant) | Paper_038_5 | **Constant (inherited/late)** — value via H₀; w=−1 forced; smallness open |
| **H₀** (Hubble rate) | cosmology | **Constant (inherited)** — empirical |
| **particle masses; gauge couplings g; α≈1/137; mass ratios** | matter sector | **Constant (inherited)** — the physics-defining dimensionless numbers, "the unfinished room" |

### The paper's tiered claims
| Claim | § | What's derived | Status |
|---|---|---|---|
| Constants ledger — c/ħ/ℓ_P present at the first events (dimensionful, ≈ units); a₀/Λ/H₀/masses inherited late (dimensionless, the personality) | §1 | P-RB-1 + P08 + Paper_027 + Paper_029; masses inherited | **Grounded** |
| `G = c³ℓ_P²/ħ`; G absent from the SM; `[G_d]=m^d` (G's *units* know the dimension count) | §3 | corpus + dimensional analysis | **Solid** |
| `G_d = c³ℓ_P^{d−1}/ħ`: the c-power is 3 in **every** dimension (a time/mass count `G`'s 2 + `ħ`'s 1, an ironic 2+1, *not* the 3+1 of spacetime); the spatial dimension lives **entirely** in `ℓ_P^{d−1}` | §4 | dimensional analysis; the `ℓ_P^{d−1}`↔area identification uses Paper_027 §3 | **Derived** — the one genuinely-computable result; **"c³ = 3D" is a coincidence, dropped** |
| The dimension-signature in `G` (`ℓ_P^{d−1}`) **IS** the holographic reach-law road — NOT a new independent road to 3D | §4 | ties Paper_027 §3 to Paper_MetricFromTheGraph (bandwidth = boundary cut `~R^{d−1}`) | **Grounded synthesis** — reinforces the holographic road; not a 4th witness for 3D |
| ħ-as-grain-of-memory; becoming/memory/extent (c/ħ/ℓ_P); G-as-first-shadow | §2, §3 | interpretive framing, grounded in the phase/commitment structure | **Asserted (framing, NOT theorems)** |

### Honest arc-state
A one-paper synthesis, honestly tiered by its own §6. Its spine: **the constants sort into event-scale (c, ħ, ℓ_P — present, dimensionful, ≈ units) vs. accumulated (a₀, Λ, H₀, masses, couplings, α — inherited, dimensionless, the universe's late personality); `G` is the first *shadow* at the mass↔geometry seam (a rearrangement of the three event-scales, absent from the SM); and its lone dimensional fingerprint is the holographic area-law (`ℓ_P^{d−1}`), not the speed of light.** The one genuinely-computable result — `G_d = c³ℓ_P^{d−1}/ħ`, c-power dimension-blind — is small but clean. Everything else is grounded-synthesis or interpretive framing, and says so. **Defensible headline:** *the constants ledger, stated at the tier it earns — three event-scale units present at the start, `G` the free first shadow at the seam (its dimension in the area-law factor, not `c³`), and the dimensionless zoo inherited late; one clean derived result (`G_d`), the rest honest synthesis/framing.*
