# Gravity Arc — Tiered-Claims Ledger

**What this is.** Every load-bearing claim in the gravity folder (the corpus flagship) and its *current* tier. Three things at once: (1) the **tier catalog**, (2) the **staleness detector** (the `Status` column is current state; any doc disagreeing with a row is by definition stale), (3) the **anti-drift authority** `read-first` checks against.

**How it's built.** Read every paper; tier each claim *from the paper's own audit table / "does NOT claim" / verdict* — never inflated. Seeded from the folder README + `event-density/docs/ED_Research_Targets.md` (#1, #5, #5b), corrected against the papers where they disagree.

**One of a per-folder set.** Companions: `entanglement/…`, `black-hole/…`, `cosmology/…`, `dark-sector/…`. This folder *synthesizes and stands* (it holds the standing KM-I MOND result — do not reinvent it).

**Tier key.** `Derived` (from primitives/structure, conditioning postulate noted) · `Grounded` (structural/conditional/account-tier) · `Measured` (simulation/probe) · `Selected/Inherited` (a value: G, ℓ_P, a₀, Λ-magnitude) · `Postulated` · `Asserted` (A→position) · `Prediction` · `Synthesis` · `Open`. `D-cond` = derived conditional on a named postulate.

*~30 papers. Read 2026-07-29 (extraction agent, then spot-checked); scope items propagated 2026-09-03 (see Staleness #9); the a₀ `1/(2π)` re-tiered to disputed 2026-09-04 (see Staleness #10). Honest-arc-state paragraph reconciled with Staleness #1 on 2026-08-30: it had continued to describe the a₀(z) propagation as the folder's urgent open task after Staleness #1 recorded that propagation as DONE on 2026-07-29 — the ledger contradicted itself on its own headline debt. No claim tier changed.*
*Spot-checked directly against the papers (7, scaled to the 30-paper flagship; **no catch** — the extraction was reliable throughout): (1) **GR-IV** α₂=0 exact + the c_T=c/c_s=c causal-cone framing + literature-verification — confirmed verbatim (§3, boxed); (2) **029** a₀=cH₀/(2π) form-derived, H₀ inherited, and the STATIC "tracks whichever H₀" reading (no a₀(z)) — confirmed (§8.5); (3) **031 §8.8** carries the MUSE-DARK III (A&A 2026) ~30σ a₀(z) confirmation — confirmed verbatim; (4) **038.5 §3.5** "updated 2026-07-16," w=−1 resolved, Λ=frozen floor, force-vs-stipulate residual — confirmed (this makes the MEMORY pointer stale, not §3.5); (5) **MetricFromGraph** metric KINEMATIC/static-linear, nonlinear OPEN, exactly-3 not a forcing — confirmed (preamble #2, §5); (6) **027** G=c³ℓ_P²/ħ downstream identification, ℓ_P value-layer — confirmed; (7) **QuadraticStrain** discharges P14 (geometric mean = forced interference modulus) + the regime assumption, residual = constructive sign — confirmed.*

---

### Derived (from primitives/structure; conditioning postulate noted)
| Claim | Paper | What's derived | Inherited / conditioning | Status |
|---|---|---|---|---|
| Temporal lapse `N²∼b` (the core new result) → weak-field Einstein metric | GR-I | front-null + commitment-rate law ⟹ `N²∼b`; Schwarzschild branch (not Nordström) | **D-cond on P-Commitment-Linear** (α=1) — later *forced* in GR-III §4 | D-cond |
| Emergent spatial metric `g_ij ∼ b⁻¹`; factor-of-two light bending | GR-I | bandwidth realization of the 033 acoustic metric; `n_opt∼b⁻¹` = square of spatial index (sim 2.09 vs 2.00) | conditional on the 13 primitives; sim uses imposed Newtonian-limit b-profile | D + simulation |
| α=1 (P-Commitment-Linear) **forced**; Nordström excluded | GR-III §4 | P04 reserve monotone-draining (P11) ⟹ β=0 ⟹ α=1 | D-cond on a named band-accounting premise (metric band = rate-numerator band) | D-cond |
| Khronon scalar speed `c_s = c` | GR-III §6 | reserve sector dissipative (P11), not kinetic ⟹ no second cone | leading order; sub-leading reserve renormalization benign | D (leading order) |
| Field-equation form `G^μν+Λg^μν=κT^μν` | GR-II §4 | Lovelock's theorem, given metric + conserved source + 2nd-order | **forced iff purely metric — the open condition, which §5 fails** (ED is khronometric, not pure GR) | D-conditional |
| `∇_μT^μν=0 ⟺ geodesic motion` | GR-II §3 | forced by P05 transport typing + P04 extensivity; exact for dust | pressure/anisotropic-stress need full rule | D |
| ED gravity is **khronometric** (Einstein tensor sector + 1 khronon); gauge = foliation-preserving diffeos | GR-II §5 | arrow (P11/P13) in the law ⟹ preferred foliation ⟹ mode count 2+1 | D-structural (not a completed linearized propagator). **Infrared identification only** — the class is Hořava's IR limit and ED does **not** inherit the Hořava UV programme: ED's V1 kernel is Lorentz-covariant (`z=1`) and the UV is regulated by the grain cutoff, not by anisotropic Lifshitz scaling at `z=3` (GR-II preamble item 10, added 2026-08-31) | D-structural |
| `c_T = c` structurally (GW170817 as identity); universal (not differential) Lorentz violation | GR-II §6–7 | one substrate, one transport (P05), one cone → shared by all species | F-independent. **Split clarified 2026-09-04 (Staleness #29):** the **identity** (tensor and light share one cone) is structural and is this row; the **value** of the shared speed is **inherited** via P-RB-1. `dynamics/Paper_ED_GW_00` marks the whole row *Inherited*, which is right for the value and loses the identity — both ledgers now carry the split | D-structural (identity) / Inherited (value) |
| **α₂ = 0 exactly** | GR-IV §3 | khronometric PPN numerator vanishes on the scalar-luminal surface once `c_T=c, c_s=c` — **the same causal cone as GW170817's `c_T=c`; one fact, two faces** | literature-verified (Blas–Lim, Ramos–Barausse, Foster–Jacobson) + numerically checked | **D + literature-verified** |
| `α₁ = −4λ_local`, `λ_local ∼ ρ_event/ρ_Planck`; commitment **forced** sparse (⟹ λ_local≪1, ≥70 orders safe) | GR-IV §3–5 | metric stiffness always-on (P02); khronon stiffness only where commitments fire (P11); dense commitment = Zeno ⟹ no QM, and ED has a QM sector | prefactor O(1) tied to κ/D=8πG | D-structural |
| Holographic bound `N(R) ≤ 4πR²/ℓ_ED²` (area law as edge-count) | 025 | channel-count on a closed 2-surface | **P-Codim-1 + P-Sat**; ℓ_ED value inherited | D (given the two postulates) |
| Newton `a_N = GM/R²` from the cumulative-strain reading | 026 / 027 | holographic source-resolution; `N(R)` factors cancel ⟹ `Φ=−GM/R` | **P-Potential-Reading** (Model A); V1 1/R envelope inherited (DCGT) | D (given P-Potential-Reading) |
| Crossover `R_H = c/H₀` | 028 | kernel speed = Hubble recession | statistical-not-geometric boundary is a **P** ontological commitment | D (form-forced) |
| The `1/(2π)` azimuthal-Fourier factor in a₀ | 028 §6 / 029 §5.1 | *was* recorded here as form-forced from period-2π orthonormality | **RE-TIERED 2026-09-04, see Staleness #10.** The orthonormality factor cancels in both papers' own displayed algebra; the surviving `1/(2π)` enters through a chosen normalization | **Postulated / disputed** (was: D form-forced) |
| Reach law `p = 1/(d−1)` ⟹ `g∼1/b` in **exactly 3D** | MetricFromGraph P2 | ball-cut exponent = d−1 (holographic surface count); 3D uniquely reproduces GR-I | **derived conditional on the surface-count**; metric is **KINEMATIC/static-linear**, nonlinear OPEN; exactly-3 is internal coherence, **not a proof ED forces 3D** | D-conditional |
| Free chains follow acoustic-metric geodesics | 032.5 (GR-3A) | eikonal/stationary-phase ⟹ `S=−mc∫dτ` ⟹ geodesic eq | **FORCED-conditional on P-FreeChain + guardrails (035)**; eikonal→action step OPEN; verdict M3 | D-cond (M3) |
| Deep-MOND asymptotic `a=√(a_N a₀)`; flat-background AQUAL field eq | 034 / 036 | `μ(x)→x` + field eq + spherical geometry; AQUAL Lagrangian variational | **FORM-FORCED** given P-MOND-Interpolation / P-MOND-Field-Form; μ form inherited empirically | D (form-forced) |
| a₀ continuum-limit invariant (does not RG-run) | 037 | c invariant × H₀ invariant | **P-H0-Cosmological-Invariant** postulate | D (given postulate) |

### Grounded (structural / conditional / account-tier)
| Claim | Paper | What's derived | Inherited / open | Status |
|---|---|---|---|---|
| **The khronon's deep field IS the MOND sector** (unifies the two gravity programs) | KM-I | promoting the acceleration term to W(A²) ⟹ AQUAL in the metric potential; deep-IR embeds 030 | **single hinge = "forced-given-030"** (matches the derived Combination Rule, does not re-derive); interpolation a Cassini-constrained family | Grounded (single hinge) — **the standing MOND result; do not reinvent** |
| Lensing tracks the MOND potential, **no added vector**; no new ghost; AQUAL ellipticity; PPN-safe | KM-I §5–6 | metric-borne modification; unitary-gauge no time-derivatives | slip O(Φ)-suppressed | D / D-structural |
| Orthogonality theorem: `θ≡0` in statics ⟹ the regulator family is invisible to every tested result | KM-II §4 | kinematics of static congruences | the cleanest result; sequesters the cosmology sector | D |
| Clustering dial = the soft spot (one dial sets degeneracy-cure + Čerenkov + clusters/CMB) | KM-II §6 | mapping | class mechanisms are **directions, not verifications**; abundance = value-inherited IC | D-structural (mapping) |
| Scalar-tensor acoustic-metric field eq (covariantization) | 033 | opens Arc ED-10 | **three P-postulates**; "conditional-positive"; deep-MOND superluminality A→cost (see Staleness #4) | Grounded (P-construction) |
| Six ED-10 weak-field prerequisites fulfilled at form level | 032 | enumerates WF-1..6 with falsifiers | WF-3 conditional on the P14 placeholder; sufficiency for curvature = A→assertion | Grounded (specification) |
| Six acoustic-metric guardrails (C1–C6) | 035 | conditions for acoustic-metric consistency | all six are **P (postulates)** | Postulated set |
| Λ as a V1 cosmological-scale integral over `R_H`; **Λ constant, w=−1 forced, = frozen saturation-floor** | 038.5 (incl. §3.5) | form-IDENTIFIED (V1 finite 2nd moment + curvature coupling + R_H domain); live-horizon Λ ⟹ w=0/dead, so only the frozen floor accelerates | value inherited; verdict **M3** conditional on Route-A; **residual OPEN: force vs stipulate the frozen value** (§3.5, updated 2026-07-16) | Grounded (form-ID, M3) + open residual |
| One-Λ thesis: `𝒲₀ = −24π²Ω_Λ ≈ −162` | KM-II §7 | shared SCBU boundary; scaling exact; sign passes | **D-via-I** (tier transported from 038.5, not upgraded); ab-initio integral OPEN with a numeric target | Grounded (thesis) |

### Measured (simulation / probe)
| Claim | Paper | What's measured | Status |
|---|---|---|---|
| Dynamical rule `ḃ=D∇²b−κρ` built; steady state `∇²b∼ρ` (corr 0.999) | GR-III §7 | fixed point of the two forced terms | measured |
| Schwarzschild `r_s∝M` (2D+3D); emergent frozen `b→0` horizon (= metric horizon = V5 surface); area-law `S∝A` | GR-III §7 | deficit linear in source; horizon forms with nothing about a horizon in the rule; severed-channel count scales as perimeter | measured (¼ coeff inherited) |
| Hawking scaling `κ=1/(2r_s)∝1/r_h` | GR-III §8 | derived (vacuum solution + `g₀₀g_rr=−1`); corrects an earlier "flat" measurement error | derived; direct sim resolution-limited |
| Phase-coherence constructive sign supplied (P12-Coh rewards alignment) | QuadraticStrain §9 update | build-verified 2D/3D; finite-reach not crystalline | measured (conditional on P12-Coh operationalization) |
| Integer-reach coupling floor on the participation graph | MetricFromGraph preamble item 7 | the discrete reach rule leaves a residual coupling that does not vanish with depth: **0.00% / 2.70% / 4.25%** across the three tested bandwidth depths | measured (2026-08-31); a floor of the integer-reach construction, not a physical prediction |

### Selected / inherited (a value taken from measurement)
| Value | Paper | Status |
|---|---|---|
| `G = c³ℓ_P²/ℏ` | 027 / 027.5 | **form-derived, value-INHERITED (M3)**; `ℓ_ED=ℓ_P` is the empirical-matching choice, not a substrate derivation |
| `a₀ = cH₀/(2π)` | 029 | form-derived **at the `~cH₀` level**; ~10% match. **H₀ inherited**; the `1/(2π)` is disputed as of 2026-09-04 (Staleness #10), so "parameter-free" does not currently hold. 029's reading is *static* — see Staleness #1 |
| Λ magnitude `ρ_Λ ≈ 1.1×10⁻⁵² m⁻²` | 038.5 | value-INHERITED; smallness = `(H₀/M_P)²` via Friedmann, gated on Route-A |
| Thermodynamic coefficients (¼ in S=A/4; T=κ/2π) | GR-III | value-inherited (the ¼; and see `black-hole/…` for the geometric-2π derived route) |

### Postulated (paper-specific, load-bearing)
| Postulate | Paper | Note |
|---|---|---|
| P-Commitment-Linear (α=1) | GR-I | later **forced** in GR-III §4 (chronology: discharged downstream) |
| **P-G-Closure** (the dimensionless coefficient in `G = c³ℓ_ED²/ħ` is unity) | 027 §2 | **named 2026-09-04**, previously carried by "the unique combination with the correct dimensions." Dimensional analysis fixes the powers, not the coefficient; `κ_V1σ(M) = GM` cannot fix it either, since `G` is on both sides. Not independent of the `ℓ_ED = ℓ_P` identification |
| **P-Bilocal-Count** (`N_bilocal = N_horizon = N(R)`) | 030 §4.2 | **named 2026-09-04**, previously absorbed into a "conventions chosen consistent with" clause. Decides the arc: the alternative reading `N_horizon = N(R_H)` suppresses the cross-term by `R/R_H ≈ 2.3×10⁻⁶` and destroys deep-MOND. Falsifier F-BC |
| P14 Bilocal Strain Coupling (geometric-mean) | 030 | **postulated, non-unique** (§8.4/8.9); later **discharged** by QuadraticStrain as the forced interference modulus |
| P-Quadratic-Strain (Model C) | QuadraticStrain | one postulate replacing P14 + 030's regime assumption |
| P-Codim-1, P-Sat (025); P-Potential-Reading (026); P-AcousticMetric + P-Scalar-Tensor-Form + P-MOND-Interp-Covariantization (033); P-MOND-Interpolation (034); P-MOND-Field-Form (036); P-H0-Cosmological-Invariant (037); P-FreeChain (032.5) | various | the folder's paper-specific postulate stack |

### Asserted (A→position, the paper's own label)
| Item | Paper |
|---|---|
| "6 prerequisites ⟹ Arc ED-10 viable" (necessity, not sufficiency) | 032 §5 |
| Superluminality structural cost "FORCED" (composite verdict) | 034 §3.3 |
| Verdict M3 rows (per Paper_095) | 027.5, 032.5, 033, 038.5, 038.6 |

### Prediction (falsifier given)
| Prediction | Paper | Falsifier | Status |
|---|---|---|---|
| BTFR `v⁴=GM_b a₀`, slope **exactly 4**, zero intrinsic scatter | 031 | slope≠4 or irreducible scatter (data: 3.95±0.08, ~0.1 dex) | current — **slope-4 + tightness are MOND-SHARED**; only the a₀ normalization is ED-distinctive |
| **`a₀(z) = cH(z)/(2π)`** evolves factor-for-factor with H | 031 §8.8 (data-confronted), 038 CO-3 | departs from cH(z)/2π | current — **only 031 §8.8 reflects the 2026 data** (Staleness #1); ~30σ evolution confirmed, exact rate α=1 the open number, data mildly faster |
| `α₁, α₂` preferred-frame | GR-II/III/IV | any nonzero `α₂` kills it (α₂=0 exact); `α₁≳10⁻⁵` kills it (α₁≲4×10⁻⁹³ safe) | current — the keystone kill-switch, passed favorably |
| Scalar (breathing) GW polarization at c | GR-II/III | definitive absence excluding the khronon mode | current |
| Wide binaries near a₀ depart from Newton | KM-I, One-Field | a clean Newtonian result at relevant precision | current |
| Lensing identity (no slip beyond O(Φ)); activity-dependent weak lensing `∝|∇μ|²` | KM-I §5 / 038.6 | measured slip; activity-independent lensing | current — 038.6 **PROVISIONAL** (α_act, 𝒜(x), the discriminator observable all OPEN) |
| CMB dust dial / cluster sector | KM-II | no `𝒲(A²,Θ)` member satisfies all five filters | current — clusters/CMB OWED |
| `𝒲₀ = −24π²Ω_Λ` (ab-initio integral must hit it) | KM-II §7 | wrong sign or magnitude off by >O(1) | current — an open number-to-miss |
| Offset–velocity "knee" law `Δr(v_rel)`: flat–knee–line–ceiling | 117 / OffsetVelocity | ΛCDM velocity-independent; MOND-EFE smooth roll-off; ED sharp knee at `v_crit~150 km/s` | current — **only the SHAPE is ingredient-robust**; the knee location rests on a flagged `ν≈0.70` (3D O(3)) assumption |

### Synthesis
| Item | Paper | Note |
|---|---|---|
| "One field, one scale, five roles" (arrow, Einstein sector, MOND, clustering dial, Λ-scale) | One-Field Letter | capstone; **carries a 2026-07-19 correction banner** downgrading "dark matter is not a particle" to a *candidate* and flagging the khronon-alone abundance gap (needs a real relic — the dark-sector two-component picture) |
| Whole-program synthesis, tiered, GR-IV folded in | PhilPapers | self-contained; α₁/α₂ front closed favorably; open debts named |
| **MOND = interference cross-term**: Newton=diagonal, MOND=off-diagonal of `|ΣP|²` | QuadraticStrain | discharges P14 (geometric mean = forced interference modulus) + 030's regime assumption (horizon is a dipole, not a monopole); STRUCTURAL, conditional on P-Quadratic-Strain; residual = constructive sign (supplied 2026-07-08 at measured tier) |

### Open (declared)
| Item | Paper | Status |
|---|---|---|
| Bandwidth ↔ strain/flow substrate-route identification | GR-I §7 / KM-I | OPEN |
| Primitives-level origin of the deep-IR (geometric-mean) branch | KM-I / PhilPapers | OPEN — "guarded," deliberately unattempted |
| Ab-initio V1 boundary integral for Λ (target `𝒲₀=−24π²Ω_Λ`); Route-A closure | 038.5 / KM-II | OPEN — highest-leverage open derivation |
| Whether Λ frozen-floor value is **forced or stipulated** | 038.5 §3.5 | OPEN (bootstrap knot; driver is matter-dilution, Cos_05) |
| Nonlinear / strong-field field equations; background-free construction | MetricFromGraph / PhilPapers | OPEN — the nonlinear regime is the distinctive/falsifiable frontier |
| **O-ε′: the khronon scalar-speed renormalization** `c_s = c(1+ε′)` from integrating out the dissipative reserve | GR-III §6 / GR-IV §10.5 | **OPEN, opened 2026-09-04.** One number. `α₂ = O(ε′)` inherits it exactly (the `α₂` numerator at `β=0` *is* the scalar-luminal condition), so it decides whether F0 is stateable, whether the causal-cone pairing is two exact facts or one-and-a-half, and whether `α₂` is a GR-shared survival or a discriminator. Same integration GR-III §6 declines |
| Clusters + CMB not discharged; khronon-standalone **abundance gap** (now carried by a separate two-component warm relic) | KM-I / KM-II / One-Field banner / 117 banner | OPEN — see `../dark-sector/` |

### Staleness, currency & cross-folder propagation
1. **a₀(z) + 2026 MUSE-DARK III data — propagation DONE 2026-07-29.** The canonical treatment is `predictions/ED_Master_Predictions_List` **1.15** (2026-07-14, A10: direct fit α≈1.18±0.04, MOND dead at 29σ, ED's α=1 at +4.4σ→~1–2σ w/ systematics, author-corroborated "faster than H(z)"; verdict: call confirmed, exact rate mildly disfavored, **not** a refutation); `Paper_031 §8.8` carries the compact version. **These predated it, now UPDATED:** **Paper_029** (added §8.8 — the evolution + data, distinct from its static §8.5); **Paper_037** (added preamble item 6 — continuum-invariance ≠ cosmological constancy; a₀ still evolves with cosmic time); **Paper_038 §3.3 CO-3** (added the data-confrontation paragraph); **Predictions_and_Falsifiers** (added a Tier-2 live-test bullet + Milgrom-row note + footer); **predictions/22_Ways #1** (sharpened to the evolving scale + status); **predictions/Paper_101** (promoted a₀(z) from a §0.3 postdiction parenthetical to **Tier-1 weapon row 2**). *Not touched (synthesis papers, lower priority):* KM-I/KM-II/One-Field/PhilPapers still use a₀=cH₀/2π without the evolution confrontation — a candidate second pass if desired. *Do not over-bank: direction confirmed, exact rate under tension.*
2. **"Wolfram Ruliad" in 4 peer-facing position statements** (027.5 §6, 032.5 §6, 038.5 §6, 038.6 §6). Violates the no-Wolfram-in-public rule. **AP's standing call: leave it.** Record-only. (PhilPapers uses the softer "Wolfram programs" once.)
3. **038.5 §3.5 — the MEMORY POINTER is stale, not the paper.** §3.5 was **updated 2026-07-16** with the w=−1 resolution + the force-vs-stipulate residual + F3. The memory note `project_lambda_constant_vs_a0_horizon` ("stale flag = 038_5 §3.5") is what needs correcting. **Fixed in memory 2026-07-29.**
4. **Deep-MOND "superluminality cost" (033 §3.4, 034 §3) re-tiered to "feature" by the KM line** (KM-I §8, GR-II §9: the superluminal scalar is the khronon class's universal-horizon character, not a pathology). Chronology-aware: 033/034 predate the KM line and were true when written; a reader hitting them first sees a "cost" the corpus has re-tiered. Record-only (or a one-line forward-pointer banner if desired).
5. **Paper_117 correctly repositioned** (2026-07-19 banner: relic account primary, the topological-defect offset-velocity law demoted to a speculative bonus). The banner governs; OffsetVelocity/F3DataTable cite it as a bonus. **Discharged** (the seed's staleness concern is already handled) — though the body §1–6 still narrates the defect mechanism as if primary, under the governing banner.
6. **Paper_032.5 (GR-3A) superseded-in-part** by GR-III's limit-forced timelike-geodesic reduction; it still frames geodesics as FORCED-conditional on the older guardrails. Not a contradiction (chronology); the GR-III/GR-I treatment is the standing one.
7. **Curvature-emergence nuance holds — no over-claim found.** MetricFromGraph explicitly tiers the emergent metric as **kinematic/static-linear**, nonlinear Einstein OPEN, exactly-3 as derived-conditional (not a forcing). GR-I/II/III consistently say ED gravity is khronometric, not pure GR.
55. **`soft-matter` gained the corpus's sixteenth arc ledger 2026-09-04, having never had one.** **Built from each paper's own audit table, not from a reading of the physics** — the method every other arc ledger states. 17 papers, every row carried at the label the paper gives it (`I`, `P-<name>`, `D-via-I`, `A→position`, `OPEN`). **The arc's `Derived` tier is effectively empty, by its own admission.** `Paper_086_5` says outright it has *“no D rows in audit”*; `Paper_080` marks universality of `Q ≈ 3.5` **NOT CLAIMED**; `Paper_077` declines the constructive NS proof and rests at `A→position`, Intermediate Path C. The single composite `D` row — `Paper_075`'s `D = 3+1` — is conditional on seven PUA axioms the same table tiers **P**, with their substrate origin tiered **I**. **So the arc claims less than its subject matter would tempt anyone to claim**, which is the pattern the clean arcs share. **Its strongest content is a negative, and that is worth saying out loud.** `Paper_082` establishes three independent ways that **advection and induction are frame-artifacts of the convective derivative, not ED content**. A framework able to say precisely which parts of a standard PDE are *not* its own is doing something harder than claiming the whole equation. **Postulate-dense: 24 paper-specific postulates across 17 papers**, the highest per-paper load of any arc, and mostly single-use. By `POSTULATE_BASIS.md`'s ladder that is the expected shape for an applications arc rather than a foundations one — but it should be *stated* rather than discovered, so the ledger names all 24 in one block. **Six staleness items recorded**, including two that matter beyond this arc: `Paper_086_SoftMatter_Synthesis` still quotes `ξ_canonical` to unsupported precision in its body (flagged, not corrected); and **this arc supplies one half of research target #15's singularity family** — `Paper_077` + `Paper_084` are the NS arm of the P04-cap + V5-cap + P11-ratchet triple — **whose unifying lemma is a banked negative.** The ledger says *do not re-attempt the lemma without reading that negative*, which is exactly the reinvention this corpus keeps having to guard against. **Also flagged: two of the 17 are working drafts, not publication status** (`Paper_UnifiedP04TransportBudget`, `Paper_P04TransportBudget_ResistivitySaturation`) — and **the arc's only Prediction row rests on one of them.** **Coverage claims corrected in four places**, since ≦54 showed the corpus was counting its instrument: `CLAUDE.md` (fifteen → sixteen arc ledgers), `PAPERS_INDEX.md` (the audit swept *the fifteen that existed*), `Foundations_TieredClaims_Ledger.md` (*the fifteen **ledgered** arcs*), and the audit summary's coverage table (soft-matter now reads **still not audited, but auditable**). **And it validated this morning's census fix.** Writing 24 postulate names into a brand-new ledger moved the count by **zero** — which is the correct behaviour and precisely the scenario that inflated it before the `is_paper()` rule was applied to the headline count (≦53). The tool now distinguishes a postulate being *used* from one being *written about*. **The arc is visible; it is not checked.** No external claim-strength pass (checklist §9 items 19–21) has run on it. Building the ledger was the prerequisite, not the audit — and that is now the outstanding item.
54. **`SC_4_1`, `SC_4_4`, `SC_4_5` migrated 2026-09-04 — and the last pointer found a sixteenth, unaudited arc.** **Three more migrated: `SC_4_1` (BH horizon `r_H` ↔ `R_H`), `SC_4_4` (`ℳ_crit`), `SC_4_5` (`Q ≈ 3.5`).** All three are **M3 FORM-FORCED + VALUE-INHERITED**, unlike `SC_4_2`'s banked negative. Each carries a read-this-first banner with the disputed `2π`, the `§6 “Rewrite Note”` checklist flag (failure mode 5), an explicit **“not audited”** line, and its own arc caveat. **The census did not move (171)** — they add no postulates, exactly as ≦52's prep predicted. **Arc caveats, one per paper, because each anchors a number its own arc has already qualified.** `SC_4_1`: it **predates and does not cite** `Paper_047_5`'s four-horizons-one-object M3 result or 2026-09-04's derivation of `Γ_cross` collapsing at `min(R_a, R_H)` — read as the earlier, coarser statement of a relation the gravity arc has since sharpened. `SC_4_4`: it anchors on `ℳ_crit = 140–250 kDa` three times, which the q-compute ledger tiers *“do NOT bank as firm, a 2-POINT extrapolation”*, and whose shape-retreat carries the ≦25 disclosure gap. `SC_4_5`: `Q ≈ 3.5` is canon-internal by the paper's own statement. **Kept together in `cosmology/` as one series, with pointers added from the arcs they project into** — `black-hole` and `q-compute` ledgers, and the `soft-matter` README. The series coheres around `SC_4_6`'s unification, so splitting it across four folders would lose that; the arc pointers restore findability in the other direction. **And wiring the third pointer found a sixteenth arc.** `soft-matter/` has **18 papers and no `*_TieredClaims_Ledger.md`** — the only content arc in `physics-papers/` without one. **This is why it was invisible to the 2026-09-04 claim-strength audit:** that pass swept the *ledgers* and reported covering “all fifteen arcs”, so an arc with no ledger was never in the set, and was not recorded as unaudited either. **The audit's coverage claim was measuring its own instrument.** (Compare ≦42's lesson from the other direction: the corpus's navigation determines what gets looked at.) **It is not an empty arc.** `Paper_077_NS_Smoothness_R1` and `Paper_084_VortexStretching` are one of the two executed results behind research target #15's singularity family, and `Paper_086_SoftMatter_Synthesis` was flagged today for quoting `ξ_canonical` to unsupported precision. Flagged in the arc's README; **creating the ledger is the natural next task**, and it is a prerequisite for auditing the arc at all. **Two of six remain in the fork:** `SC_4_3` (MOND — carries **14** of the 32 `2π` occurrences) and `SC_4_6` (the synthesis, which depends on all five others).
53. **`SC_4_2` migrated 2026-09-04, and the migration caught the census inflating itself.** **`Paper_ED_SC_4_2` migrated to `physics-papers/cosmology/`, with all three of ≦52's conditions applied.** A read-this-first banner marks it as a **BANKED NEGATIVE** — the derivation of `ξ_canonical(H₀)` does **not** close, and the §3 machinery must not be read as a derivation. Inline flags on the two load-bearing inherited rows: **I-029** (the `2π` and its *“azimuthal-Fourier signature”* are disputed — the factor cancels in `Paper_028` §6.3 / `Paper_029` §5.1's own algebra; the scale `a₀ ~ cH₀` is unaffected) and **I-096** (`ξ = 1.7575` → measured `1.76 ± 0.30`, `σ = 0.303`, processing-dependent). **A fourth item recorded rather than silently fixed:** its §6 is titled *“Rewrite Note”* where the checklist's failure mode 5 wants *“Position Statement”* — left faithful to the migrated text and flagged. **The precision correction sharpens this paper rather than weakening it**, which is worth saying because it reads the other way at first glance: a target value carrying **17% scatter** is a *harder* thing to derive than a five-figure constant, so `ξ`'s measured spread makes the non-closure less surprising, not more damning. **And the migration exposed a flaw in the census script — in the direction that inflates ED's assumption count.** The run went 173 → 175, and neither new name came from the migrated paper. Both came from **`Gravity_TieredClaims_Ledger.md` itself**: writing `P-BTFR-Slope-4` and `P-Deep-MOND-Limit` into ≦52 while *reporting the SC-4.x census* caused the census to count them. **Writing about a postulate in a ledger was inflating the corpus's own postulate total.** **Fixed, using the rule the corpus had already stated but the tool was not applying.** `POSTULATE_BASIS.md`'s ladder says *“ledgers, notes and READMEs excluded — they catalogue, they do not use”*, and `is_paper()` already implemented it **for the triage only**. The headline count now uses it too: a name occurring solely in a ledger/note/README is **written about, not declared**, and is excluded and reported separately. **Headline and ladder now agree at 171**; they had silently disagreed by 2 since the ladder was written. Baseline re-set 173 → 171. **The four excluded names, and none is lost:** `P-Adiabatic-CG` and `P-MOND-Interp-Covariantization` are abbreviations (already known orphans); `P-BTFR-Slope-4` and `P-Deep-MOND-Limit` are **real postulates in `SC_4_3`/`SC_4_6`**, which have not been migrated — they will count when those papers arrive. **The migration of `SC_4_2` itself added zero postulates**, exactly as ≦52's census prep predicted. **Five of six remain.** `SC_4_1`, `4_3`, `4_4`, `4_5`, `4_6` are still in the frozen fork under the same three conditions. `SC_4_3` is the one to handle carefully: it carries **14** of the 32 `2π` occurrences.
52. **The SC-4.x six decided 2026-09-04 — NOT superseded; they are `Paper_SCBU`'s own downstream development and they discharge its falsifier F4. Migrate, with three conditions.**

**Decision: NOT superseded — the relation runs the other way, and the corpus had it backwards.** Canonical `Paper_SCBU` closes with *“The corpus is ready for ED-SC 4.x development per the roadmap in companion document”*, and its falsifier **F4** — *does a third or fourth canon-internal anchor (BH horizon `r_H`, Q-Compute `ℳ_crit`, NS-Q operating point) fail to anchor to `R_H = c/H₀`?* — describes itself as ***“the bridge to ED-SC 4.x”***. **SCBU is upstream. The six are the development it calls for.** **And they answer F4.** `Paper_ED_SC_4_6` reports all **six SCBU projections** — `r_H` and `R_H` (4.1), `ξ_canonical` (4.2), `a₀` (4.3), `ℳ_crit` (4.4), `Q ≈ 3.5` (4.5) — occupying *“distinct, FORM-FORCED positions within a unified four-regime structure”*, i.e. the anchors **landing** rather than failing. **So F4's discharge exists while F4 stands open in the canonical corpus, and `Paper_SCBU` was not citing it.** Pointer added there. **Held to the day's standard: NOT asserted as discharged.** The SC-4.x claims are theirs, at M3, and have had **no claim-strength audit** against this corpus's standard. `Paper_ED_SC_4_2` explicitly **does not close** its own derivation (≦51). **What is established is narrower and still worth having: the work exists, it answers a live falsifier, and it was unreachable from the paper that posed it.** **Census prep run, and it came back reassuring.** The six use 3–9 postulates each and add only **two** the corpus does not already count — `P-BTFR-Slope-4` and `P-Deep-MOND-Limit`, both in the MOND papers (`SC_4_3`, `SC_4_6`). **Not a hidden assumption reservoir**, and migrating them would move the declared count from 173 to 175. **Three conditions on migration, because they predate today.** (1) **The `2π`** — all six carry it, 32 occurrences, 14 in `SC_4_3` alone, none flagged. (2) **`ξ = 1.7575`** — in five of six; measured value is `1.76 ± 0.30`, processing-dependent. (3) **`SC_4_2` must migrate as a banked negative**, not a derivation. Recorded in the fork's `SUPERSEDED.md` so the conditions travel with the papers. **What this closes.** The fork check (≦51) asked whether the six were superseded or live. **They are live, upstream-sanctioned, and answer an open falsifier** — which makes them the highest-value thing sitting outside the canonical corpus. The decision the marker deferred is now made; **the migration itself is not done** and is the next concrete task on this thread.
51. **The duplicate checked 2026-09-04 — stale fork frozen 2026-07-05, not a mirror; 31 files contradict today's corrections; six SC-4.x papers exist only there, one of them a banked negative `Paper_096` was not citing.** **Verdict: a stale fork, frozen, not a mirror — so today's corrections do not need mirroring.** The duplicate `Paper_097` was last touched **2026-05-15**; the whole `event-density/papers/Generative Papers/` folder last **2026-07-05**. The canonical copies have been edited through today. **Nothing is tracking anything.** **But it is an active hazard, and the numbers say how much.** Of its **133** files: **71 identical** (harmless), **31 stale-divergent**, **29 with no canonical counterpart**. The 31 contradict today's work directly — the fork's `Paper_029_a0.md` carries **zero** DISPUTED flags on the `2π`, and its `Paper_030` still displays the **three-term profile** `a_N + a₀ + √(a_N a₀)` that was removed at source this morning (≦37). A reader landing there gets the May–July corpus with no marker saying so. **The 29 orphans sort cleanly, and only one group matters.** ~11 are process/admin docs (`UPDATE_PLAN_after_M1–4`, `CONSOLIDATION_SUMMARY`, `DEPENDENCY_GRAPH_ED`, …). ~7 are the **deliberately archived M-series** (`paper_M0–M4`, the omnibus, and `ARCHIVED_M_SERIES_NOTICE` itself) — correctly absent, since canonical `Paper_087` supersedes them. Three are theorem entries `T19–T21`. One is a rename artifact (`Paper_093_KernelArrow` → `_of_Time`). One is canonical but lives in `position-paper/` rather than `physics-papers/`, so the search missed it. **That leaves six: the `Paper_ED_SC_4_1` through `4_6` series, which exists only in the fork.** **And one of the six is directly relevant to work done today.** `Paper_ED_SC_4_2_xi_canonical_H0_derivation.md` attempts the **substrate derivation of `ξ_canonical(H₀)`** and reports that it **does not close**: *“value-INHERITED status preserved; structural setup is FORM-FORCED M3”*, with the missing piece named as either a substrate-derived `ℓ_V5(H₀)` relation or a substrate-derived scaling law. It calls itself *“the highest-leverage ED-SC 4.x paper”*. **This corroborates ≦47 rather than conflicting with it** — ξ is Measured, and deriving it is a banked negative. **But `Paper_096` did not cite it**, so anyone attacking “derive `ξ_canonical`” from the canonical corpus would have repeated a failed attempt. **Pointer added to `Paper_096`.** That is the exact reinvention hazard `CLAUDE.md` exists to prevent, and it was invisible until the fork was opened. **Not applied, and it is a decision rather than an edit:** the fork carries no supersession marker. Writing one is a claim about the status of 133 files in the *other* repo, including six papers that exist nowhere else, so it is left as a recommendation. **The minimum is a `SUPERSEDED.md` in that folder** naming the freeze date (2026-07-05), pointing at `ED Generative/physics-papers/`, and excepting the SC-4.x six until they are either migrated or retired.
50. **The two-`0.6` question SETTLED 2026-09-04 — different objects; `Paper_097`'s exponent is unsourced; ≦47's repoint corrected.** **Verdict: they are NOT the same object, and `Paper_097`'s exponent has no located source.** A repo-wide search finds *“characteristic exponent ≈ 0.6”* **only in `Paper_097` itself**. Its cited source `project_ed_rg_three_regime.md` contains exactly one exponent — the **dynamic exponent `z = 2`** in the beta functions — and never associates `0.6` with the transition regime. That memo's own §9, *“The 0.6 problem resolved”*, is entirely the **dimensional-dictionary coefficient**: `T_0 = L_0²D_nd/D_phys`, Madelung `D_phys = ħ/2m`, Compton `L_0 = ħ/mc`. And the four RG memos it cites contain **no `0.6` at all**. **So the likely history is a fusion.** One source carries two separate results — a three-regime RG structure *and* a dictionary timescale coefficient — and `Paper_097` combined them into *“the transition regime exhibits a characteristic exponent ≈ 0.6”*, which neither result supports. **An exponent and a coefficient are different kinds of object**, and nothing located argues the identification. **`P-0p6-Canonical` is flagged as unsourced, not withdrawn.** An RG derivation may exist outside the searched set, and the postulate is honestly declared canon-internal either way, so deletion would overreach. **The three-regime RG structure itself is sourced and unaffected** — what is unsourced is specifically the number attached to it. **And this corrects my own edit from ≦47.** That repoint sent `Paper_097`'s citation to `The_0_6_Problem_Resolution.md` **on the assumption the two `0.6`s were one.** They are not. The resolution paper addresses the dictionary coefficient and is **not** a source for `P-0p6-Canonical`. Corrected in place, with the wrong assumption named rather than quietly overwritten. **Scope correction for ≦49, which is the useful part.** The `D_nd = 1/3` derivation bears on the **dictionary** `0.6` — real, well-sourced, and exactly what the resolution paper treats — so **that result stands unchanged**. What changes is what it is *about*: it does **not** bear on `Paper_097`'s claimed RG exponent. Two numbers were being treated as one, and the derivation belongs to the one that exists. **Noted in passing, not actioned:** a duplicate copy of `Paper_097` sits at `event-density/papers/Generative Papers/Paper_097_RG_0p6_Problem.md`. Corrections applied here have not been mirrored there, and whether that copy is a stale fork or an intended mirror is unchecked.
49. **Research 2026-09-04 — the `D_nd(quantum)` derivation run: the template closes on `1/3`, the Atlas convention is `0.3`, and the gap is exactly 9/10.** **The template closes in the quantum regime, and it lands on `1/3` — not the Atlas's `0.3`.** `ED-Dimensional-01-Ext` applies the rate-balance template to cavity-QED, condensed matter, galactic and cosmological regimes but **not** to the quantum regime, which is the one the dictionary is built on. ≦48 named that as the remaining work; this ran it. **The step that makes it tractable, and it does not appear to have been noticed: `γ_dec` is FORCED, not chosen.** The dictionary construction `T_0 = L_0²D_nd/D_phys` and the rate-balance prescription `D_nd = γ/(γ+ω)` are two expressions for the same quantity. Equating them gives `T_0 = 1/(γ+ω)` and identifies **`γ_dec = D_phys/L_0² = mc²/(2ħ) = ω_C/2`** — algebra on the dictionary, no physical judgment, with the factor of ½ inherited straight from `D_phys = ħ/(2m)`. **Template step 3 is discharged without interpretation, so the whole question reduces to one identification: `ω_sys`.** **With the dictionary's own scale, the answer is exact.** `ω_sys = c/L_0 = mc²/ħ = ω_C`, the Compton frequency, gives **`D_nd = (ω_C/2)/(ω_C/2 + ω_C) = 1/3`** exactly — hence `2D_nd = 2/3 ≈ 0.667` and `c_0 = 1.5c`. **Run backwards, the Atlas convention `D_nd = 0.3` requires `ω_sys = (7/6)ω_C`** — seven sixths of the Compton frequency, for which no interpretation is offered anywhere in the Atlas or the extension. The two differ by exactly **9/10**. **Suggestive, and deliberately not banked.** `ω_C` needs no justification beyond `L_0`; `7/6·ω_C` needs one and has none — and `0.3` is what `1/3` looks like at one decimal place. **But this note does not claim the convention is a rounding.** That is a hypothesis about how a number was chosen, and settling it needs the Atlas's own record of the choice, not this calculation. **The honest statement: the derivation closes on `1/3`, and `0.3` is unexplained.** Nothing downstream breaks either way — the resolution paper §3.3 already tiers `c_0 > c` as a regime-boundary marker rather than a physical prediction, so `1.5c` is as harmless as `1.667c`. **And it raises a question about my own edit from earlier today.** `Paper_097` calls its `0.6` a characteristic **exponent**; the dictionary's `0.6` is a dimensionless **coefficient** in `T_0`. **Different kinds of object**, and their identification is assumed rather than argued anywhere located. ≦47 repointed `Paper_097`'s citation to `The_0_6_Problem_Resolution.md` **on the assumption they are the same `0.6`**. If they are not, that repoint is wrong and 097's number has no resolution paper at all. **Flagged on 097 and here; not settled** — checking it means reading the three-regime RG derivation, which was not done. **Tier: M2.** `γ_dec` forced (**D**), `T_0 = 1/(γ+ω)` (**D**), `ω_sys = ω_C` **form-IDENTIFIED** (the template's own §2.3 says steps 1–4 are interpretive), `D_nd = 1/3` **D-via-I** conditional on it. **`0.6` is not derived by this note; `2/3` is.** Owed: justify `ω_sys = ω_C` from the substrate rather than from the dictionary's length scale; recover the Atlas's record of how `0.3` was chosen; settle the two-`0.6` question. `Note_Dnd_Quantum_RateBalance_Attempt.md` (new).
48. **The 0.6 question settled 2026-09-04 — reduced, not derived; tier unchanged; one unregistered consequence flagged.** **Verdict: `0.6` is REDUCED, not derived. `Paper_097`'s tier was already correct and does not move.** The resolution paper's own three-level table settles it: `T_0 = 2D_ndħ/(mc²)` **structurally forced** by the dimensional-dictionary construction (*no freedom*); `c_0/c = 1/(2D_nd)` **structurally forced** as a corollary (*no freedom*); **`D_nd(quantum) = 0.3` a convention, not a derivation.** Its verdict line is *“partial derivation + structural reduction”* and §8 is blunt: *“The 0.6 is reduced but not eliminated.”* **The gain is real even though the tier does not move, and it is worth stating precisely: the inheritance MOVES.** What ED inherits is no longer the number `0.6` but the convention `D_nd(quantum) = 0.3`, joined by an exact algebraic bridge. The paper puts it best — ***“The ‘0.6 problem’ does not have its own standalone resolution. It collapses cleanly into the already-named `D_nd` anchoring problem”***, which `ED-Dimensional-01-Ext.md`'s rate-balance-template program is actively working. **One unexplained number becomes one unexplained number that is already a named, worked problem.** That is a reduction in the count of *distinct* open questions, not in the count of free parameters. **Three consequences the corpus did not carry, all from §6 of the resolution.** (a) The superluminal `c_0 ≈ 1.667c` is an **atlas-nondimensionalisation artifact**, not an ED prediction — it exists purely because the Atlas picks `D_nd = 0.3`. That is worth having explicitly, since a stray *“ED predicts 1.667c”* would be a credibility problem. (b) **`c_0 = c` exactly at `D_nd = 1/2`**, the rate-balance point `γ_dec = ω_sys` — a clean selection rule, no fitted parameter. (c) A **cross-regime consistency relation** for analogue platforms, `c_s/c = 1/(2 D_nd(regime))`. **(c) is not registered in any ED prediction list, and I did not register it.** The resolution paper itself cautions the relation is *“algebraic, not predictive — it does not tell you `D_nd`; it tells you `c_0` once `D_nd` is specified”*. So it is a test **only where `D_nd` is anchored independently per regime**, which is precisely what the rate-balance template supplies for optomechanics / cavity-QED / condensed-matter / galactic / cosmological. **Flagged as a candidate with that condition attached** rather than banked as a new prediction — registering an algebraic identity as a falsifiable bet is the shape of error the standing caution on over-banking names. **Thread closed.** Foundations staleness #4 is now fully resolved in both halves: ξ traced to a measurement artefact with its precision corrected (≦47), and 0.6 settled as reduced-not-derived with its citation repointed to the resolution paper. **Next on this thread, if reopened:** derive `D_nd(quantum)` by applying the rate-balance template inside the quantum regime — identify `γ_dec` and `ω_sys` from Madelung/Schrödinger structure and compute `D_nd = γ_dec/(γ_dec + ω_sys)`. The resolution paper §8 calls that and its two companions *“concrete, tractable theory-only targets”*.
47. **ξ_canonical traced to ground 2026-09-04 — it is Measured, not memory-floating; the precision is overstated by four significant figures; 0.6 already had a paper. Foundations staleness #4 resolved.** **Chased because ≦46 flagged it as the weaker half of Core Theory row 24. It is not what Foundations staleness #4 said it was, in either direction.** #4 called both numbers *“canon-internal benchmarks … should re-anchor to a paper”*. **ξ is not a benchmark and should not anchor to a paper; 0.6 already has one.** **ξ_canonical is MEASURED, with a live artefact.** `ed-lab/outputs/ed_sc_3_1/xi_canonical.json`, produced by the simulator of record `r2_grf_falsifier_tests.py` + `ED_Update_Rule.ed_step_mobility`; method *GR-SC 1.7 half-decay of the 2D radial autocorrelation*; **10 seeds × 40 snapshots (400 total)**, 64² periodic, α=0.03 γ=0.5 σ_noise=0.0556, mobility-exp 2.7, 500 steps. Audit row 7 in `Paper_096` re-tiered from **I** to **Measured**, which is the Tier Key's own category for a simulation result on the certified substrate. **The provenance was never the problem.** **The problem is the precision, and it is a four-significant-figure overstatement.** The artefact reports `xi_mean = 1.7575325729470939` **with `xi_std = 0.3034757800063313`** — a **17% spread**, per-seed range 1.623–2.191. **The honest value is `ξ ≈ 1.8 ± 0.3`.** Quoting *1.7575* gives five significant figures to a number uncertain in its first decimal place. **And it is processing-dependent:** the same artefact's smoothed autocorrelation gives `3.05 ± 0.53` — **a factor of 1.74 away**, from the same data with a different autocorrelation treatment. No paper mentions that the choice moves it that far. **Propagated to seven documents, because that is this session's recurring failure.** Flags now in `Paper_096` (§7 full note + §§4/5 rows), `Paper_SCBU` (abstract **and** the displayed equation), `Paper_ED_Postdictions_PassedTests` — **which listed the five-figure value in a *passed-tests* document** — `Paper_060`, `Paper_086`, and the Foundations ledger's tier row. Every document quoting the number now meets the correction. **0.6 was mis-diagnosed in the opposite direction: it already had a paper.** `event-density/theory/The_0_6_Problem_Resolution.md` argues `0.6 = 2·D_nd(quantum)` with `D_nd = 0.3` the cross-regime nondimensional invariant, via a dimensional-dictionary construction, with a five-route audit. `Paper_097` was citing the **memory file** instead. Citation repointed. **Tier deliberately NOT upgraded:** whether that resolution promotes 0.6 from inherited to derived requires reading the paper, which was not done here, and 097's item 1 (*“does not claim derivation of the exact numerical value”*) stands. **Named as the next step on this thread.** **Where the sources actually live, which is the structural finding.** Both papers cited memory files in `.claude/projects/C--Users-allen-GitHub-**Event-Density**/memory/` — *another project's* memory directory, outside both git repos, invisible from this project, and denied authority by `CLAUDE.md`'s own read-first rule. Those memory files in turn point at **`GitHub/ed-lab/`**, a **third** location no paper or ledger names. So the provenance chain ran **paper → another project's memory → an unnamed third repo**. It is intact and the work is real; it was simply unreachable from the corpus. **Now cited directly.** **And the memory file is wrong where the artefact is right** — a small, exact illustration of the rule. `project_ed_sc_3_arc.md` states a per-seed span of **[1.039, 2.19]**; the artefact's own per-seed values run **[1.623, 2.191]**. The memory's lower bound is wrong by 36%. Recorded in `Paper_096` §7. **Effect on Core Theory row 24 (≦46).** The residual there read *“ξ is canon-INTERNAL and memory-sourced, so half this claim rests on the pointer layer”*. **That is now half-wrong and worth restating:** ξ rests on a measurement, not the pointer layer — but it is a measurement with `σ/ξ ≈ 17%` and a factor-1.74 processing dependence, so *“FORCED to share one origin”* is joining a disputed coefficient to a 17%-uncertain measured scale. **The pairing is looser than either half suggests, for two different reasons.**
46. **The “38 lines” checked 2026-09-04 — structure unchanged, one tier moved, basis now stated, and row 24 verified against the `a₀` work.** **The “38 lines” is 13 primitives + 10 constants + 15 postulates, and the postulate third carries a basis that was never stated.** Only **one** of the 38 changed today: row 16, `a₀ = cH₀/(2π)`, whose tier now reads *Constant— SCALE ok, COEFFICIENT DISPUTED*. **Structurally the claim is unchanged**: nothing added, nothing removed, still 38. **But the 15 postulates on rows 24–38 are the narrow basis, and the frame is exposed the same way `Paper_088`'s “31 total” was (≦33).** Against ≦45's ladder: **15** here, **26** cross-cutting (≥4 papers), **171** declared anywhere. Read as *“everything ED assumes”* the figure understates the declared load by an order of magnitude; read as *“the structural spine”* it is accurate and a good frame. **The difference is one sentence**, now added to the sheet as rows 40–43. **And the selection is by headline, not by load — which is worth knowing and was not written down.** The 15 overlap only partly with the 26 cross-cutting: shared are `P-RB-1`, `P-Quadratic-Strain`, `P-Profile-Rescaling`, `P-QD-LiveWeight`. **Heavily used but absent from the 38:** `P-Codim-1` (10 papers), `P-Corr-Budget` (10), `P-LinRate` (9), `P-Channel-Orthogonality` (7), `P-Sat` (7). So these are *the postulates carrying headline claims*, not *the most load-bearing postulates*. Both are legitimate selections; only one is what the row set actually is. **Row 24 checked against the day's `a₀` work — it survives, and is if anything strengthened.** The line reads *“`a₀` & `ξ_canonical` **FORCED** to share one substrate-cosmology boundary origin”*, and *forced* next to a newly-disputed coefficient is exactly the item-20(c) profile. **The dispute does not reach it:** what the two scales share is the boundary object `R_H = c/H₀`, **not** the coefficient — the `1/(2π)` sits on `a₀`, the shared origin sits on `R_H`. Meanwhile `a₀ ~ cH₀` gained a **fourth** independent route the same day (≦34), so the horizon-tying half is better supported than when the row was written. **Tier `Postulated` is correct and unchanged**, resting on the named `P-Substrate-Cosmology-Unified`; SCBU's own verdict is M3 form-FORCED + value-INHERITED, and SCBU already carries this morning's DISPUTED flag on its `1/(2π)` line (propagation from ≦27 confirmed landed). **Two residuals recorded on the row, neither a tier change.** (i) The *other* half of the pair, `ξ_canonical = 1.7575 lu`, is **canon-INTERNAL and sourced to memory files rather than a paper** (Foundations ledger staleness #4) — the corpus's own read-first rule denies memory authority, so **half of this claim rests on the pointer layer.** (ii) SCBU step 12 leaves the functional form of `ξ_canonical(H₀)` **OPEN**, so what is claimed is co-variation under `H₀`, not a derived relation. **“FORCED” is defensible given the postulate and should not be read as unconditional** — now stated on the row itself and mirrored into Core Claims.
45. **Target #20 CLOSED 2026-09-04 — the postulate basis is a ladder, not a number. `internal notes/POSTULATE_BASIS.md` (new); `--triage` added to the census script.** **The answer is that there is no single number, and the ladder is the deliverable.** Basis chosen and stated: **distinct papers naming the postulate**, with ledgers, notes and READMEs excluded because they *catalogue* rather than *use*. A postulate invoked in one paper is a modelling choice local to that derivation, the way a gauge choice is; one invoked across many is a standing commitment. Mechanical, arguable, re-runnable. **The ladder:** 171 declared anywhere (+13 = **184**); 68 used in ≥2 papers (+13 = 81); 43 in ≥3; **26 cross-cutting** at ≥4 (+13 = **39**); 14 carrying a Postulated-tier claim row (+13 = 27, the workbook's basis). Two further names — `P-Adiabatic-CG`, `P-MOND-Interp-Covariantization` — appear **only** in a ledger and are abbreviations, not postulates, which is why the raw 173 becomes 171. **The fact worth carrying: 103 of 171 (60%) are used in exactly one paper.** That reframes the whole parsimony question. It is also unremarkable in itself — every framework accumulates local commitments during individual derivations — and the point to make is that **ED's discipline of naming them at point of introduction is why they are countable at all.** A framework that does not name them carries the same load with no way to audit it. `P-RB-1` is in a class of its own at **30 papers**: the identification of the substrate rate with observed light speed, and the one non-primitive commitment that arguably belongs in the same conversation as the 13. **A near-coincidence checked and rejected, recorded so it is not re-found and believed.** A first pass appeared to show the workbook's *“15 live”* matching a breadth-based count of 15 cross-cutting postulates — an apparently striking convergence of two independent methods. **It was an artifact of bucketing order.** Corrected, the breadth count is **26**, and the overlap with the workbook's live set is **6 of 14**. The two methods measure genuinely different things and do **not** converge. Recorded because a false convergence between the corpus's own figure and an external check is exactly the kind of thing that gets banked. **Applied.** New `internal notes/POSTULATE_BASIS.md` carries the table, the rule, the cross-cutting list, the recommended public wording and a stricter-reading variant (dropping the four `-Naming`/`-Definition` members gives 22 / +13 = 35, offered as a variant rather than a silent choice). `_census_postulates.py` gains **`--triage`**, so the tables regenerate rather than going stale, and the script still exits nonzero on count drift. The workbook's **Ratio** sheet carries the ladder alongside the basis note added earlier today. **Public-material rule, both directions.** Do not quote *“15”* or *“31”* without basis — they are the workbook's claim-row count and a Wave-2 scoped figure (≦33). **And do not quote 171 alone either:** without the 60%-single-use fact it overstates the standing load as badly as 31 understates it. **The ladder is stronger than any single figure**, because an understated count invites a reader to go find the rest, and the ladder hands it to them.
44. **Staleness #13 RESOLVED 2026-09-04 — QuadraticStrain's second discharge stands; the asymmetry it needed is #36's, not a dipole. Applied to `Paper_QuadraticStrain_v1` §6.**

**The dilemma is dissolved, not chosen between, and the tool was already on the table.** ≦13 posed a fork: **(i)** `b^(H)` isotropic — first discharge and BTFR stand, but §6.2's removal of `Σ₀` loses its justification, a constant `a₀` returns, and the **second** discharge fails; **(ii)** `b^(H)` carries the true dipole `∝ |a|cosθ` — sign-indefinite, so not a P04 bandwidth, `√(b^L b^H)` imaginary over half the sphere, and the **first** discharge fails too. **≦36's source-vs-response asymmetry removes branch (i)'s cost**, so the paper can sit on branch (i) with the removal justified: `b^(L)` sums to `-GM/R`, the potential of a **source** whose gradient is a force; `b^(H)` carries `a₀R`, the **normalization of a response** that exists only because the chain accelerates. **Both terms are in the identity; only one has a gradient that means anything.** That is the principled asymmetry ≦13 said the dipole argument failed to supply. **And it is strictly better than the repair ≦13 itself floated.** ≦13's candidate was to carry the directional character in the relative phase `Θ_LH` rather than in `b^(H)`. **That route is closed, and the reason is in §5.1:** the cross-term is written `Φ_cross = 2cosΘ_LH · Σ_K √(b_K^L b_K^H)`, with `cosΘ_LH` **factored out of the channel sum** — which presumes it is channel-independent. A `Θ_LH` carrying `cosθ` cannot be pulled out, and inside an isotropic `√(b^L b^H)` sum it would partially cancel. **A cost ≦13 did not name.** Under §6.2a nothing has to carry a dipole downstream at all: the `cosθ` structure is how `Paper_029` **derives the scale**, and what propagates here is the projected normalization `a₀`, isotropic by construction. **Applied.** `Paper_QuadraticStrain_v1` §6.2 keeps its original argument for the record under an audit banner stating why it does not carry the removal; new **§6.2a** supplies the source/response asymmetry and spells out what it buys (isotropic non-negative `b^(H)`, real geometric mean, `Θ_LH` channel-independent so §5.1's factorization holds as written, angular structure located upstream in `Paper_029`). **§6.1 stands unchanged** — it is correct about what `Paper_029` derives; what it did not license was reading that as making `b^(H)` itself anisotropic. **Both discharges now stand. No new postulate; census unmoved.** **Correction to ≦37, recorded because it trims a claim I made this morning.** ≦37 called the removal of `Paper_030` §5.3's regime assumption an *“unlooked-for gain”*. **`Paper_QuadraticStrain_v1` §6.3 already had it**, dated 2026-07-07/08: *“`a = a_N + √(a_N a₀)` … No regime assumption is needed … This is the second discharge.”* So ≦37 did not discover the two-term law — it brought `Paper_030` into line with a result QuadraticStrain had held for two months, and the two papers had been displaying **different profiles** that whole time. What the later work genuinely added is the **justification** (§6.2a) and the removal of the inter-paper inconsistency. A convergence note is now in §6.3 so the ordering is not misread. **Residual, stated so it is not banked as closed.** §6.2a is an *argument*, not a derivation: it rests on `Paper_029` §5.1's separability of `ρ₀` from the response amplitude and on `Paper_029` §4.4's own characterization of `a₀` as a threshold. Both are in the corpus and quoted, but neither is a theorem. **Tier: M2 — an internal-consistency resolution by re-reading.** The `2π` is untouched, and §9's `cosΘ_LH ≈ 1` residual is unchanged.
43. **Status pass, second round 2026-09-04 — target #15's headline contradicts its own body; the lemma is a banked negative. Headline corrected; drift tested and found NOT systematic.** **Recommended a target off its headline; the headline was wrong, and its own body said so.** #15 read *“DESIGNED 2026-07-24, NOT RUN”*. Four paragraphs into the same entry: *“**ATTEMPTED** 2026-07-24 — two negatives banked, no promotion”*. The general cap-cap-ratchet lemma **does not exist as a non-trivial single statement** (once the caps are granted the result is near-definitional; the first-draft “two proof-modes / P11 opposite-facing” framing was **oversold and withdrawn**), and the `ρ_max` derivation **FAILED on a conflation** — GR-III's `b→0` horizon is a nonlocal Poisson-potential horizon in *vacuum*, not a local density ceiling, and the `b→0` route is closed as a dead end. **So the thing I proposed working on is a banked negative.** **Then tested whether the drift is systematic — it is not, and that is worth recording as a checked negative.** Swept every numbered target mechanically, comparing headline status words (NOT RUN / DESIGNED / OPEN / SCOPED / PARKED) against body status words (ATTEMPTED / RUN / FAILED / negatives banked / dead end / CONFOUNDED / WITHDRAWN / RESOLVED / CLOSED). **One instance: #15.** Every other target's headline agrees with its body. **So the targets doc is disciplined about headlines and #15 is an outlier, not a pattern** — recorded that way rather than banking a trend from two anecdotes. **Corrected, and the residual stated in the headline where a scanner will see it.** #15's live content is not the lemma but the one hard residual the body already names: derive Arc_BH_3's **local** mobility-capacity law `Γ₀(ρ) → 0 as ρ → ρ_max` from the primitives. The doc's own verdict on it is *“a real open problem, not a quick win”*, and the certified rule has no `ρ_max` at all (`ρ → 3.0` unbounded, `evaluation/CoarseGrain_Arc/Diffusion_Arc_Finding.md`). **The singularity family's density cap stays declared/inherited.** `Paper_HowPDECoarseGrainReality` §4 is unaffected — it already tiers the obstruction at synthesis and flags the caps as declared. **Third recommendation in a row overturned by reading before starting, and the sequence is the lesson.** ≦41: a target recommended a **confounded** test. ≦42: a target cited a **renamed** file, and a ledger row read OPEN against its own discharged paper. ≦43: a target headline contradicted its own body. **None was found by auditing — all three surfaced at the moment of use**, and in each case the correct information already existed somewhere in the corpus. **The corpus's content is in better shape than its navigation**, and the read-first rule is doing more work than its authors probably intended: it is not only preventing reinvention, it is the only thing catching pointer rot. **Not started.** #15's residual is the hard problem by the doc's own assessment, the `b→0` route is closed, and starting a substrate derivation on the back of three overturned recommendations in one session is how over-banking happens. Flagged for a session with fresh context.
42. **Status-verification pass 2026-09-04 — candidates checked against their sources, not their descriptions. Two stale pointers found and fixed.** **Prompted by ≦41: after a target doc sent the session at a confounded test, the remaining candidates were verified against their sources rather than their target-doc descriptions.** Five checked. **Two more stale pointers, one of them the costly kind.** **(1) Target #15 cites a file that has not existed under that name since a rename.** `Paper_TwoHarderShadows.md` → `Paper_HowPDECoarseGrainReality.md`. §4 is intact and accurate. **What makes this the expensive kind of dead path:** it is attached to the instruction *“do NOT spin out a thin standalone note; that would duplicate §4”*. A reader who cannot find §4 concludes it is gone and writes exactly the note the target warns against. **Note also:** the 2026-09-03 pointer sweep did rename-detection and called it load-bearing, but it swept `PAPERS_INDEX.md` in this repo — **not `ED_Research_Targets.md` in the sibling repo.** The sweep's own method was right and its scope was short. **(2) Staleness #16 read OPEN while its paper records the fix as discharged.** `Paper_030` §4.2 now displays the cancellation and names **P-Bilocal-Count** with falsifier F-BC and a statement of what breaks without it; the paper says *“A flag recorded here on 2026-09-04 has been discharged by the rewrite below”*. **The ledger row never moved.** This is the reverse of the day's usual direction — paper fixed, ledger stale — and it is the more costly one, because the ledger is what a session reads to choose work: OPEN invites redoing finished work. Now marked DISCHARGED with the original finding retained. **(3) Staleness #15 carries a closed form computed from a profile that no longer exists.** It derives `μ = s²/(s²+s+1)` from `a = a_N + a₀ + √(a_N a₀)`, whose standalone `a₀` ≦37 removed at source. The corrected profile gives **`μ = s/(s+1)`** — materially different (at `a_N = a₀`: 0.500 vs 0.333), verified numerically over `10⁴…10⁻⁴`. **Both of the item's conclusions survive**: saturation is still `~x^{-1/2}` so Cassini still excludes the profile outside the deep field. Updated in place rather than withdrawn, with a pointer to ≦34's re-diagnosis (`μ` is the inherited V5 envelope shape, so *“cannot be derived”* is right for the wrong reason). **Verified sound, no action:** target #17's parked negative is filed and present (`event-density/theory/Note_SpectralDimension_ArenaScoping.md`); target #21's basis (`Paper_090` §7.2, envelope shape = value-layer) was confirmed directly earlier today; target #15's §4 content matches its description; staleness #23 and #24 are genuinely open as stated. **The pattern, now with five instances and two detection modes.** Four were found by reading lists against each other; ≦41 and the two here were found by **trying to use the claim** — selecting work, then verifying before starting. **Use is a better detector than audit for pointer defects**, because a stale pointer is internally consistent and only fails against the thing it points at. *Practical consequence, and it is cheap:* before committing a session to a target, open what it cites. Three of the last four targets examined that way were stale.
41. **Research 2026-09-04 — target #16's ‘near-term alternative’ was confounded on the day it was written; withdrawn. Found by acting on it.** **A target doc pointed at a dead test for six weeks, and it was caught only by acting on it.** Target #16 (*“ED's single most load-bearing current gap”*) closes by naming a **map-independent near-term alternative**: the second-harmonic fingerprint, *“plausibly checkable in existing Fein-2019 / Jan-2026-nanoparticle data”*. **`ED_Master_Predictions_List` 4.2 marks that signature CONFOUNDED, dated 2026-07-24 — the same day target #16 was written.** KDTL grating optics produce higher harmonics anyway, so a bare second harmonic confirms nothing; a real test needs *excess* over the full Nimmrichter–Hornberger optics prediction from **raw** fringe data, which is not published. **How it was found, which is the useful part.** It was not found by auditing. It was found because the clause was **acted on** — selected as the session's next research target on the strength of the target doc, then read up in the predictions list before starting. **Two documents, same date, opposite verdicts, and the reader who reconciles them is the one who tries to use the claim.** That is a fifth propagation failure and a different detection mode from the four before it: those were found by reading the lists against each other; this one only surfaced under use. **Withdrawn, and the honest near-term status stated in its place.** The matter-wave weapon currently has **no near-term test**: the *number* is pressured (140–250 kDa vs the ≥170 kDa Nature-2026 report, a 2-point extrapolation flagged *do not bank as firm*); the *second harmonic* is confounded; and *architecture-independence* — which ≦25 called the cleaner discriminator — needs two platforms at comparable mass that **do not exist**. The map (#16 proper) remains the gate. Leaving a hopeful clause in place made the arc look more testable than it is. **Checklist consequence.** Item 21 says a ledger entry is not propagation, and its mirror says a ledger's own prose summary is a propagation target. This adds a third face: **a *research target* is a propagation target too.** When a prediction's status changes, the targets that recommend it must be updated in the same pass — targets are where a reader goes to decide what to *do*, which makes a stale one more expensive than a stale claim row. **No physics changed.** ML 4.2 was correct and dated; `Paper_056` and the q-compute ledger were both honest. **Only the pointer was wrong**, which is the day's recurring shape.
40. **Research 2026-09-04 — Prediction 1.16 registered: `H₀ = 2πa₀/c`, the Hubble constant from rotation curves. `Paper_ED_Pred_H0_From_a0.md` (new).** **The inversion nobody had written down.** `a₀ = cH₀/(2π)` read backwards is **`H₀ = 2πa₀/c` — a determination of the Hubble constant from galaxy rotation curves.** **This probe has no MOND analogue:** there `a₀` is a constant of nature with no cosmological connection, so there is nothing to invert. It exists only if the scale is horizon-set, which is this arc's central claim. **It is ED's one genuinely novel *measurement*, as against a novel explanation of a known number.** **Current value and standing.** Desmond 2023 gives **`H₀ = 77.0 ± 6.3 km/s/Mpc`** — **+0.62σ vs SH0ES, +1.50σ vs Planck**. Favours the local ladder; **too blunt to arbitrate** against a 5.6 gap. Arbitration needs `a₀` to **~4%** (±3 km/s/Mpc) or **2.6%** (±2); Desmond is at ~8%, so **a factor of two**, and the systematic there is mass-to-light normalisation rather than anything irreducible. **The virtue that makes it worth registering: independent systematics.** `a₀`'s error budget is dominated by stellar population synthesis and disk/bulge M/L. That shares nothing with the CMB sound horizon or with Cepheid/TRGB ladders. **A third probe in a two-camp tension is worth most when its errors are uncorrelated with both**, and this one's are. **Guarded against the obvious misuse.** One equation, two undetermined quantities (`k` and `H₀`), so it is a **joint constraint** usable in one direction at a time. Assume `k = 2π` → it measures `H₀`, and the result is a *test* of `k`. Assume `H₀` → it measures `k` (currently `5.67 ± 0.47` at `H₀ = 70` against `2π = 6.283`). **Measuring `H₀` from it and citing the agreement as support for the `2π` is circular and the paper says so.** **Free with the flagship's data.** ML 1.15 already predicts `a₀(z) = cH(z)/(2π)` with exponent exactly 1, and `Paper_029` §2.5 row 14 records that the exponent is **coefficient-independent**. So one redshift survey of the RAR delivers both: the **shape** tests the exponent, the **normalisation** gives this. The second half had not been written down. **Registered 2026-09-04, deliberately before the precision to test it exists**, so a later agreement is a prediction rather than a postdiction — the corpus's standing caution on that applies with full force. **Tier: M3, conditional on a coefficient that remains Postulated/disputed.** Nothing here derives the `2π`.
39. **Research 2026-09-04 — the `a₀` currency check run: the systematic tightened 2.7×, the `2π` is at 1.1σ not 0.5σ, and it prefers a high `H₀`. `Paper_029` §5.5 (new).** **The check named in ≦38 was run, and the ground had moved.** §5.4 was written against **McGaugh, Lelli & Schombert 2016**, `a₀ = 1.20 ± 0.02 (stat) ± 0.24 (sys)` — the ±0.24 being a 20% normalisation uncertainty from holding disk and bulge mass-to-light ratios fixed across SPARC. The current determination is **Desmond 2023** (MNRAS 526, 3342), an HMC joint inference that fits galaxy parameters simultaneously: **`a₀ = 1.19 ± 0.04 (stat) ± 0.09 (sys)`**. **The systematic has tightened by 2.7×.** **So §5.4's wording is stale, as ≦38 suspected but could not show.** ED's `2π` requires `a₀ = 1.082` at `H₀ = 70`. Against ±0.241 that is **0.49σ** — *“consistent”* was fair. Against ±0.098 it is **1.09σ**, and the offset is one-sided at every `H₀` in the range. The honest reading is now **not excluded at ~1.1σ**, not *consistent*. §5.4's note is replaced by a new **§5.5** carrying both determinations, the σ-table, and the falsifier below. **The substantive result: `a₀ = cH₀/(2π)` takes a side in the Hubble tension.** Required `a₀` runs 1.042 (Planck `H₀ = 67.4`) to 1.144 (`H₀ = 74`), so against Desmond the offset is **1.50σ at Planck and 0.62σ at SH0ES**. **The relation is markedly more comfortable with the local distance-ladder `H₀` than with the CMB value.** *This is not a prediction of `H₀`* — `Paper_029` §8.1 disclaims deriving it, and ED inherits both numbers — **it is a consistency constraint between two independently measured quantities**, and it is the sharpest empirical handle the `2π` has ever had. **The `a₀` side is robust against the obvious objection.** A fitted `a₀` might be thought to depend on the assumed `μ` family, which would blunt the test. It does not: `a₀` is *defined* by the deep-MOND normalization and the asymptotes cross at `a₀` for every family (threshold note §5, checked numerically across three). Desmond marginalises over functional forms and lands within 0.01 of the 2016 fixed-form value — which is exactly what that argument predicts, and is a small independent confirmation of it. **New falsifier F-2π, and it sharpens without anyone doing theory.** If `a₀` tightens further while the CMB `H₀` holds, the `2π` is squeezed structurally-untouched: at `H₀ = 67.4` a combined `±0.05` puts the offset at **3σ**. Conversely a local-ladder `H₀` with `a₀` settling near 1.13 confirms it below **1σ**. **The coefficient question may be settled by measurement before it is settled by derivation.** Given that four structural routes reach `cH₀` and none reaches the coefficient, that is worth naming as the live possibility rather than treating route 5 as the only way out. **Tier unchanged: the `2π` remains Postulated/disputed (≦10).** Nothing here derives it. What changed is that its empirical standing is now stated precisely, is worse than the corpus believed, and is **directional** — it points at a high `H₀`.
38. **Research 2026-09-04 — Target #18: the content-depth framing; the geometric candidate `k = 2` eliminated. `Note_a0_TwoPi_RepairRoutes.md` §4e.** **What the `Σ₀` rewrite (#37) bought here.** Until today `Σ₀ = -a₀R` was a potential whose gradient was a force and `a₀R_H` had no distinguished status. `Σ₀` is now a **content normalization**, so `a₀R_H = c²/(2π)` is a **displayed physical quantity — the cosmic horizon's content depth**. Writing the depth as `c²/k` gives `a₀ = cH₀/k` directly, which turns *“where does the `2π` come from?”* into *“is a horizon's content depth `c²`, `c²/2`, or `c²/(2π)`?”* — a question about a number the corpus can approach independently. **The new candidate, and it is eliminated. `k = 2`, the geometric depth.** Not previously enumerated in this note or `Paper_029`, and it is the obvious competitor because it is what the corpus's **own** machinery gives for a horizon: `Σ_N(R_s) = -GM/R_s = -c²/2` exactly at a Schwarzschild radius, and de Sitter's `1 - H²r²` gives the same depth at `R_H`. It predicts **`a₀ = 3.40×10⁻¹⁰`, 2.8× the measured value** — far outside any error budget on `a₀` or `H₀`. **Excluded.** *(Caveat kept in the note: the analogy is natural, not forced — `Σ_N` is content sourced by mass inside a radius, `Σ₀` is content presented by a boundary outside the chain. What is eliminated is the natural reading, not a theorem.)* **Why the elimination is worth having: it was the only natural non-thermal candidate.** `k = 1` (bare `c²`) is 5.7× too large, `k = 2` is 2.8× too large, `k = 2π` is 9.8% too small. **Among natural structural candidates only the thermal reading survives** — which is the reading Route 2 (§3) needs. This is an elimination, **not** a derivation: removing a competitor does not establish `2π`, and the 9.8% residual is untouched. **And it converges on the same locus a third time.** Asking whether the depth is geometric or thermal *is* asking whether the horizon presents as a `κ` or as a `T` — exactly the asymmetry §4 says `κ_a = T_H` needs and §4b tried to ground in the layer seam. Reached from an unrelated direction (a content normalization in `Paper_030` §3.2), it lands on the same question. §4d already recorded that every route reduces to comparing a chain's acceleration scale against `cH₀`; the *coefficient* question is likewise single-valued. **Consistency, not independent support — one fact seen a third time is still one fact.** **Checked before writing, and NOT new: the `2π`-required `a₀` range.** `Paper_029` §5.4 already states it — `H₀ ∈ [67,74]` gives `≈[1.03,1.14]×10⁻¹⁰` against an empirical `1.2×10⁻¹⁰`. An independent recomputation reproduced it (`[1.042,1.144]`). **Not claimed as a new prediction.** *One wording item recorded there:* §5.4 calls the ~10% gap *“consistent with the joint uncertainty”*, but the offset is **one-sided** — below the canonical `a₀` at every `H₀`, needing `a₀ ≈ 1.08×10⁻¹⁰` or `H₀ ≈ 77.6` to close. **Not asserted to be a real tension:** that needs the current best `a₀` with separated random/systematic errors, which is **the named next check** and is deliberately not asserted from memory. **No tier change.** The `2π` remains **Postulated/disputed** (≦10). One competitor removed, the question relocated onto a displayed quantity, one currency check named. **Modest, and recorded as modest.**
37. **Applied 2026-09-04 — the `Σ₀` rewrite. `Paper_030` §§3.2/3.4/5.3/6.3/7.1 + abstract. Target #22 CLOSED.** **Applied on instruction, and the paper is now self-consistent.** ≦36 proposed the fix and deliberately left it unapplied. Applied 2026-09-04 across **§3.2, §3.4, §5.3, §6.3, §7.1 and the abstract**. §3.2 now presents `Σ₀ = -a₀R` as a **content normalization** feeding §4.2's `b_K^horizon = a₀R/N_horizon` — legitimate, and equal to `c²/(2π)` at the horizon — and **takes no gradient of it**. The standalone `a₀` is gone at source rather than subtracted downstream. **The paper had already refuted the term, three sections before using it — which is why the fix is safe.** `Paper_030` §3.4 (*“Naïve additive combination — why it fails”*) rejects `a = a_N + a₀` because in deep-MOND `a → a₀`, giving `v² = a₀R`, ***“empirically refuted by BTFR data”***. That is verbatim the argument against the term §3.2 then produced. **And `Paper_031` independently rejects the same additive form** in its alternatives list (*“no `M_b` dependence; slope undefined”*). So the removal is not a new judgment call — **two sections of the corpus already required it**, and the rewrite makes `Paper_030` agree with its own §3.4 and with `Paper_031`. Both places now carry a pointer. **The strengthening nobody was looking for: §5.3's regime assumption is gone.** §5.3 previously wrote three gradient terms and then invoked an explicit, labelled *regime assumption* — that the bilocal cross-term dominates the two pure-source terms — to reach the ECR. That assumption was false as stated (≦35) and was load-bearing (Staleness #13, #15). With `Σ₀` corrected there are **two** terms, and `a → √(a_N a₀)` follows **as a limit**. Verified numerically over `a_N/a₀ = 10⁶ … 10⁻⁶`: `a/a_N → 1.001` at the top, `a/√(a_N a₀) → 1.001` at the bottom, symmetric. **A postulate-free assumption removal**, and the census confirms the postulate count did not move. **What did not change, stated so it is not read as more than it is.** The profile still saturates as `1-μ ~ x^{-1/2}` and is still **excluded at solar-system accelerations** by ephemerides — the removed term was never what caused that, so §7.2's scope limit, `Paper_KM-I` §6.3 as the standing crossover treatment, ≦35's screening requirement and the `n > 2.79` envelope bound all stand unchanged. **The `2π` is untouched.** And the interpolation *shape* remains inherited (≦34): the rewrite fixes which terms exist, not where their shape comes from. The abstract now carries the scope caveat, which it previously did not. **Propagated.** `Paper_031` and both gravity research notes carry dated supersession notes; §7.1's contradiction flag from ≦35 is marked RESOLVED and kept as the audit trail rather than deleted. A corpus-wide grep for the three-term profile returns only historical/audit-trail occurrences. **Target #22 is closed.**
36. **Research 2026-09-04 — Target #22 resolved: `a₀` is a normalization and `Paper_030` §3.2 uses it as a force. Resolution PROPOSED, not applied.** Written up as §11 of `Note_TheSwitchIsDerived_TheShapeIsInherited.md`. **The resolution is a re-reading of `Paper_029`, and it turns on two objects in one line.** §5.1 gives `ρ_cosmic(θ,φ) = ρ₀(|a|/c)cosθ + O((|a|/c)²)` with **`ρ₀ = cH₀`**. Separable: `ρ₀` is the **normalization** and carries no `|a|`-dependence; `(|a|/c)cosθ` is the **response amplitude**, first order and zero at `a = 0`. `a₀ = cH₀/(2π)` is the *projected normalization* — **not the response**. `Paper_029` §4.4 says as much in its own words: `a₀` is *“the **transition acceleration** — the **threshold** between Newtonian and deep-MOND dynamics”*, whose role is set by *“the **ratio** of `a_N` to `a₀`”*. **A ratio is a comparison and a threshold is a scale. Neither is a force.** **The error, precisely.** `Paper_030` §3.2 writes `Σ₀ = -a₀R` and takes `-dΣ₀/dR = a₀`, giving a **uniform background acceleration acting on every chain**. That is the normalization promoted to a field, which §5.1 does not license. **This sharpens §3.2's existing flag rather than replacing it** — the flag's *“converts a response into a source”* is right; §11a names which part got converted: not the response, but the response's **normalization**. **And it explains the banked negative instead of merely recording it.** Target #19's failed repair (`a₀ → k|a|`, giving `a ∝ GMH₀/R²`) is **the same conflation from the other side**: it gives `a₀`, the normalization, the `|a|`-dependence that belongs to the amplitude. One error promotes a scale to a field, the other demotes it to an amplitude. **Both break MOND, and neither was a near-miss to retry with better algebra.** **The repair works and the cross-term survives.** Dropping the standalone term gives `a = a_N + √(a_N a₀)`, verified numerically at `a_N/a₀ = 10⁴ … 10⁻⁴`: `a/a_N → 1.010` at the high end and `a/√(a_N a₀) → 1.010` at the low end. **Both limits correct with no regime switch**, and §10b's contradiction is gone. The cross-term is **not** lost with the term it was built from, and the reason is exact: §4.2 needs `a₀R` as a per-channel **content** measure, which is legitimate — at the horizon `a₀R_H = c²/(2π)`, a potential depth of order `c²`, which is what a horizon should have. What is illegitimate is taking `-d/dR` of that same expression and calling it a force. **Same expression, two uses, one licensed.** **What it does not fix, and this is not a footnote.** The resulting profile still saturates as `1-μ ~ x^{-1/2}`, so at Saturn it still predicts `≈8.8×10⁻⁸ m/s²` against `~10⁻¹⁴`. **Cassini still requires ≦35's screening and the `n > 2.79` envelope bound.** Removing `Σ₀` fixes the deep end, not the near end; the two results are independent and both are needed. **The `2π` is also untouched** — `a₀` is now correctly a normalization and the factor is a coefficient on it, exactly where ≦10 left it. **Tier and what is owed. M2 — internal-consistency resolution by re-reading, not new physics.** No new postulate; the census confirms the count did not move. **Owed:** `Paper_030` §3.2 needs *rewriting*, not flagging — `Σ₀` presented as a content normalization feeding §4.2, no gradient taken, no standalone term, and §6.3's profile corrected. That is an edit to a load-bearing derivation and is **deliberately not performed**: it changes what the paper derives, and the arc's own discipline is that such a change is proposed and reviewed rather than applied by the finder. **Until it is done, §6.3's displayed deep limit remains wrong.**
35. **Research 2026-09-04 — the regime map: Newtonian half derived, deep-MOND half blocked by an unrecorded contradiction between `Paper_030` §6.3 and §7.1. Plus a correction to ≦34's Cassini bound.** Attacking debt 2 of `Note_a0_ThresholdFromHorizonCompetition.md` §6, written up as §10 of `Note_TheSwitchIsDerived_TheShapeIsInherited.md`. **Result 1 — the Newtonian half of the map is derived.** Above `cH₀` the chain's own boundary is nearer, so cosmic content is screened by `F_V5(u)`, `u = R_H/R_a > 1`. Both horizon-dependent terms in `Paper_030` §6.3's profile die — the pure cosmic term `a₀ → a₀F` linearly, the bilocal cross-term as `√F` — leaving `a → a_N`. **Newtonian dynamics recovered from screening rather than declared out of scope**, which is what §7.1 previously had to do and what Cassini requires. **Result 2 — the deep-MOND half cannot be mapped, and the reason is an arithmetic contradiction inside `Paper_030` that was not recorded.** §6.3's profile is `a = a_N + a₀ + √(a_N a₀)`; §7.1 says that for `a_N ≪ a₀` the *“bilocal cross-term dominates over both pure-source terms”*. **False.** With `a_N = εa₀`, the terms are `εa₀`, `a₀`, `√ε a₀`, and `ε < √ε < 1` for every `ε < 1`, so the ordering is **always** `a_N < √(a_N a₀) < a₀` — the **constant cosmic term dominates**, by `1/√ε`, which *grows* with depth. Checked numerically at `ε = 10⁻¹, 10⁻², 10⁻³, 10⁻⁴`: `a₀` dominates at every point. A constant floor `a → a₀` gives `v² = a₀R`, i.e. **rising** rotation curves, not flat. §6.3 and §7.1 cannot both be right. **Result 3 — and the resolution is a flag the paper already carries, which upgrades that flag to load-bearing.** §3.2's same-day flag says `Σ₀ = -a₀R` **converts a response into a source**: `Paper_029` §§3.3/5.1 make horizon content first order in the chain's own acceleration and **zero for a non-accelerating chain**, and integrating it as a constant background manufactures a standalone `a₀`. **Drop that term and the profile is `a = a_N + √(a_N a₀)`, whose deep limit is `√(a_N a₀)`, and §7.1 becomes correct.** So the `Σ₀` flag was recorded as an attribution problem and is in fact **load-bearing for whether ED reproduces MOND at all**. **Caution, recorded in both places:** the *other* repair — restoring `Paper_029`'s `a₀ → k|a|` — is a **banked negative** (gives `a ∝ GMH₀/R²`, breaks MOND, target #19). Dropping the term and restoring its `|a|`-dependence are different repairs with different fates. **Neither performed.** **Result 4 — correction to yesterday's hour-old Cassini bound: `n > 1.40` was wrong, the bound is `n > 2.79`.** Found while doing §10. The bilocal cross-term is a **geometric mean** (`Paper_030` §5.2: `σ_bilocal ∝ √(GM a₀)`) and `a₀` is **linear** in horizon content (§3.2, the `Paper_029` dipole amplitude), so screening by `F` sends the cross-term to `√(a_N a₀)·√F` — **the square root of the suppression, not the suppression**. Requiring `√F < 1.14×10⁻⁷` gives `F < 1.29×10⁻¹⁴` and **`n > 2.79`**. The constant `a₀` term is linearly screened and needs only `n > 0.82`, so the cross-term binds. **Consequences:** exponential and stretched-exponential envelopes still clear it trivially, so ≦34's cross-arc check with soft-matter's Maxwell relaxation stands; but `n = 2` now **fails** where ≦34 said it passed. §6 and its table are corrected in place with a dated note. **Status of Target #19 after today: one debt of three discharged, one half-discharged, one blocked.** The switch is derived (≦34). The regime map is derived on the Newtonian side and blocked on the deep-MOND side by `Σ₀`. `√(a_N a₀)` remains `Paper_QuadraticStrain_v1` §5's job. **The `2π` is still untouched.** **The bottleneck is now named and it is `Σ₀`**, not the threshold mechanism — which is a more useful place to be stuck than ‘the regime map is asserted’.
34. **Research 2026-09-04 — Target #19's asserted switch is now derived, `μ` is re-filed as inherited, and Cassini bounds the V5 envelope. `Note_TheSwitchIsDerived_TheShapeIsInherited.md`.** **The debt was dischargeable from material already in the corpus, and nobody had looked.** `Note_a0_ThresholdFromHorizonCompetition.md` §6 conceded its switch was asserted and its F1 asked for a construction carrying both boundaries. That construction is `Paper_039` §3.1's definition of `Γ_cross` — a **single integral over one kernel** `K_V5` — combined with `Paper_090` §4.4, which makes the V5 envelope a function of a **single argument**, Lorentz-invariant separation. The two horizons are one object on two axes (`Paper_047_5`, M3), so they are two ways for one argument to diverge, and a bounded decaying function of one variable is killed by whichever diverges first. **Cutoff at `min(R_a, R_H)`; `a_switch = cH₀`; shape-independent; no new postulate** — which matters given the same day's measurement of 173 named postulates (≦33). **In exchange the note withdraws the discreteness, which is failure mode 7 committed while documenting failure mode 7.** The threshold note said *“that changes **discontinuously**… a **discrete switch**, not a smooth response”*. Derived is *which* boundary dominates and *where*; how sharply it hands over is the V5 envelope's profile near cutoff, and `Paper_090` §7.2 states that shape is **value-layer**. A hard envelope gives a switch, a broad one a gradual crossover, both admissible. **The scale survives; the discreteness does not.** Corrected in §3, §6 and F1 of the threshold note. **The substantive result: `μ` is the V5 envelope, so it is inherited rather than open.** `Paper_030`'s bilocal cross-term is V5-mediated horizon content; `μ` measures the fractional handover between cross-term-negligible and cross-term-dominant; so `μ`'s shape is the coarse-grained image of `F_V5`'s shape, which §7.2 lists as inherited. **Three blocked attempts to derive `μ` (threshold note §5b, `Paper_030` §6.3, ≦15) were not incomplete work — they were attempts to derive a value-layer quantity.** Filed as form-IDENTIFIED (M2). It also closes a bookkeeping gap: `Paper_026` preamble item 3 carries exactly this disclaimer for the **V1** envelope, and no gravity or dark-sector paper had ever carried it for **V5**, the kernel that actually carries the horizon coupling. **And one new observational bound on a value-layer quantity.** The threshold note's F4 said everything rests on cosmic content being screened at solar-system accelerations. Quantified: at Saturn `a_N = 6.46×10⁻⁵`, so `R_a = c²/a_N = 1.39×10²¹ m` against `R_H = 1.32×10²⁶ m`, putting the cosmic boundary at **`u = R_H/R_a = 9.50×10⁴`** in units of the chain's own. The unscreened cross-term `√(a_N a₀) = 8.80×10⁻⁸ m/s²` against a Cassini-class bound `~10⁻¹⁴` demands suppression `8.80×10⁶`, hence for a power-law envelope **`n > 1.40`**. Exponential and stretched-exponential clear it by ~10⁴ and ~130 orders; `n = 1` **fails**. **Cross-arc check, positive:** `Paper_090` §5.1's independent soft-matter identification of V5 with Maxwell stress relaxation is **exponential**, so the envelope class chosen on polymer-melt grounds satisfies a bound imposed by planetary ephemerides. Two inherited choices agreeing, recorded as consistency rather than as evidence. **The `2π` is untouched.** This is a fourth route to the scale and still none to the coefficient. It does close one hiding place: the factor cannot live in `μ`, since `μ`'s shape is inherited and its normalization is pinned by the deep-MOND asymptote (threshold note §5). **New highest-value open item created:** a substrate derivation of the V5 envelope shape would convert `μ` from inherited to predicted and make the Cassini bound a sharp test.
33. **F2 audit of `foundations`: ED's stated postulate count is a fifth of the actual one, and today's programme added two postulates without reaching this arc. OPEN/PARTLY FIXED 2026-09-04.** **Arc-selection note — and how it was missed.** `foundations` was the **last** arc, found only by counting coverage after the programme was declared complete: I had written *“all fifteen arc ledgers now audited”* in commit `797b54e` when thirteen had been. Every other arc had either an F2 audit or a 2026-09-03 pointer-sweep entry; this one had neither, last read 2026-07-29 by the extraction agent. **The audit programme's own coverage claim drifted one notch past its support — failure mode 7, committed by the audit, about the audit.** **The arc is meta, not physics, and it is honestly self-tiered — the finding is not a bad claim but a stale count of the thing the whole framework is judged on.** `Paper_088` states a boxed **Postulate-count summary statement**: *“The framework operates with 13 universal primitives + ~18 paper-specific postulates = **31 total substrate-level commitments**”*. `Paper_087` §4.15 gives *“canonical 13 + ~22 … a postulate set of ~35 commitments”*. **A mechanical census (`internal notes/_census_postulates.py`, written the same day) returns **173** distinctly-named `P-*` postulates** in `physics-papers/`, with 7 further matches merged out as spelling variants. **13 + 173 = 186 against a stated 31.** Widening the scan to the whole repo adds only 2. ⚠ **Number corrected 2026-09-04 (same day).** This note first said **51**, from a hand-typed grep that matched only **bolded or backticked** names and so missed every postulate cited in plain prose or in a table cell — an understatement of 3.4×, made *inside the note documenting an understated count*. The figure now comes from `internal notes/_census_postulates.py`, which is re-runnable and carries a baseline; **its first run caught this.** Widening the scan past `physics-papers/` adds only 2, so the number is robust. **The drift is a scope drop, four lines apart, and it is the same shape as ≦23 and ≦26.** The line immediately above the box reads *“Total **Wave-2** paper-specific postulates introduced beyond canonical 13: ~18”* — correctly scoped to a wave, and correct for that wave. The box then drops *Wave-2* and says *“The **framework** operates with”*. **A wave-scoped count restated as a framework-wide one.** Nothing was miscounted; the scope word was dropped, and the sentence became a claim about ED's whole input list. **Why this is the most consequential finding of the programme, small as it looks.** Parsimony is ED's central claim and the axis every audit today turned on — ≦26 (`G` “adds no information”), ≦17 (`P-G-Closure` named), ≦16 (`P-Bilocal-Count` named), ≦23 (three locks are two). **A reader auditing ED for parsimony goes to 087/088 and is told 31.** The accurate figure exists in the corpus and sits in the least authoritative place: `Paper_100`, the program overview, says ***“22+ FORCED theorems and 60+ paper-specific postulates declared transparently”*** — and “60+” is right. **The overview is honest and the constitution is stale**, which is backwards. **And this programme made it worse without noticing.** Today named **two** new labelled postulates — `P-G-Closure` (`Paper_027`, ≦17) and `P-Bilocal-Count` (`Paper_030`, ≦16) — both confirmed present in the census. **Neither had any path back to the arc that counts them.** That is the propagation-failure class of ≦27 and ≦30 landing in the one arc whose job is to hold the input list: **the corpus can add postulates faster than the place that counts them gets re-read.** **Everything else in the arc checks out, and some of it is exemplary.** T18 (the kernel-level arrow) is **M2 (Intermediate)** resting on a *substrate-operational, explicitly NOT algebraic* closure, and Wheeler–Feynman is engaged directly with the honest concession that symmetric electrodynamics is mathematically consistent and the empirical distinction underdetermined — **it does not claim to refute W-F.** `Paper_099`'s Navier–Stokes synthesis is tiered *structural-positive*, the same register as Yang–Mills, and says **NOT a Clay proof**. ξ = 1.7575 and the `0.6` RG exponent are marked **canon-INTERNAL, a benchmark and NOT a solved prediction**. The arc's own staleness log had already caught and fixed the verdict-grammar drift (095's M1 example vs 098's M2 reclassification) on 2026-07-29. **Two live items left standing, both recorded here rather than fixed.** (i) `ξ = 1.7575` and the `0.6` exponent are **sourced to memory files** (`project_ed_sc_3_arc.md`, `project_ed_rg_three_regime.md`), and `CLAUDE.md` says memory is a pointer set and **not ground truth** — a canon-internal number whose only source is the layer the corpus's own rules deny authority to. Re-anchoring to a paper is a real task, not a wording fix. (ii) The reconciled postulate figure itself: the four stated numbers use genuinely different accounting bases (per-primitive, per-wave, corpus-wide, registry-line), so **a single reconciled count needs a stated basis before it can be written**, and inventing one here would be the same error in the other direction.
30. **F2 audit of `cosmology`: a recorded propagation failure, unactioned for two months. FIXED 2026-09-04.** The cosmology ledger's own staleness #3, dated 2026-07-29, records that the CCC paper's 2026-07-06 correction — **Θ_ED has no canonical `Paper_087` primitive grounding**; its "P12 (ED-threshold)" label is a mistaken citation, since canonical P12 is the chain-level stability functional `Σ_C = Coh−Str−Grad`, unrelated to an event-density threshold — *"was never checked against Paper_087 before propagating downstream"* and had **not** been propagated to Cos-02–06. Verified still true: Cos-02 (14 uses), Cos-03 (12), Cos-04 (8) and Cos-06 (7) all carry `Θ_ED` and **none carried the correction**, with one live *"at primitive level"* phrasing in Cos-03 §5. All four now carry the box and the phrasing is fixed. **The tier does not move** — `Θ_ED` was and remains substrate-parameter INHERITED; only the primitive citation was wrong. **The point of this item is not the fix, it is the interval.** A correction was found, written into a paper, recorded in an arc ledger with the note *"actionable phrasing fix"*, and then sat for two months while four downstream papers kept the mistaken citation. That is precisely gravity Staleness #27, where I did the same thing in a day. **Two independent instances, two months apart, same defect: recording a correction is not propagating it.** Checklist item 21 exists for this and should be treated as blocking, not advisory.
31. **F2 audit of `relativistic-qm`: no finding. 2026-09-04.** **`relativistic-qm` is the most self-disciplined arc in the corpus, and the evidence is that its headline derivation disowns itself.** `Paper_112` derives $\hbar$ as a participation quantum per chain-step, then prints a **circularity disclaimer** in its own body: $\mathcal{B}_{\mathrm{P04}}$ *"has no independent substrate definition in the current corpus"*, so $\hbar = \int \mathcal{B}_{\mathrm{P04}}\,ds$ is a **substitution**. And that disclaimer is **propagated** — the arc ledger's row carries it verbatim, which is precisely the propagation step whose absence generated Staleness #21 and #27 today. The neighbouring row goes further: the $[\hat{x},\hat{p}]=i\hbar$ entry records that an earlier *"structural origin of $\hbar$"* framing was **explicitly disowned as an overclaim**. **This is the drafting habit checklist failure-mode 7 was written to install, already installed here.** **Everything else in the arc holds at the tier claimed.** The two M1 results (103 Cl(3,1) uniqueness, 104 anyon prohibition) are honestly *"form-IDENTIFIED, no D rows"*. `P-Gauge` (114) is a **declared, non-primitive postulate** with its substrate derivation flagged OPEN, and the paper's own title-word *"unconditional"* is glossed in the ledger as unconditional on *which* group, not on the postulate. The mass sector (113) marks the substrate-Higgs and substrate-Yukawa **ASSERTED, NOT derived**. The `A→position` rows (111's UV-divergence reading, the Arc-R stage placements) are labelled as framing rather than derivation. **Strength-word sweep across both arcs: clean.** Every *forbid* in `Paper_116` is standard physics tiered **I** (chiral symmetry forbids a Dirac mass; gauge invariance forbids a gauge-boson mass without a Higgs) — inherited prohibitions, not ED declaring things impossible, so the no-*forbid* rule is not engaged. Row 14 is the arc in miniature: it observes the Standard Model has two mass-generation mechanisms, states it does **not** derive that a third is impossible, marks the structural claim **OPEN**, and then argues with its own label in parentheses. **No fix found. No tier changed. Nothing applied to this arc.** **Two record-only items, neither a defect.** (i) Two files share the number 112 (`LightlikeWorldlines` and `hbar_Origin`) — flagged in the arc's own staleness log, rename deferred because it breaks cross-refs. (ii) The Wolfram/Ruliad lineage sentence appears in all fifteen Position Statements, against the standing no-Wolfram-in-public rule; the ledger records **AP's standing call: leave it**. A recorded, deliberate divergence is the opposite of drift.
32. **F2 audit of `state-reduction`: one stale self-flag in the arc-state summary. FIXED 2026-09-04.** **One stale self-flag, of the class already found twice today.** The arc-state paragraph read: *"honestly tiered throughout and internally consistent **(bar the one open-piece-i banner/body desync)**"* — while **staleness item 1 of the same ledger** records that desync **FIXED 2026-07-29**. Verified against the paper: `StateReduction_OpenPiece_i_EnergyFunctional.md` line 3 now reads *"Status: RESOLVED"*, and the pre-resolution sections carry an explicit note (*"this section poses the gate; it is RESOLVED below"*) ahead of the **2026-07-21 EFE calculation** that closed it. **The fix landed; the summary that flags it never got updated.** Corrected — the parenthetical now records the desync as fixed and points at the resolution. **This is the third instance of a staleness note that is itself stale** (after the `OtherTheories` ledger and my own unpropagated #10). The pattern is narrow and worth naming: **a summary paragraph that enumerates the arc's exceptions is a second place the correction has to reach, and it is the place nobody looks after fixing the paper.** Checklist item 21 says *"a ledger entry is not propagation"*; this adds the mirror — **a ledger's own prose summary is a propagation target too.** **The rest of the arc is clean and, unusually, clean *because* it de-weaponized itself.** It introduces **no new derivation**; it locates ED's standing measurement account against Diósi–Penrose. The 2020 Donadi bound: the immunity argument is **not** rested on the 2020 authors' own caveat but on later work, and says so. The MOND-$E_G$ line: **honestly de-weaponized** — the EFE calculation showed Earth's field Newtonizes the collapse and the 5–50× signature is an **EFE artifact**, and the paper reports that against its own interest. Three items sit **OPEN** (collapse coefficient, exact energy functional, channel-granularity theorem), with the last explicitly downgraded to *"a write-up task, no longer load-bearing"*. Falsifiers F1/F2 are stated, and §7 declines to claim a prediction it could have claimed — *"No F5"*, since ED's non-emission of collapse radiation is shared with standard QM.
29. **F2 audit of `dynamics`: cross-arc tier disagreement on `c_T = c` — D-structural in gravity, Inherited in dynamics. FIXED 2026-09-04.** Arc chosen by the #25/#28 model: longest unread **and** lowest perceived risk, since care scales with risk and nobody writes defensively about GW phenomenology. **Two arc ledgers give `c_T = c` different tiers, and the corpus's rule is that the ledgers are the authority.** The **gravity** ledger: *`c_T = c` **structurally** (GW170817 as identity) … one substrate, one transport (P05), one cone → shared by all species — F-independent — **D-structural***. The **dynamics** arc: `Paper_ED_GW_00` preamble item 1 — *“does **not** claim derivation of … the speed-of-GW ≈ c (GW170817). All three are **INHERITED**”* — and its comparison table rows the propagation speed as ***Inherited** (GW170817 confirms)*. **Same claim, two authorities, opposite tiers.** **The resolution is ED's own grammar, and neither ledger applied it.** These are two different statements about two different things. The **identity** — that tensor gravitational waves and light share one cone — is what GR-II derives, from one substrate and one transport process; it is structural and it is what makes GW170817 a *survival* rather than a fine-tuning. The **value** of that shared speed, `c ≈ 3×10⁸ m/s`, is empirical, fixed by P-RB-1's identification of the substrate rate with the observed light speed. **Form-derived, value-inherited** — the corpus's own `Paper_095` vocabulary, and exactly the case it was written for. GW-00 marks the whole row *Inherited* and so loses the identity; the gravity ledger marks it *D-structural* without noting that the number is inherited. **This is the first *under*-claim found today, and it is not harmless.** GR-IV's untunability argument leans on `c_T = c` being structural — *“the same causal cone as GW170817's, one fact, two faces”*, paired with `α₂ = 0`. If the dynamics arc tiers the same fact **Inherited**, that argument is undercut from inside the corpus, by a ledger the discipline says is authoritative. A skeptic comparing the two ledgers finds ED claiming a structural result in one folder and disclaiming it in another. **Under-claiming is a smaller credibility risk than over-claiming and a larger coherence problem.** **Not a defect in GW-00's judgment.** That paper is unusually severe with itself — the arc ledger records that **every** paper in it declares *“zero pure-D rows”*, so the `Derived` tier is **empty arc-wide**, and a three-paper spot-check returned *“no catch”* with GW-00 noted as *“NOT oversold”*. The arc's instinct is to inherit rather than claim, which is the right instinct and is why this went unnoticed: **nobody audits an arc for excessive modesty.** **Fix applied:** both ledger rows now carry the split — *identity structural (GR-II), value inherited (P-RB-1)* — and each points at the other. **No tier is raised in the dynamics arc:** GW-00's own claim (that it does not derive the *speed*) stands, since the speed's value is indeed inherited. What changes is that the row no longer reads as disclaiming the identity, which is a different thing and is derived upstream.
28. **F2 audit of the Yang–Mills arc (`Paper_021`/`023`, the mass gap): CLEAN. No finding. 2026-09-04.** Chosen as the highest overclaim risk in the corpus — a Clay Millennium Problem — and from the time-since-re-read queue. **It passes every check applied today, end to end.** **Paper-level:** `Paper_023`'s preamble carries seven non-claims, item 1 being *“does not claim a solution to the Clay Yang–Mills Millennium Prize problem”*, and item 7 defines *Clay-relevance* as *“strictly below constructive proof.”* A dated FIXED note records a prior round relabelling an audit row from a derivation claim to A→position. **Audit-table level:** `Paper_021` names its three postulates explicitly in the table — **P-Gap-Coercivity**, **P-Profile-Rescaling**, **P-Quartic-Sign** — labels the standard machinery (Perron–Frobenius, Glimm–Jaffe) **I**, and scopes the single **D** row to *“substrate adaptation of standard machinery … the new content of this paper.”* That is exactly right: the adaptation is the contribution, not the machinery. **Ledger level:** the arc ledger tiers the result as *spectral-gap **form** `Δ ∝ 1/ℓ_*`, form FORCED **given postulates**, coefficient INHERITED*, and records a five-paper spot-check with **no catch**. **Propagation level — the check that failed on #27:** the summaries are as careful as the papers. `ED_UnifiedFramework_Report`: *“the mass gap is a mechanism, not a Yang–Mills existence proof; continuum survival is open.”* `ED_Program_Review` §6.2: *“Physics explains; constructive field theory proves. They are not interchangeable currencies.”* The public essay is titled *“an Honest Word About a Million-Dollar Problem.”* **Checked against items 20(a)–(d), 19 and 21; nothing fires.** **Reported clean per F3** — the audit prompts and this programme both carry the instruction not to manufacture a finding, and that applies to me. *Two things this supports: the drift model from #25 (worst where an arc has not been re-read; this one was spot-checked and is clean), and a second correlate — **care scales with perceived risk**. The corpus is most disciplined exactly where a reader would most expect it to overreach.*
27. **PROPAGATION FAILURE on Staleness #10 — the disputed `2π` stood unflagged in **twelve** documents, including all three public-facing ones. FIXED 2026-09-04.** Found while auditing `dark-sector` (next in the time-since-re-read queue). `Paper_ED_DarkSector` §1 asserts the `2π` *“forced by the azimuthal-Fourier normalization of the dipole projection”* — the exact claim re-tiered to **Postulated / disputed** that morning. A corpus sweep for the claim, excluding files already carrying the flag, returned **twelve unflagged documents**: `ED_WHITEPAPER`, `ED_Program_Review`, `paper_ED_Framework_13_Primitive_Generative_System`, `Paper_ED_DarkSector` + its arc ledger, `Paper_ED_Predictions_Bundle`, `Paper_ED_Postdictions_PassedTests`, `Paper_SCBU_SubstrateCosmologyBoundary` + the cosmology ledger, `Paper_027`, `Paper_GR-IV`, and `Paper_SelectedFormalisms_I_Gravity`. **Ten carried the claim as a live assertion and are now flagged inline** (the other two mention the mechanism without asserting the forcing). **This is my failure, not the corpus's.** Staleness #10 was recorded at 028/029 and this ledger and propagated nowhere — the same close-the-loop defect this session opened by fixing elsewhere. **The lesson is structural and belongs in the checklist:** when a claim is re-tiered, the re-tier is not done until a corpus-wide grep for the claim's *distinctive phrasing* has been run and every hit either flagged or confirmed as not asserting it. A ledger entry is not propagation. *Nothing was re-derived and no tier changed here; ten documents now say what the gravity ledger already said.*
26. **F2 audit of `constants-inherited`: “`G` adds no information” sits two rows from “`ℓ_ED` set by Newton-recovery”, and today's `Paper_027` work never reached this ledger. 2026-09-04.** **Arc selection note.** Chosen by the criterion Staleness #25 produced — *time since last re-read*, not importance. This was the **only** arc ledger of fifteen with **no stated read date**, and untouched since the 2026-07-29 corpus sweep. The criterion worked on its first use: the arc that had gone longest unexamined is the one carrying an unnoticed circularity and a missed same-day propagation. **The circularity is on one page, two rows apart, and unnoticed.** The event-scale table lists **`ℓ_P = ℓ_ED`** with the note *“`ℓ_ED = ℓ_P` set empirically via Newton-recovery (`Paper_027` §5.3)”* — i.e. its value is fixed **by matching `G`**. Two rows later, under *Derived combination (no new input)*, sits **`G = c³ℓ_P²/ħ`**, described as *“a combination, not an independent input (**adds no information**)”*. **If `ℓ_ED` is fixed by matching `G`, then presenting `G` as a derived combination of `ℓ_ED` is circular as an explanation** — the two carry one piece of information between them, and which one is called the input is a labelling choice. **The count is right; the word is wrong.** There *is* one free parameter here, not two, so the ledger's arithmetic is correct. What *“adds no information”* misstates is the direction of gain: `G` was not explained, it was **renamed**. Today's `Paper_027` work settles what the real parsimony is, and it is **not** this row — it is a **multi-use** argument: `ℓ_ED` is one postulated scale spent in the holographic channel count (`Paper_025`), the black-hole area law and entropy coefficient (`Paper_043`, `BH_Thermal2Pi`), and the ultraviolet cutoff (`Paper_111`). A framework carrying `G` as an independent input must *separately* supply a grain and a cutoff. That is the reduction, and it is a claim about the corpus-wide input list rather than about this one combination. **And this arc missed today's close-the-loop, which is my failure, not the corpus's.** On 2026-09-04 `Paper_027` gained the labelled postulate **P-G-Closure** (dimensional analysis fixes the *powers*, not the coefficient; the equality needs a postulate), the multi-use parsimony defence, and the **F-P** result (all three companion roles *inherit* `ℓ_ED` from §5.3 rather than determining it — one determination, three uses). **None of it reached this ledger**, which is the arc catalog for exactly these claims. The gravity ledger recorded it; the constants ledger, which is where a reader goes to ask what ED treats as an input, did not. **Verified sound, for the record.** The dimensional generalisation `G_d = c³ℓ_P^{d−1}/ħ`, tiered *derived*, checks out: in `d` spatial dimensions `F = G_d m₁m₂/r^{d−1}` gives `[G_d] = L^d T⁻² M⁻¹`, and `c³ℓ^{d−1}/ħ = L^{3+d−1−2} T^{−3+1} M⁻¹ = L^d T⁻² M⁻¹`. Match, and it recovers `G` at `d = 3`. No issue. **Fixes applied below.**
25. **F2 audit of the q-compute wall (`Paper_056`/`054`, `M_crit`): arc already audited and clean; one disclosure gap in the shape-retreat. 2026-09-04.** Chosen as the first *prediction* rather than derivation, in an untouched arc, and because the README carries an unusual two-sided falsifier. **The arc has already had this audit, and passed it — the first one today of which that is true.** The q-compute ledger records a spot-check of four papers with **“no catch”**; tiers the 140–250 kDa mass as *“Selected/Inherited (number-pressured — do NOT bank as firm), a 2-POINT extrapolation”*; records `Paper_056` as having been **stale on the number and fixed 2026-07-29** when the ≥170 kDa Nature-2026 result landed inside the window; records **README tier optimism as fixed** (it had called the arc “CLOSED” and exhaustiveness “proven” where the papers rest it on two postulates); and summarises the arc as *“coherent, well-audited … whose spine is **Grounded, not Derived**”*. `Paper_054` labels the power-law form **A→regime** in its own audit table and says in prose that P04 + P11 + V1 give the *existence* of a commit-rate and **do not force its functional form**. Nothing here overclaims. **The one gap is a disclosure one, in the retreat itself.** `Paper_056`'s 2026-07-29 update moves the claim from the number to the shape: *“Read the **shape** (sharp + architecture-independent) as the prediction, not the 2-point-extrapolated number”*, on the ground that the new data *“does not touch the load-bearing structural claim.”* **But the shape rests on the same open assumption the number does.** The arc's own ledger says so explicitly: the power-law form of `Γ_commit(M_eff)` is **OPEN** and *“load-bearing under both the wall's shape **and** number”*, with exponential and log alternatives substrate-consistent and changing *“`M_cap` **and the wall**”*. **The update note does not mention this.** A reader of `Paper_056` alone takes the retreat as a move to firmer ground; it is a move to ground resting on the same A→regime choice. **And that weakens the falsifier in a way the public README does not show.** The README row is two-sided — *“superposition past a sharp mass ceiling, **or no ceiling at all**”* — which is a genuinely good structure. But if the power law is what makes the wall *sharp*, then observing a **gradual** falloff instead would not distinguish *“ED is wrong”* from *“the `Γ_commit` form is wrong”*. The sharpness half of the falsifier is only as sharp as an assumption the arc lists as open. *(The architecture-independence half is unaffected and remains the cleaner discriminator — it needs two platforms at comparable mass, which do not yet exist.)* **Recommended, small.** One clause in `Paper_056`'s update note: that the shape, like the number, is conditional on the power-law `Γ_commit` form (`Paper_054` §3.2, A→regime; arc ledger, open row). And the README's falsifier row should attach the same condition to its *sharpness* clause while leaving *architecture-independence* unconditioned. **No tier changed** — every tier involved is already correct; this is about what the retreat discloses. **What this says about the audit programme (F2).** Eight chains audited today across six arcs. This is the **first where prior work had already found what I would have found**, and the ledger shows the same moves — a spot-check, a dated staleness fix, a README de-optimisation, honest A→regime labels. **So the drift is not uniform across the corpus: it is worst where an arc has not been re-read since drafting, and effectively absent where one has.** That is a better model of the defect than “claim-strength drift is corpus-wide”, and it makes the checklist items preventive rather than merely diagnostic.
24. **F2 audit of `Paper_038_5` (Λ, `w = −1`): the paper is honest, the README is not, and the elimination leaves an interval. OPEN, 2026-09-04.** First cosmology-arc audit; chosen because *“forced”* is an item-20(c) trigger and because `w = −1` is a Tier-1 falsifier in the public README. **The paper itself is the most honest text audited today, and the drift is downstream of it.** `Paper_038_5` §3.5 is titled *“What is OPEN”*. It says `w = -1` is *“forced by **viability**”* — not by the substrate — names the **residual open** (*“whether the substrate cleanly forces the frozen-floor value or merely stipulates it”*), identifies the bootstrap knot (acceleration is needed to reach the de Sitter state that freezes `H`), and states its falsifier. Nothing in that subsection overclaims. **The public README does.** Its falsifier row reads *“Dark energy with `w ≠ −1`, evolving, or phantom — `w = −1` is **forced**”*. The paper says forced-by-viability with an open residual on whether the substrate forces or stipulates the frozen value. **If it is stipulated, `w = −1` is a commitment rather than a derivation** — still a legitimate thing to be falsified on, but not what the README's one word claims. **And the elimination is not exhaustive — this part is new and the paper does not discuss it.** §3.5 argues: a `Λ` tied to the *instantaneous* Hubble radius gives `ρ_Λ ∝ H² ⇒ w = 0`, no acceleration; therefore `Λ` is the frozen floor and `w = −1`. That is a dichotomy between two endpoints of a continuum. Writing `ρ_Λ ∝ H^{2n}`, matter domination gives `ρ_Λ ∝ a^{-3n}` and hence **`w = −1 + n`**: `n = 1` is the refuted live-tracking case, `n = 0` is the frozen floor. **The argument eliminates `n = 1` and asserts `n = 0`; it says nothing about the interval between.** Current bounds (`w = −1.03 ± 0.03`) give `|n| ≲ 0.03`, so the whole observationally allowed range sits inside the interval the argument does not address. A measured `w = −0.98` would be `n = 0.02` — not the refuted case, and not what ED claims. **This is the third instance today of the same argument shape:** decisive against one endpoint, asserting the other, with the interval carried by an unstated genericity. `GR-III` §4 kills `β = 1` and needs `β = 0` exactly (Staleness #12); `GR-IV` states `α₂ = 0` exactly on a leading-order input (#20); this states `w = −1` exactly on a two-endpoint elimination. **In all three the corpus's own source is honest and the summary is not.** **APPLIED 2026-09-04.** F3 is sharpened to *any* detected evolution at any precision, stated as a **running test** rather than a threshold, with the honest asymmetry recorded (the commitment is exact; the argument eliminates only the `n = 1` endpoint). The README row is rewritten to match and names the paper's residual. §3.5 carries the interval note. **The endpoint-elimination pattern is added to the checklist as item 20(d)**, with all three instances tabulated — it is the one part of failure mode 7 the strength-word grep cannot catch. *Originally recorded as:* **Recommended, not applied.** (i) The README row should read *“`w` measurably different from `−1`”* with the paper's residual named, not *“`w = −1` is forced”*. (ii) §3.5 should record the intermediate-tracking gap explicitly — one sentence noting that the elimination covers `n = 1` and the claim is `n = 0`, with the interval open — since it is currently invisible and it is where the data actually lives. (iii) The genuinely sharp falsifier is worth stating in its place: **ED is committed to `n = 0` exactly, so any detected evolution at any level refutes it** — which is a *stronger* and more honest position than the vague *“w ≠ −1”*, because it makes precision improvements in `w` a live test rather than a threshold to clear. *No tier changed; §3.5's own residual already carries the correct tier.*
23. **F2 audit of `Paper_066` (no-signaling): the three “independent locks” are one commitment at two levels plus an inherited bridge. OPEN, 2026-09-04.** Chosen because *“over-determined by three independent structural locks”* is exactly what checklist item 20(c) flags, and because GR-IV had just shown two *“independent”* facts to be one equation. **The three locks are not independent: they are one commitment at two levels, plus the bridge between them.** **L1** is microcausality in the emergent operator algebra — `[A_A, A_B] = 0` at spacelike separation, supplied by P-Bipartite-Mapping. **L2** is causality in the substrate — `K_V5 = 0` for spacelike-separated arguments, supplied by P-Substrate-Causality. **These are the same physical commitment stated at two description levels.** **L3** is not a third support at all: it says DCGT coarse-graining *preserves* algebra-locality, i.e. that the two levels correspond. It is the **bridge**, and without it L1 and L2 are statements about disconnected things. **So §3.4's “refutation of any single lock leaves the others intact” does not hold for L3.** Refute the bridge and L1 concerns an emergent algebra with no established relation to the substrate, while L2 concerns a substrate with no established relation to the physics — neither establishes no-signaling *for the theory*. The three do not fail independently; two of them stop meaning anything without the third. **And §3.4 concedes the L2 case in its own hedge.** It argues that refuting L2 leaves L1 intact because *“the marginal would still be independent **at the formal level**”*. That qualifier is the whole issue: formal marginal independence together with superluminal substrate signalling does not mean no-signaling holds — it means the formalism has stopped being faithful to the substrate. A lock that survives only formally is not a lock. **The load-bearing element is the one that is not ED's.** Audit row 9 tiers L3 as **I**, inherited from `Paper_073`. So the bridge on which the composition depends is inherited, while the two things it bridges are the same commitment twice. That is the opposite of over-determination: it is a single support with an inherited joint. **What is not in question.** No-signaling itself holds in ED, and this audit does not touch it — L1 is the standard argument correctly applied and is honestly tiered **D-via-I**. What fails is the *over-determination rhetoric*: §1's *“FORCED over-determined … three independent structural locks”* and §2's *“refutation of any single lock leaves the others intact”*. **Audit row 10 already tiers the composite verdict as A→position**, so the table is right and the prose is a notch above it — the same claim-strength drift recorded as checklist §10 failure mode 7, and the second instance today (after GR-IV's `α₂`/`c_s`) where two *“independent”* facts are one fact twice. **Item 20(c) flags it on the word “independent” alone.** **Recommended, not applied.** §1 and §2 should say *two levels of one causality commitment, joined by an inherited coarse-graining bridge* rather than *three independent locks*; §3.4's independence argument should be replaced by the honest structure (L3 is necessary for L1 and L2 to compose); and the genuine robustness claim that survives is worth keeping in its correct form — **the commitment is checkable at two levels, so a failure at either would be visible**, which is real and is not over-determination. *No tier changed: row 10 is already A→position.*
22. **F2 audit of `Paper_065` (monogamy) — §3.3's CKW inequality does not follow from §§3.1–3.2. OPEN, 2026-09-04.** Chosen because the entanglement arc is nominally *closed*, which is where drift survives, and because this ledger already records that *“CLOSED, no open frontier” is an overstatement*. **(a) §3.3 does not follow from §§3.1–3.2.** The chain gives `Σᵢ ℰ_{A,Bᵢ} ≤ Σᵢ W_{A,Bᵢ} = W_A ≤ W_max` — a bound by a **chain-local constant**. The displayed CKW form is `Σᵢ ℰ_{A,Bᵢ} ≤ ℰ_{A,(B₁∪…∪Bₙ)}`, a bound by a **state-dependent quantity**. These are different statements, and the second is strictly stronger whenever `ℰ_{A,union} < W_max`. Concretely: `ℰ_{A,B₁} = ℰ_{A,B₂} = 0.3 W_max` with `ℰ_{A,union} = 0.1 W_max` satisfies the derived bound (`0.6 ≤ 1`) and violates CKW (`0.6 > 0.1`). Nothing in §§3.1–3.2 relates `ℰ_{A,union}` to `W_A`, so nothing excludes it. **(b) The gap is in the stated postulate, and closing it costs the inequality.** The step works if `ℰ_{A,union} = W_A` — saturation holding **generally**, for every pair *and* for the union. But **P-Measure-Saturation is stated only for the maximally-entangled limit** (*“saturates the V5 budget in the maximally-entangled limit”*). Strengthen it to general saturation and §3.3 follows — but then `Σᵢ ℰᵢ = ℰ_{A,union}` is an **equality**, and CKW's physical content is the *strict* inequality: the deficit is the residual three-tangle, the genuinely tripartite entanglement. **A pure budget-additivity account gives equality and leaves the three-tangle nowhere to live.** The strict inequality needs the measure to be *sub-additive* relative to the budget, and nothing here supplies that. **What is actually derived, and it is worth keeping.** `Σᵢ ℰ_{A,Bᵢ} ≤ W_max`: a finite shared budget capping total pairwise entanglement, with `ℰ_{A,B₁} = W_max ⇒ ℰ_{A,Bᵢ} = 0` for `i ≥ 2` (§3.4, maximal exclusivity — which **does** follow). That is genuinely monogamy-flavoured, it is the right *kind* of mechanism, and it is a real result. **It is not CKW.** **The paper half-knows this.** §4's FORM-FORCED line reads *“Existence of finite budget partitioning”* — which is exactly the defensible claim, and matches (c). It is **§3.3** that attaches *“Form FORCED”* to the displayed CKW inequality, and the arc ledger's row inherits that: *“Monogamy (CKW-form inequality) — form D (form-forced)”*. So §3.3 and §4 disagree about what is forced, and the stronger of the two propagated. **Textbook claim-strength drift (checklist §10 failure mode 7), and checklist item 20(a) would have caught it:** the claim is stated above the tier of its weakest input, which is a postulate scoped to one limit. **Recommended, not applied.** §3.3 should state `Σᵢ ℰᵢ ≤ W_max` as the derived result and present CKW as *consistent with* it rather than forced by it; the ledger row should re-tier from **form D** to **Grounded** with CKW marked as the inherited target; and if the CKW form is wanted as a result, the open item is a **sub-additivity argument for `ℰ` against the V5 budget** — which is where the three-tangle would have to come from, and is a real piece of physics rather than an editorial fix. *No tier changed pending the author's call, since this narrows a published claim in a nominally closed arc.*
21. **F2 audit of the ℂ-selection chain (`Paper_QuantumLogicKeystone_GleasonReconstruction` §5, `Paper_001`) — first audit outside gravity, and the pattern holds. OPEN, 2026-09-04.** Chosen deliberately outside the gravity arc to test whether *“correct result, claim a notch too strong”* is a gravity habit or corpus-wide. **It is corpus-wide.** **The ℝ-exclusion's ED-side premise is a representational choice, and unlike its neighbour it is not flagged.**

§5 rules out ℝ well: it explicitly rejects the naive argument (*"a real space cannot represent a phase"* — false, real-Hilbert QM represents `U(1)` via `J` with `J² = −I`) and identifies the correct discriminator as **scalar versus operator**. A real division ring has no central scalar square root of `−1`, so if ED's phase is a *scalar* amplitude factor the field must contain `i`. That clarification is genuine and non-obvious.

The question is where *"ED's phase is a scalar"* comes from. The section cites `Paper_001`'s carrier `P_K = √b_K·e^{iπ_K}`. But **P09 supplies an angle, not a complex number** — `U(1)` is definable as `ℝ/2πℤ` or `SO(2)` with no reference to ℂ — and writing that angle as `e^{iπ}` rather than as a rotation `J(π)` on a real 2-space is a **representational choice made in `Paper_001`**. So the chain is: P09 gives an angle → `Paper_001` represents it as a complex scalar → §5 rules out ℝ because the phase is a scalar. **The step that excludes ℝ is the representation, not the primitive.**

**Both papers are honest about tier, and that is why this is a disclosure finding rather than an error.** `Paper_QuantumLogicKeystone` preamble item 2 calls it *"an account-tier selection, not a from-scratch derivation."* `Paper_001` preamble item 2 says the complex carrier is not forced from P01–P04 alone and needs P09. Neither claims a derivation. What is missing is narrower: **§5's ℝ-argument does not name its premise, while §5's ℍ-argument does** — the latter states outright that *"the ED-side premise (that ED has a genuine ⊗) is supplied by P-Bipartite-Mapping / Paper_063, a paper-specific postulate, not one of the canonical 13."* Asymmetric disclosure inside one section, and the undisclosed side is the one carrying more weight. **This answers the question left open in `Paper_PrimitiveMinimality_IndependentReconstructionProbe` §6.** Both blind runs concluded that phase and complex amplitudes must be *injected*, which raised the question of how much the Solèr selection does given P09 already carries a `U(1)`. **The answer is that the two halves differ.** The ℍ-exclusion does real work and is honestly conditioned. The ℝ-exclusion does less than it appears: **P09 alone is consistent with ℝ**, and what rules ℝ out is `Paper_001`'s scalar representation. The blind runs were right that something must be put in; ED's answer is P09 *plus* a representational commitment, and only the first is a declared primitive. **The repair is available and would strengthen the claim rather than weaken it.** Argue from the primitives why the phase must act as a **scalar** — uniformly on all amplitudes at a locus — rather than as an operator that could act channel-dependently. P03 (spatial homogeneity / no preferred channel) and P07 (channel distinguishability) are the natural candidates: if the phase were an operator it could mix channels, which P07's intrinsic-identity commitment arguably forbids. **ATTEMPTED 2026-09-04, and the proposed route fails while a better one succeeds.** P03/P07 do **not** deliver it: real-Hilbert `J` acts *within* each channel's amplitude space and never mixes channels, so P07 is not engaged, and P03 only requires `J` be locus-independent. **The working argument is continuity of observable phase:** P09 gives a continuous `U(1)` phase; `QuadraticStrain` §3.2 and `Paper_005.5` §3.3 make relative phase observable *continuously* via `cosΘ`; real amplitudes give a cross-term whose only freedom is a sign, not a continuous `cosΘ`; continuous phase dependence requires a rotation action on a 2-real-dimensional amplitude space, i.e. a complex structure; and a `J` commuting with all observables is ℂ (Stueckelberg). **This keeps the tier at account** (it is conditional on the interference results, not on the thirteen alone) **but removes the circularity** — the ℝ-exclusion now rests on ED's phase being continuously observable rather than on how `Paper_001` writes its carrier, and it is empirically anchored: sign-only interference would readmit ℝ. **APPLIED 2026-09-04.** §5's ℝ-bullet is rewritten around the four continuity steps, replacing the appeal to `Paper_001`'s carrier form; it now names its own premise (step 2, ED's interference is continuous in the relative phase, resting on P-Quadratic-Strain and the Born-rule reading), matching the ℍ-bullet. The old argument and both attempted repairs are preserved as a provenance note beneath the list, including the P03/P07 negative. Preamble item 2 now names both premises. **Tier unchanged: account.** The asymmetry is gone and the circularity with it. **No tier changed** — both papers already sit at account tier and neither claims a derivation. *Filed to the gravity ledger for continuity of the day's audit trail; the substantive home is `qm-kinematics/` and `substrate-evaluation/`, where it is cross-filed.*
20. **F2 audit of `Paper_GR-IV`: the `α₂ = 0` algebra is exact, the claim is one tier too strong, and its own source says so. OPEN, 2026-09-04.** Audited GR-IV §3 as the next un-audited load-bearing chain — the preferred-frame kill-switch, a Tier-1 falsifier in the public README, and the flagship example of the untunability reading. **The algebra checks, and is tighter than the paper states.** At `β = 0` the khronometric `α₂` numerator is `−λ + α + 2αλ`, and the scalar-luminal condition is `α = λ/(1+2λ)`. Rearranged, that condition **is** `α + 2αλ − λ = 0` — the same expression. So `α₂ = 0` and `c_s = c` are not two facts, they are one equation in two variable sets. GR-IV says the numerator *"vanishes precisely on the scalar-luminal surface"*, which is correct but understates: it is that surface. **And that is exactly why the claim is over-tiered.** `α₂ = 0` is precisely as exact as `c_s = c`, with no slack. `Paper_GR-III` preamble item 7 gives `c_s = c` as **derived at leading order**, and states outright: *“Integrating out the dissipative reserve **could renormalize the scalar speed by a small amount**; it cannot restore a second cone.”* If `c_s = c(1 + ε′)` then `α₂ = O(ε′)`, not zero. **`Paper_GR-IV` contains no instance of “leading order”, “sub-leading” or “renormaliz”** — it does not carry the caveat from its own source, and states `α₂ = 0` as *“not merely suppressed but exactly zero”*. **Three consequences, and one of them is good news.** *(i)* The public README's Tier-1 falsifier *“Any nonzero preferred-frame `α₂`”* is too strong; the honest form is *“`α₂` above the sub-leading reserve-renormalization scale”*, and **that scale is uncomputed**. *(ii)* The *“one causal-cone fact seen twice”* untunability example is weakened at the seam: `c_T = c` is structural and exact (one substrate, one transport), but `α₂ = 0` rides `c_s = c`, which is leading-order. The pairing is still one fact twice — more literally than intended, per the algebra above — but it is not two exact facts. *(iii)* **The upside:** if `α₂` is not identically zero but `O(ε′)`, and `ε′` is plausibly of the same order as `λ_local ~ ρ_event/ρ_Planck ~ 10⁻⁹³` that suppresses `α₁`, then ED predicts a **specific tiny nonzero `α₂` where GR predicts exactly zero**. Unmeasurable in practice, but it converts `α₂` from a *survival shared with GR* into a *discriminator in principle*, which is a better structural position than the paper claims for itself. **Not asserted — `ε′` has not been computed and GR-III only calls it small.** **ALL THREE APPLIED 2026-09-04.** GR-IV's abstract now says *"to the order that `c_s = c` is established"* and names `O(ε′)`; a new **F0** replaces "any nonzero `α₂`" with *"large compared with `ε′`"*, and notes that a **small** measured `α₂` would *confirm* rather than refute, since GR gives exactly zero; a new §10.5 records **O-ε′** as an open number with the reasons it is worth computing, and states that it is the same reserve integration GR-III §6 already declines, so the two open items are one. The public README falsifier row is softened to match. *Originally recorded as:* **Recommended, none applied since they change a public claim:** carry GR-III's leading-order caveat into GR-IV §3 and its abstract; soften the README falsifier row to name the renormalization scale; and record `ε′` as an open number to compute — it is the same dissipative-reserve integration GR-III already declines to perform, so the two open items are one. **No tier changed pending that decision.** *Method note: the audit found nothing wrong with the derivation. It found that a correct result is stated at a tier its own upstream source withholds — the fifth instance today of a claim being a notch stronger than what supports it.*
19. **F2 audit of `Paper_KM-I` — the corpus holds two incompatible accounts of the MOND transition, and #15 named the wrong one as standing. 2026-09-04.** Audited KM-I as the next un-audited load-bearing chain, chosen because its hinge is *forced-given-030* and 030 changed today (#13, #16). **The finding is not in KM-I.** KM-I is careful: its hinge is labelled *D conditional on I-030*, its §4.4 says outright that *"the khronon carries Paper_030's law; it does not re-derive it,"* and the IR Cancellation Requirement is boxed as *"the construction's only delicate point"* and stress-tested in §6. **The finding is that KM-I and `Paper_030` handle the high-acceleration limit by two different and incompatible mechanisms, and neither cites the other's.** `Paper_030` §7.1 uses a **switch**: three regimes with *"P14 inactive"* above `a₀`, so the cross-term does not exist where ephemerides look. `Paper_KM-I` uses a **smooth Cassini-constrained family**: preamble item 5 (*"slow-saturating members excluded by ephemerides"*), §6.3, and §7's screening discussion, where at solar accelerations `x ~ 10⁷–10⁸` the correction is *"the saturation tail plus O(α) preferred-frame terms"* — present and small, not absent. Its falsifier sentence is explicit that **"there is no mechanism here to evade the static bounds."** One paper says the term is switched off above `a₀`; the other says it is on and constrained. That is different physics, not different wording. **This corrects Staleness #15, and the correction matters.** #15 concluded that `μ` cannot be derived because *"smooth ⇒ Cassini kills it; switched ⇒ the transition is assumed"*, and called ED's transition **a switch**. That was drawn from `Paper_030` alone. **KM-I is the standing MOND result** (this ledger's own Grounded row: *"the standing MOND result — do not reinvent"*), and it is **smooth**, with the Cassini problem handled by constraining the family rather than by switching the mechanism off. So *"ED's transition is a switch"* is false as a corpus-level statement; it is `Paper_030`'s answer, and 030 is the older non-relativistic construction. **And the two findings agree on the substance.** #15 computed that `Paper_030` §6.3's profile saturates as `x^{-1/2}` and would give `8.9×10⁻⁸ m/s²` at Saturn against a `~10⁻¹⁴` bound. KM-I's preamble item 5 already excludes exactly that class — **slow-saturating members** — by name. Independently reached, same conclusion, and KM-I got there first. **The right statement is therefore narrower and stronger than #15's:** `Paper_030` §6.3's transition profile is a slow-saturating member **excluded by the corpus's own standing criterion**, and KM-I's constrained family supersedes it. `μ` is not derived in either paper, but the reason is a constrained family with an unfixed member, not an undecidable choice between smooth and switched. **Two smaller items.** *(a)* KM-I's *"conditional on I-030"* is accurate but **under-specified as of today**: 030's deep-field law now carries P-Quadratic-Strain, the restored §5.3 regime assumption (#13) and P-Bilocal-Count (#16), and KM-I names none of them. *"Forced-given-030"* reads as if 030 were settled. One sentence in §4.4 naming the inherited conditions would fix it. *(b)* If KM-I's smooth account is standing, **target #19's horizon-competition switch is answering a question posed by 030's framing**, not KM-I's. The threshold (where the regimes divide) and the shape (how the curve rounds) are separate, and #19 supplies the first — so it remains useful, but it should not be described as supplying the P14 on/off that KM-I does not use. **#15's closing line to that effect is withdrawn.** **FIXED 2026-09-04.** `Paper_030` §7.1's Newtonian row now reads *"this construction has no content here — P14 is out of scope rather than switched off"*; §7.2 is rewritten (second pass, correcting the first) as *"Parameter-free, and scoped rather than switched"*, stating that the three regimes are a **scope decomposition** and that nothing in the substrate turns off at `a₀`; and §6.3 now flags its own profile as a slow-saturating member excluded outside the deep field, with the Saturn numbers. KM-I carries the matching resolution note. **The two papers are not two mechanisms but a limited-scope construction and its completion:** 030 supplies the deep-field law, KM-I supplies the crossover and carries the ephemeris constraint. **No tier changed in KM-I.** The audit found nothing wrong in it; what it found is a corpus-level inconsistency between two papers and an error in this ledger's own #15.
18. **Chasing the factor of 4: two channel counts, and a multiplicity bound that collides with Bekenstein — OPEN, 2026-09-04.** Ran down the `1 vs 1/4` coefficient `Paper_025` §6 reports as "order-unity" (see #17's F-P entry). **(a) `Paper_043` states its own postulate two ways, differing by `4π`.** Its **P-Horizon-Participation** reads `N = A/(4πℓ_P²)`, while §1's derivation chain cites `Paper_025`'s `N = 4πR²/ℓ_ED² = A/ℓ_ED²`, and audit row 3 records `N = A/ℓ_P²` "up to constants". Three statements, two values, one paper. **(b) The difference is load-bearing, and the branch `Paper_025` supplies is the one that fails.** With `S = N·log g` (row 6) against Bekenstein `S = A/(4ℓ²)`: under `N = A/ℓ²` the required multiplicity is `log g = 1/4`, i.e. **`g ≈ 1.28`** — which **violates `P-Multiplicity-g`'s own stated `g ≥ 2`**. At the postulate's minimum `g = 2`, ED over-produces horizon entropy by **2.77×** Bekenstein. Under `N = A/(4πℓ²)` the requirement is `log g = π`, `g ≈ 23.1`, which satisfies `g ≥ 2`. **The two internal counts give opposite verdicts on whether the corpus is consistent with Bekenstein–Hawking.** **(c) There is no dishonesty here, and that is worth saying.** `Paper_043` is titled *"Area-Law Form FORCED; Coefficient (log g) INHERITED"*, its item 1 explicitly disclaims deriving the `1/4`, and item 5 says "omitting numerical prefactors". The defect is that **"inherited" was doing more work than stated**: `log g` is not free, it is bounded below by the `g ≥ 2` postulate, and that bound collides with the matching under the corpus's own count. **(b)/(d) CORRECTED 2026-09-04, same day: both were artifacts of reading a superseded paper as current.** `Paper_HorizonTilingThreeCounts` §4 already resolves this and states it outright — *"the earlier 'coefficient inherited' reading (Paper_043) is superseded — the coefficient is derived, and it lives in the thermal route, not the tiling."* The standing account is **tiling × thermal factor**: three independent counts (holographic, dynamical frozen-state, straddling-edge) put the tiling near **one** bit per Planck cell, and the `1/4` is supplied by `Paper_BH_Thermal2Pi`. Because the counts land near 1 and not near `1/4`, **the coefficient was never in the count** — so there is no entropy over-production to suppress, the `g ≥ 2` collision is an artifact of the superseded `S = N log g` placement, and the two routes are *not* in conflict. **The real defect is narrower and is a pointer one:** `Paper_043` carried no supersession banner, so a reader arriving there met the broken framing as current. Banner added. *Method note worth keeping: this was one step from a correlated-cell suppression mechanism to fix a problem the corpus had already dissolved. The read-first check caught it, which is the whole reason the rule exists.* The original (d) text follows for the audit trail. **(d) [superseded] The sharper collision is with `Paper_BH_Thermal2Pi`.** That paper claims to *derive* `S = A/4` thermodynamically — `½` from `κ = 1/(2r_s)` and `2π` from near-horizon Rindler smoothness — with no reference to the channel count. If it stands, `log g` is **not** free to be inherited: it is determined, equals `1/4`, and is then forbidden by `g ≥ 2` under `Paper_025`'s count. **Two routes the corpus treats as independent are in conflict through the multiplicity bound, and nothing records it.** **Three ways out, none chosen here.** *(i)* `P-Horizon-Participation`'s `4π` version is the intended count and `Paper_025`'s is being misapplied at the horizon — cheapest, but it makes the two papers disagree about `N` rather than about `g`, and `Paper_025` §6 explicitly compares *its* count with Bekenstein. *(ii)* `g ≥ 2` should be `g > 1`; the postulate's floor may be an unconsidered tidiness rather than a commitment, though a cell with `g < 2` distinguishable configurations is hard to read. *(iii)* **The most promising:** audit row 6 is labelled *"standard entropy on independent cells"*, and horizon cells need not be independent. Correlated cells give `S < N log g`, which relaxes the bound and would let `g ≥ 2` coexist with `S = A/4`. That is a substantive physical claim about horizon correlations, it is exactly the kind of thing ED's V5 cross-chain structure could supply, and **it is not made anywhere.** **(a) FIXED 2026-09-04, and it went the inconvenient way.** `Paper_043`'s P-Horizon-Participation is corrected to `N = A/ℓ_P²`: `Paper_025` derives the bound as area-over-footprint with an `ℓ²` footprint, `4πR²` **is** `A`, and two of the paper's three statements plus the upstream derivation already agreed. The stray `4π` is what a `4πR² → A` substitution looks like applied twice. **This closes escape route (i) and sharpens the problem:** the admissible branch was the erroneous one, so the requirement `log g = 1/4` against the `g ≥ 2` floor now stands unambiguously, with horizon entropy `2.77×` Bekenstein at the floor. **The arc now has a live coefficient problem where it had an ambiguity** — the more useful state. Routes (ii) and (iii) remain; (iii) is the promising one. Then state which of (ii)/(iii) carries the coefficient. **No tier changed:** `Paper_043`'s area-law *form* is unaffected, and it never claimed the coefficient. *Filed to the black-hole arc ledger as well; the substantive work is there, not in gravity.*
17. **Live audit of the `Paper_027` Newton chain — 2026-09-04. Two real findings, one false positive, and one error of mine rather than the corpus's.** Both frontier families were given the 026/027 chain cold. **(a) `G = c³ℓ_ED²/ħ` is dimensional matching, not derivation — REAL, and sharper than what is recorded.** `Paper_027` §5.3 already says the identification is "a dimensional rearrangement of the standard Planck-length definition" and tiers `ℓ_ED = ℓ_P` as empirical. The audit adds two points the paper does not make: dimensional compatibility gives `G ~ c³ℓ_ED²/ħ` and **not** `=`, since any dimensionless `C` is admissible, so the equality needs a postulate fixing `C = 1`; and `c` and `ħ` are never introduced in the postulate set at all. **The consequence for the parsimony claim (§4.7, "reduces the number of independent fundamental constants by one") is the important part:** that holds only if `ℓ_ED` is fixed **independently of `G`**. If `ℓ_ED` is fixed by matching observed gravitational strength — which is what §5.3 says — then no constant has been eliminated, only renamed. *The defence, not currently made in the paper: `ℓ_ED` is also the UV cutoff (`Paper_111`) and the horizon-count grain (`Paper_025`), so fixing it once and spending it several times is a genuine reduction. That argument should be in `Paper_027` §4.7 and is not.* **F-P RUN 2026-09-04 — it does not fire, and it cannot fire as written.** All three companion roles **inherit** `ℓ_ED` from `Paper_027` §5.3 rather than determining it: `Paper_025` labels the identification **I** (*"empirically identified with ℓ_P via Newton-recovery"*), `Paper_111` says the cutoff is *"INHERITED via Planck-scale matching"*, and `Paper_043` needs P-Horizon-Participation to fix its count. **One determination, three uses.** The defence written into §1.2 earlier the same day said "spent in several independent places", which is true of the uses and false of the determinations; **it has been corrected in place.** The surviving claim is an accounting one about the *input list* — a rival needs `G` plus a grain plus a cutoff, ED needs one scale — not a claim of predictive success. **F-P reformulated** to fire only if some role ever determines `ℓ_ED` independently of `G` and disagrees with `√(ħG/c³)`; conditional, since no independent determination exists today. **One quasi-independent comparison does exist and is off by 4:** `Paper_025` §6 finds its bound matches Bekenstein's in form *"up to the dimensionless coefficient (1 vs. 1/4)"*. That is the only outside check on the shared value, and in this arc the forms have been reliable while the coefficients have not — flagged, not dismissed. **BOTH EDITS APPLIED 2026-09-04.** `Paper_027` §2 now declares **P-G-Closure**; §5.2's "unique combination" is corrected to *unique in its powers, not its coefficient*, with a scope note recording that `κ_V1σ(M) = GM` defines the product in terms of `G` and so cannot independently fix it. The parsimony claim in §1.2 now carries the multi-use defence: `ℓ_ED` is one postulated scale spent in the horizon channel count (025), the BH area law and entropy coefficient (043, BH_Thermal2Pi), and the UV cutoff (111), so a framework carrying `G` as an independent input must separately supply a grain and a cutoff — ED supplies one number and uses it three times. The claim is now explicitly *about the count of independent inputs across the corpus, not about this derivation in isolation*, with falsifier **F-P**: if `ℓ_ED` needed a different value in any of those roles than the one reproducing `G`, the reduction is spurious. *(Section number correction: this was called §4.7 in session notes; the parsimony claim is in §1.2 and §4 ends at §4.6.)* **(b) `N(R)` does no work in the `1/R` — REAL, and it is my error, not the corpus's.** Because the count cancels *exactly*, the radial form comes wholly from V1's inherited envelope and the same cancellation runs in any spatial dimension. `Paper_027` never claims otherwise. The claim *"inverse-square is P06 showing up, not a balance of two effects"* was written by me into `Desktop/ED New Organization/What ED Is_Technical.md` on 2026-09-04 and is now corrected there. What P06/P08 actually buy is the **distribution** step `σ_ch = σ(M)/N(R)`, which is what prevents the naive `Φ ∝ R`, plus the scale that later fixes `G`. *A summary document invented a claim the papers do not make: the read-first rule exists for exactly this and I broke it.* **(c) P-Codim-1 mislabelled — FALSE POSITIVE, held to the same bar as a hit.** One audit argued that a codimension-1 object in 3-space is a 2D sheet, which meets a 2-sphere in curves rather than in `ℓ²` patches, so the count `A/ℓ²` misapplies intersection geometry. Standard usage would make that right, but `Paper_025` §2 **defines its own term**: *"when a channel crosses an n-dimensional substrate-graph surface, it occupies a region of n-dimensional extent at the substrate scale"* — a 2D footprint on a 2-surface, exactly what the count needs. Its audit table already labels the row **P** and states it is not derivable from P02+P07. **No defect.** *Worth keeping as a naming hazard: the label invites precisely this misreading, and a reader who knows differential geometry will make it.*
16. **Channel-count factors dropped in the bilocal geometric mean — ~~OPEN~~ **DISCHARGED 2026-09-04**, and this row was stale against its own paper until the status pass (#42).** `Paper_030` §4.2 now displays the cancellation explicitly and states the condition it needs as the labelled postulate **P-Bilocal-Count** (`N_bilocal = N_horizon = N(R) = 4πR²/ℓ_ED²`), with falsifier **F-BC** and an explicit statement of what breaks without it (if `N_horizon` were `N(R_H)` instead, the ratio would be `R/R_H ≈ 2.3×10⁻⁶` at 10 kpc and the deep-MOND result would be destroyed). The paper carries *“A flag recorded here on 2026-09-04 has been discharged by the rewrite below”* — **the fix landed in the paper and this row was never updated.** Reverse of the usual direction, and the more costly one: a reader choosing work sees OPEN and re-does finished work. *Original finding retained below for the audit trail.* **`Paper_030` §4.2 defines two channel-count factors and drops both in the next equation.** It states `b_K^{loc}(R) ∝ GM/(R·N(R))` and `b_K^{horizon}(R) ∝ a₀R/N_horizon`, so the geometric mean of those two quantities is `√(GMa₀ / (N(R)·N_horizon))`. The equation immediately following instead reads `√(GM/R · a₀R) = √(GMa₀)`, with both count factors absent. The only bridge is the clause *"for substrate-level bandwidth conventions chosen consistent with Papers_027 and 029"*. **The magnitude is not marginal:** `N(R) = 4πR²/ℓ_P² ≈ 4.6×10¹¹¹` at 10 kpc, so the dropped `1/√N(R)` alone is `~10⁻⁵⁶`, before `N_horizon`. **`Paper_QuadraticStrain_v1` inherits the post-drop form**: its §2 lists `b^{(L)} ∝ GM/R` and §5.1 uses it to obtain `√(GMa₀)` — while its own §4 uses `b_K^{(L)} ∝ GM/(R·N(R))` for the same symbol, so the two forms sit in one paper, differing by `N(R)`. **This is the third instance of one defect class, and it is the same class as Staleness #10 and #13:** an unfixed normalization absorbing whatever factor the result requires, with the absorption carried by a clause rather than a derivation. In #10 it was `N_substrate` erasing a `2/3` and supplying a `1/(2π)`; here it is "conventions chosen consistent with" erasing `N(R)·N_horizon`. **Fairness note, and it matters:** a strain-to-potential normalization may legitimately carry channel-count factors — `Paper_026`/`027` show exactly such a cancellation working correctly, where `N(R)` divides out because the source content is *distributed* across channels. The defect is not that count factors cancel; it is that here they are written into the definitions and then absent from the result with no cancellation shown, so the normalization is again doing unbounded and unaudited work. **Provenance: this was found by an outside model, not in-house.** The `Paper_QuadraticStrain_v1` chain was put to two frontier families as a live audit (`Note` §—, probe note §5d). Both located the diagonal-removal defect this ledger already had as #13; **one additionally found this, which neither the in-house read nor the item-19 symbol sweep had caught** — the sweep missed it because `b^{(L)}` and `b_K^{loc}` are different tokens and both forms are separately *stated*, so a form-collision search does not fire. **Item 19 should be strengthened: the sweep must follow a quantity across renamings, not just across occurrences of one spelling.** A second audit point, also correct and separately recorded: `R` carries two meanings inside `√(b^{(L)}b^{(H)})` — local radius in `GM/R`, and whatever makes `a₀R` a horizon quantity — and if both are the local radius then `b^{(H)}` grows without bound with distance from the local mass, which is the same incoherence #13 records from the diagonal side. **SETTLED 2026-09-04 by reading `Paper_027` §§4.3–4.4. The cancellation exists and is legitimate; the audit finding stands as a presentation defect, not a physics error; and the clause conceals one specific, decidable assumption.** *(1) The mechanism is real.* `Paper_027` §4.3 distributes the source content, `σ_ch = σ(M)/N(R)`, structurally from P03 translation-invariance plus P07 distinguishability plus the holographic count — explicitly *not* a phenomenological partition. §4.4, titled *"the channel-count factors cancel"*, then sums over `N(R)` channels and the factors divide out exactly, `Φ = −κ_V1σ(M)/R`. That section even records the round-1 error of losing the cancellation and getting `Φ ∝ R`, so the corpus knows this trap by name. *(2) It extends to the geometric mean, conditionally.* Summing the cross-term over bilocal channels gives `N_bilocal·√((b_L/N)(b_H/N_h)) = √(b_L b_H)·N_bilocal/√(N·N_h)`, so the `√(GMa₀)` of `Paper_030` §4.2 is exactly right **iff `N_bilocal = √(N·N_horizon)`** — which holds trivially when each of the `N(R)` channels through Σ_R carries both local and horizon content, the natural reading of the construction. *(3) The hidden assumption is `N_horizon = N(R)`.* `Paper_030` line 163 introduces `N_horizon` as "channels connecting chain to dipole-projected cosmic horizon" and **never assigns it a value**; it appears nowhere else in the arc. If it is instead the count on the cosmic surface, `N(R_H)`, the surviving factor is `√(N(R)/N(R_H)) = R/R_H ≈ 2.3×10⁻⁶` at 10 kpc — which would destroy the deep-MOND result. **So the clause "conventions chosen consistent with" is standing where a named identification belongs, and the identification is defensible** (horizon content reaching the chain crosses the same enclosing 2-surface) **but it is an assumption, not a derivation.** **Verdict: no tier change to `√(a_N a₀)`** — it was already conditional on P14/P-Quadratic-Strain and is now additionally conditional on one named identification. **FIX APPLIED 2026-09-04.** `Paper_030` §4.2 now displays the full sum `N_bilocal·√(b_loc·b_hor) = √(GMa₀)·N_bilocal/√(N(R)N_horizon)`, notes that the cross-term is quadratic in amplitude so each factor contributes `1/√N` rather than `1/N`, and names the missing identification as a labelled postulate **P-Bilocal-Count** with its justification (P-Codim-1 puts horizon content across the same enclosing 2-surface; P03 gives no channel a preferred source). The alternative reading and its `R/R_H` consequence are stated, with falsifier **F-BC**. Registered in the Postulated section above. **Audit credit stands:** the finding was correct about what is displayed, and the corpus is better for having the condition named rather than absorbed.
15. **μ cannot be derived as a smooth function, and `Paper_030` §6.3/§7.2 overstate what exists — 2026-09-04. ⚠ THE CLOSED FORM BELOW IS SUPERSEDED (updated 2026-09-04, #42).** The profile this item computes from, `a = a_N + a₀ + √(a_N a₀)`, **no longer exists**: ≦37 removed the standalone `a₀` at source (`Σ₀` is a content normalization, no gradient taken). The corrected profile is **`a = a_N + √(a_N a₀)`**, giving with `s = √(a_N/a₀)` the closed form **`μ = s/(s+1)`**, not `s²/(s²+s+1)`. *(Verified numerically over `a_N/a₀ = 10⁴…10⁻⁴`; the two differ substantially — at `a_N = a₀`, `μ = 0.500` against the old `0.333`.)* **Both conclusions of this item survive unchanged, which is why it is updated rather than withdrawn:** the saturation is still `1-μ = 1/(1+s) ~ x^{-1/2}`, so the Cassini exclusion outside the deep field stands; and §§6.3/7.2 were reworded accordingly. **What DID change is the diagnosis:** ≦34 re-files `μ` as the coarse-grained **V5 envelope shape**, which `Paper_090` §7.2 lists as value-layer — so `μ` is **inherited, not an open derivation**, and this item's title (*“cannot be derived”*) is right for the wrong reason. *Original text below.* Attempted the derivation of the MOND interpolation function from the substrate. **ED has a candidate and it is written down:** `Paper_030` §6.3 gives `a = a_N + a₀ + √(a_N a₀)`, which with `s = √(a_N/a₀)` is the closed form `a/a₀ = s²+s+1`, `μ = s²/(s²+s+1)`. **Its saturation is `1-μ ~ x^{-1/2}`** — far slower than the simple (`x^{-1}`) or standard (`x^{-2}`) families. The `√(a_N a₀)` term is a `1/R` force, **not** degenerate with a `GM_☉` rescaling, and it gives **8.9×10⁻⁸ m/s²** of anomalous acceleration at Saturn against a Cassini-class bound near **10⁻¹⁴** — excluded by ~7 orders. **`Paper_KM-I` preamble item 5 already states the criterion** ("a Cassini-constrained family, slow-saturating members excluded by ephemerides") **and neither paper notices that §6.3's profile is such a member.** *ED is not refuted*, because `Paper_030` §7.1 switches P14 **inactive** in the Newtonian regime. But that escape contradicts §7.2's "smooth and parameter-free crossover": the profile is **three branches with P14 toggled at the boundaries**, and the toggle is the Staleness #13 regime assumption. **§6.3's "no interpolation function" claim does not hold** — a piecewise profile with an assumed switch *is* an interpolation function, minus the smoothness. **The two are exclusive:** smooth ⇒ Cassini kills it; switched ⇒ the transition is assumed, not derived. **SCOPED 2026-09-04 by item 19: the conclusion below is `Paper_030`'s, not the corpus's.** `Paper_KM-I` — the **standing** MOND result — uses a smooth Cassini-constrained interpolation family, not a switch, and states plainly that *"there is no mechanism here to evade the static bounds."* What survives from this item is the sharper half: `Paper_030` §6.3's profile saturates as `x^{-1/2}` and is a **slow-saturating member excluded by KM-I's own stated criterion**. **Convergence with item 14:** the horizon-competition mechanism *is* that switch, and switches the right way — above `cH₀` the chain's own Rindler horizon lies inside the cosmic one, cosmic content is cut off, and the bilocal term has nothing to couple to. It would **derive §7.1's assumed regime structure**, which is what Cassini demands. Residual: the switch sits at `cH₀` not `a₀` (item 14's 5.67), and a sharp switch is not the gradual transition rotation curves show. **Recommended: §7.2's "smooth" and §6.3's "no interpolation function" should be reworded; the honest claim is a derived deep-MOND asymptote plus an assumed regime switch.** New falsifier logged on the threshold note as F4: if the cross-term is ever shown active at solar-system accelerations, `Paper_030` is refuted outright.
14. **A threshold mechanism for `a₀`, and a convergence that isolates the `2π` — 2026-09-04.** (`Note_a0_ThresholdFromHorizonCompetition.md`, opened as research target #19.) `Paper_047_5` already holds the ingredient at M3: Rindler acceleration horizons and cosmological horizons are **the same ED substrate object**, the V5-saturated boundary where `Γ_cross` collapses, differing only in which axis parameterises it. A chain therefore has **two** saturation boundaries — its own at `R_a = c²/a` and the cosmic one at `R_H = c/H₀` — and only the nearer binds. That is a **discrete switch**, and it is the same competition `Paper_028` §3.2 runs to fix `R_H` itself. The switch is at `c²/a = c/H₀`, i.e. **`a = cH₀`**: above it the chain's own horizon is inside the cosmic one (Newtonian), below it the cosmic horizon binds (deep-MOND). **This answers target #19's question without a new postulate:** the threshold is not in the response, which stays linear in `|a|` per `Paper_029`; it is in which boundary saturates. **The convergence is the important part.** This lands on `a₀ = cH₀` bare. Target #18's Route 2, from entirely different machinery (Rindler smoothness + de Sitter temperature), found the *symmetric* condition `T_a = T_H` also gives `cH₀`, with only an unmotivated asymmetric condition producing the `2π`. **Two independent mechanisms now reach `a₀ ~ cH₀`; neither produces the `2π`; and the azimuthal-Fourier derivation that claims it provably cancels (Staleness #10).** Three routes to the scale, none to the coefficient. That *strengthens* horizon-tying — the substantive claim now has two independent derivations — and makes the coefficient look **inherited**. **Numerical gap, unresolved:** `cH₀ ≈ 6.8×10⁻¹⁰` against a fitted `a₀ ≈ 1.2×10⁻¹⁰`, a factor of ~5.7 ≈ `2π`. Either the factor is real and unexplained (= target #18), or a fitted `a₀` in an interpolation function `μ(x)` is not the same object as a mechanism's crossover point. **CHECK RUN 2026-09-04 — the strong form is refuted.** In any law `μ(a/a₀)a = a_N` the asymptotes `√(a_N a₀)` and `a_N` cross at `a_N = a₀`, so the fitted `a₀` **is** the transition location for every `μ` family: the shape of `μ` rounds the corner, it does not move it (checked across three families; the solution at `a_N = a₀` lands at 1.10–1.62 `a₀`). No `μ` puts the transition at `cH₀` while `a₀` is `cH₀/(2π)`. A weak form survives — the switch might mark the *onset* of departure rather than the crossing, and the 10%-departure point ranges 1.0–9.0 `a₀` across families, bracketing 5.67 — but that range spans an order of magnitude, `μ` is fitted not derived, and picking “10% departure” after seeing the number is not evidence. **Recorded as not-refuted, not as support.** Net: 5.67 is a genuine residual (5.42–5.99 over the Hubble-tension range, i.e. `2π` to within 5–14%), and it is *the same* residual as Staleness #10's. **Every route reduces to comparing the chain's acceleration scale against `cH₀` while the measurement says `cH₀/(2π)`: one unexplained factor at the horizon-to-chain join, identical in every derivation.** Deriving `μ` from the substrate would make this a sharp test. **Pay-off if the switch can be shown rather than asserted:** `Paper_030` §5.3's regime assumption — restored under Staleness #13 — becomes *derivable*, since "the cross-term dominates in deep-MOND" is exactly "the cosmic boundary binds below `cH₀`". That would restore QuadraticStrain's second discharge by a valid route. Not claimed. **No tier changes.**
13. **QuadraticStrain — the second discharge is inconsistent. ~~OPEN~~ **RESOLVED 2026-09-04** — see #44.** The dilemma below is dissolved rather than chosen between: #36's source-vs-response asymmetry removes branch (i)'s cost, so the paper sits on branch (i) with `b^(H)` isotropic and the `Σ₀` removal justified without a dipole. The `Θ_LH` repair floated at the end of this item is **closed** — §5.1 factors `cosΘ_LH` out of the channel sum, so it cannot be channel-dependent. *Original finding retained below.* Adversarial read of `Paper_QuadraticStrain_v1` §§2–6, requested as a fresh audit target after Staleness #10. **`b^(H)` is used with two incompatible meanings, and the R-linear form is load-bearing for both.** §2 inherits the per-channel convention `b^(H) ∝ a₀R` (Paper_030 §4.2) as a non-negative bandwidth — it must be, since P04 requires `b_K ≥ 0` and §5.1 takes its square root. That R-linear form is exactly what makes the geometric mean work: `√(GM/R · a₀R) = √(GMa₀)`, R-independent, which is the whole point. **The same object, on the diagonal, is `Σ₀ = a₀R`** — the term §6.2 removes as "inconsistent with Paper_029's dipole." Under P-Quadratic-Strain, `|P^L + P^H|² = b^L + b^H + 2√(b^L b^H)cosΘ` is a single algebraic identity: the second and third terms both follow from `P^H ≠ 0` on the channel. Keeping the cross-term while deleting the self-term is not a physical claim about dipoles; it is dropping one term of an expansion the paper's own postulate generates. **Both repairs cost something, which is why this needs a decision rather than a patch.** *(i) If `b^(H)` is isotropic* — which is what §2 and §5.1 actually use — the geometric mean, the P14 discharge and BTFR all stand, but §6.2's removal of `Σ₀` loses its justification, the diagonal contributes a constant `a₀` (gradient of `a₀R`), and Paper_030's regime assumption returns. The **second** discharge fails. *(ii) If `b^(H)` carries the genuine dipole structure* `∝ |a|cosθ` that §6.1–6.2 rely on, it is sign-indefinite, so it is not a bandwidth under P04 and `√(b^L b^H)` is imaginary over half the sphere. Then the **first** discharge fails too. As written the paper is on branch (i) and §6 is the part that does not fit. **Candidate repair, not attempted here.** The horizon's directional, acceleration-induced character could be carried by the relative phase `Θ_LH` rather than by `b^(H)` — a `cosθ` structure sits naturally in an angle. That keeps `b^(H)` non-negative and isotropic, preserving §5 intact. It does **not** by itself remove the diagonal, so §6.3's ratio effect would still need an independent argument for why the horizon self-term does not contribute a gradient. **Scope.** The first discharge (P14 → the geometric mean forced as the off-diagonal of `|P^L + P^H|²`) is a good argument and survives on branch (i); it is the stronger half of the paper. The MOND phenomenology, BTFR slope-4 and Newtonian recovery are unaffected on that branch. What is at risk is specifically §8's claim of **two** discharges and the "no regime assumption is needed" line in §6.3. Note also that this compounds with Staleness #10: §6.1 rests on Paper_029's `1/(2π)` dipole derivation, itself now disputed. **Sharpened by the item-19 sweep, same day: `Paper_030` pre-emptively answers the argument §6.2 uses against it, and §6.2 does not cite the answer.** `Paper_030` line 123 states outright: *"Σ₀(R) = −a₀R is **not** a separately-postulated stability-landscape function; it is the coarse-grained potential **derived from Paper_029's dipole-projection mechanism via radial integration**."* QuadraticStrain §6.2 rejects it on the ground that "there is no static, source-independent, isotropic field of strength a₀ from which a radial integration could build a potential −a₀R" — arguing against a monopole reading Paper_030 explicitly disclaims. Two papers now disagree in the record about whether the same radial integration is legitimate, and the later one does not engage the earlier one's stated justification. **The empirical constraint is the thing that actually decides this**, and `Paper_030` line 145 names it: with Σ₀ present and additive, deep-MOND gives `a ≈ a₀` constant, hence `v² = a₀R`, which BTFR data refutes. So Σ₀ must be either removed (QuadraticStrain's route) or suppressed (Paper_030's regime assumption). It cannot simply stand. That is the real pressure behind both papers' moves, and any repair has to clear it. **A third option neither paper takes:** `Paper_029` §3.3 has the horizon contribution existing *only for an accelerating chain* and proportional to its own acceleration — which makes it a **response** term, not a **source** term, and a response does not enter the potential additively at all. That would remove Σ₀ for a reason that survives §6.2's problem, and it is a research question rather than an edit. *The paper's §2.5 audit table is otherwise exemplary — eleven labelled rows, the phase residual honestly flagged A/OPEN at row 11 — and this slipped past it because the inconsistency lives **between** rows 6 and 7 rather than inside either. Same structural class as Staleness #10: a quantity carrying one meaning in one section and another meaning three sections later, invisible to a per-row check.* **RESOLVED 2026-09-04 — branch (i) taken, and the defect is upstream.** §6.2's *conclusion* stands but its *argument* does not: `Σ₀ = −a₀R` is not isotropic — a linear potential with constant gradient along the chain–mass axis is exactly what an acceleration-aligned dipole gives, and `Paper_030` §3.2 integrates along that axis — so anisotropy does not disqualify it, and §6.2 never engages `Paper_030` line 123's stated provenance. **The real defect is one word in `Paper_030` §3.2:** it integrates *"the **constant** cosmic-anisotropy acceleration a₀"*, while `Paper_029` §§3.3/5.1 make the horizon content first order in the chain's own `|a|` and **zero for a non-accelerating chain**. That step converts a **response** into a **source**. Flagged at source in `Paper_030`. **A third branch was attempted on that basis and fails:** restoring `b^{(H)} ∝ H₀|a|` would have been free — the diagonal stays, so the algebra is honest, and it vanishes as `a → 0`, so no constant-`a₀` floor and BTFR is safe — but the cross-term then gives `a² ∝ GM H₀|a|/R²`, hence `a ∝ GM H₀/R²`, a Newtonian-like `1/R²` scaling rather than `√(a_N a₀)`. **The naive restoration breaks MOND. Recorded as a negative.** **Branch taken: (i).** `b^{(H)}` isotropic and R-linear as §2/§5.1 already use it; the diagonal **stays** (it is generated by the paper's own postulate and cannot be dropped selectively); `Paper_030` §5.3's regime assumption is **restored**. QuadraticStrain audit row 7 **WITHDRAWN**, row 8 re-tiered to **P (regime assumption restored)**, §8's "two postulates to one" reads **one and a half**. §5's geometric-mean forcing and the P14 discharge are **unaffected and remain the strong half**. **What this buys is worth more than the discharge it costs:** the formal inconsistency is now a specific physical question — *`Paper_029` gives a horizon response first-order in `|a|`, and MOND requires a threshold at `a₀`; how does a linear response produce a threshold?* Neither paper asks it, it was invisible while `Σ₀` was carried as a constant source, and it is arguably the deepest open question in ED's MOND account. **Promoted to an open research item.**
12. **P-Commitment-Linear / GR-III §4 traced 2026-09-04 — sound in kind, one asymmetry worth naming. No tier change.** Audited the way `N_substrate` was audited for the a₀ `1/(2π)` (Staleness #10), and the verdict is different: **this is not that class of problem.** GR-III §4 states its premise inside the boxed claim itself (*"modulo one band-accounting premise: that the metric band and the rate-numerator band are the same `b_int`; named, not proven"*), scopes its falsifier at line 211 to the exact condition **β ≠ 0** rather than to the endpoint it argues hardest against, and states the counterfactual plainly (*"had P04 defined the reserve as a fixed fraction of the total or supplied a replenishment band, ED would have been Nordström"*). Contrast 028/029, where §5.3 claimed "no free parameter" while the choice sat unflagged in §5.1. The algebra also checks: with `Γ ∝ b^α`, the GR-I §4.2 null condition `0 = −N² + b⁻¹b^{2α}` gives `N² ~ b^{2α−1}` and `g₀₀g_rr ~ −b^{2(α−1)}`, so α = 1 is Einstein and α = 0 is Nordström, exactly as stated.

    **The asymmetry.** Writing `reserve ∼ b_int^β`, so `α = 1 − β`, the §4 argument is *decisive against β = 1*: Nordström needs the reserve **raised** where `b_int` is high, which is replenishment, which P11 forbids. But Einstein needs **β = 0 exactly**, and any intermediate `0 < β < 1` — a reserve merely correlated with ambient capacity without tracking it — gives `g₀₀g_rr ≠ −1` and no Schwarzschild relation. The step from "does not track" to "is exactly zero" is carried by the word *"generically"* and is not separately argued. The paper's own falsifier is correctly scoped to β ≠ 0, so the claim is stated at the right strength; it is the §4 body's supporting argument that is stronger at one endpoint than across the interval. **Recommended: one added sentence in §4 (or a GR-III preamble item) stating that the replenishment argument bounds β away from 1 and that β = 0 exactly is a genericity claim on the P04 band structure.** Not a re-tier: the ledger already carries this as *D-cond on a named band-accounting premise*, which remains accurate.

    **One question raised, not resolved.** If the reserve is monotone-draining with no replenishment (§4, §6), what sustains a steady `Γ ∼ b_int/reserve` in a static exterior? As the reserve drains, `Γ` grows without bound; §7 uses precisely that mechanism for horizon formation (*"where the reserve is exhausted, so the cut is frozen"*). Either the reserve is a per-chain carried budget rather than a local field — in which case "co-scaling with `b_int`" needs restating in those terms — or a static exterior needs something holding the reserve steady, which would be the replenishment §4 forbids. Likely a reading error on the auditor's part rather than a defect; flagged for whoever next opens GR-III.

11. **`N² ~ b` is the arc's least-obvious step — measured 2026-09-04, record-only.** In an independent-reconstruction probe (`../substrate-evaluation/Paper_PrimitiveMinimality_IndependentReconstructionProbe.md`), two frontier models from different families were given the thirteen primitives plus V1/V5 and asked to derive gravity. **Both independently took the commitment rate linear in bandwidth** — which *is* P-Commitment-Linear, so this is convergent support for that postulate, not a challenge to it. What neither run did was separate the rate a front advances from the rate a clock ticks: both set `ν_clock ∝ B` directly. Neither reached `ν_clock ∝ √b`. One noticed the shortfall, proposed `g₀₀ = −(B/B₀)²` to recover its own linear law, and stated it could not reach `√(1 − 2GM/c²R)`. GR-I's front-on-the-null-cone construction gives `N² ~ b`, hence `dτ/dt = √b` and `g₀₀ ~ −b`, which is what yields `g₀₀g_rr ~ −1` — the Schwarzschild relation, the Einstein branch rather than the conformal one, and therefore the factor of two. **The load-bearing step is therefore GR-I §4.2's front-null identification, not the linearity.** Putting the advancing front on the null cone of `ds² = −N²dt² + b⁻¹dx²` with `dx/dt = Γ ∝ b` gives `0 = −N² + b⁻¹b²`, hence `N² ~ b`, `N = √b`; in general `Γ ∝ b^α` gives `N² ~ b^{2α−1}`, so the whole Einstein/Nordström fork lives in that exponent. No outside run made that move or mentioned it. *GR-III §4's forcing of α = 1 was traced separately on 2026-09-04 — see item 12.* *No tier changed. Record-only, flagged for the next gravity pass.* Same probe also found that both runs **postulate** the `1/R` source profile that 026/027 derive from the `N(R)` channel count, and that removing P08 costs the inverse-square law and `G` outright.
10. **The `1/(2π)` in a₀ — OPEN, found 2026-09-04, not previously recorded.** Reading `Paper_028` §6 and `Paper_029` §5.1 line by line, the 1/(2π) in `a₀ = cH₀/(2π)` is **not established as structurally forced** by the papers as written. `Paper_028` §6.3 computes the azimuthal integral and states its value: `∫₀^{2π} dφ/(2π) = 1`. §6.4 then "pulls out" a 1/(2π) that the algebra has already cancelled, and concedes in its own words that the factor's *location* (a separate `dφ/(2π)` measure versus absorption into `Y10`'s `√(3/4π)`) is **conventional**. `Paper_029` §5.1 shows the same cancellation explicitly: its displayed equation carries `(1/2π) · ... · 2π`, leaving no `2π` on the right-hand side, and the sentence immediately after it nonetheless reports the factor as surviving. The 1/(2π) then re-enters at the assembly step, where `N_substrate` is *chosen* (§5.1 calls it "a substrate-level normalization choice") at exactly the value that produces the answer, absorbing the `2/3` polar factor and `G_φ` along the way. **`N_substrate` is fixed nowhere.** `Paper_026`/`Paper_027` fix `κ_V1`, the per-channel V1 envelope coupling, a different normalization doing a different job; neither mentions `N_substrate`. `Paper_027` defers the `2π` forward to `Paper_029` §5.1. `Paper_028` defers both the substrate-source normalization and the order-unity coefficient forward to `Paper_029` §5. `Paper_029` §5.1 calls it a convention. The references form a loop and nothing computes it. **Why this is not bookkeeping.** A measure-normalization convention cannot carry a physical prediction: adopt the unnormalized measure `∫dφ` and the same algebra returns `2π` rather than `1`, moving the "parameter-free" number by `4π²`. A quantity that changes when the bookkeeping is renamed is not predicting anything. **What is unaffected.** `a₀ ~ cH₀`, the horizon-tying, which is the physically substantive claim, stands untouched, as does the `R_H = c/H₀` decoupling surface itself. So does `a₀(z) = cH(z)/(2π)` **as a form**, including the forced exponent of exactly 1, because the evolution follows from `a₀` riding the live horizon and not from the `2π`. The flagship falsifiable prediction is intact. What is suspended is narrower: the "parameter-free, 1/(2π) structurally forced" claim, which is precisely the part this arc identifies as ED-distinctive, since BTFR slope-4 and its tightness are MOND-shared. **Repair routes — updated 2026-09-04, see `Note_a0_TwoPi_RepairRoutes.md`.** The mode-amplitude route named here originally is **CLOSED as a negative**: the `∫dφ` is a coarse-grained stand-in for a sum over discrete channels (P07), and P04 makes bandwidth additive *across* channels, so the substrate object is a sum, not an angular density — converting it introduces a channel count `N_az`, never a bare 1/(2π). The route asks the primitives for the opposite of what they say. **A materially better route is now open.** `Paper_BH_Thermal2Pi_EntropyCoefficient` derives a genuinely physical `2π`: the no-conical-defect smoothness condition on ED's near-horizon Rindler `b`-profile, which nothing cancels. An accelerating chain has a Rindler horizon with `κ_a = a/c`; the cosmic decoupling surface is de Sitter-type with `T_H = κ_H/(2π) = H₀/(2π)`; and `κ_a = T_H` returns `a₀ = cH₀/(2π)` **exactly**, with the `2π` supplied by the horizon's own smoothness. If grounded, the `2π` in `a₀` becomes **the same `2π`** as in `T = κ/(2π)` — one fact appearing twice, the untunability structure `c_T = c` / `α₂ = 0` already carries. **SHARPENED 2026-09-04 by external audit — the normalization does *two* jobs, not one.** The flawed chain was put to two frontier models as a blind calibration test (known answer, withheld). **Both independently located the defect at the same step**, and one went further than this ledger had. The point missed here: after the azimuthal cancellation the surviving geometric factor is `2/3`, so the displayed integral yields **`(2/3)·cH₀`**, not `cH₀`. Reaching `cH₀/(2π)` therefore requires the §5.1 normalization to do **two separate things** — *erase* the `2/3` **and** *supply* a `1/(2π)` that the algebra had cancelled. `Paper_029` §5.1 does exactly that, in one line: `N_substrate·G_φ⁻¹ = (2/3)(|a|/c)⁻¹` is set precisely so the `2/3` and the `|a|/c` divide out. *Fairness note: absorbing an O(1) geometric factor into a strain-to-acceleration normalization is not illegitimate in itself — the defect is that the normalization is fixed nowhere, so it can absorb and supply whatever is required. But the audit is right that the honest output of the displayed integral is `2/3`, and this ledger previously understated how much the normalization was carrying.* **Second audit point, also correct:** the `2/3` presupposes a dipolar `cosθ` in the density that §5.1 does not display — the monopole term against the `cosθ` projector integrates to zero, so the step is sound only given an explicit dipole form. **Third, and it corroborates the repair route:** one model concluded unprompted that the step could only survive given *"a second, independent source of a `1/(2π)` factor — not the same azimuthal normalization already used"*. That is precisely `Note_a0_TwoPi_RepairRoutes.md` §3's Route 2 (the horizon smoothness `2π`), identified independently and without sight of it. **Matching condition advanced 2026-09-04 (note §4b), not closed.** The *horizon* half is now grounded, and on a statement made for unrelated reasons in 2026-05: `Paper_028` §3.4 and §7 declare the cosmic surface **statistical, not geometric**, so there is no `b`-gradient to read off it — hence no `κ` is available to it and its only characterisation is a state, which carries the smoothness `2π`. The *chain* half is now a **layer argument** rather than an assertion: a chain's acceleration is a P11 commitment-rate asymmetry, a bare layer-1 count-rate, while `T_H` is a layer-2 object by `Paper_BH_Thermal2Pi` §4b's own finding. The `2π` is then the **coarse-graining seam crossed exactly once**: comparing layer-1 `a` to layer-2 `T_H` keeps one factor, while comparing `T_a` to `T_H` applies the same conversion twice and cancels it, giving the refuted `cH₀`. **The residual debt is one specific thing:** why the comparison is executed at the chain's layer rather than at a common layer. The offered reason — the chain is what responds, so the response runs in the chain's own machinery, while the horizon can only present itself coarse-grained because it is statistical by construction — is a reasonable sentence carrying the entire `2π`, and is not a derivation. **A testable pattern falls out:** if the `2π` is a layer-crossing factor, every ED quantity comparing a bare chain rate to a horizon thermal state should carry exactly one, and like-layer comparisons none. Unchecked across the corpus. **The whole remaining debt is the matching condition:** `κ_a = T_H` equates a surface gravity with a temperature, dimensionally clean in natural units but asymmetric and unmotivated, and the *symmetric* condition `T_a = T_H` has the `2π` cancel and gives `a₀ = cH₀`, wrong by the factor at issue. A criterion justified only by landing the number is numerology; it must be argued from the substrate or abandoned. Tier ceiling if it closes: *structural given the coarse-grained horizon*, inheriting `Paper_BH_Thermal2Pi` §4b — not substrate-native. Until then the honest label on `N_substrate` is **P**. *Two process notes. `Paper_029` carried **no audit table**, which the paper-writing checklist §1 requires; a P/A/I/D pass would have forced a label onto `N_substrate` and surfaced this at drafting time. **FIXED 2026-09-04: §2.5 added, fifteen labelled rows.** It records `N_substrate` as **P** in the paper's own words, the azimuthal factor as **DISPUTED**, `a₀ = cH₀/(2π)` as **Postulated/disputed**, and — separately — `a₀ ~ cH₀` as **D** and `a₀(z)` with its forced exponent as **D**, so the surviving claims are visible alongside the suspended one. It also flags row 5 (the kinematic-dipole analogy) as an **A** row the checklist does not permit outside verdict framing. And this was found by tracing a pointer, not by re-deriving anything: the papers' own equations say it.*
9. **Two gravity scope items propagated into this ledger — 2026-09-03.** Both landed in the papers on 2026-08-31 (commit `df5d529`) and this ledger did not carry them until now, which made the arc's own staleness detector one session stale on its two most recent sharpenings. (a) **`Paper_GR-II` preamble item 10:** the khronometric identification is **infrared only**; the khronometric row above now records that ED does not inherit Hořava's UV behaviour. (b) **`Paper_MetricFromTheGraph` preamble item 7:** the integer-reach coupling floor (0.00% / 2.70% / 4.25%), now a Measured row. Neither changes an existing claim's tier; both narrow a scope that was previously unstated here. *Method note: this gap was found by a pointer-layer sweep, not by reading the papers — the papers were correct throughout.*
8. **P14 double-status** (record, not error): 030/031/032 carry P14 live; QuadraticStrain (2026-07-07/08) discharges it. Each pre-discharge paper flags P14 honestly; QuadraticStrain is the current resolution.

### Honest arc-state
The gravity line is the corpus flagship and it hangs together at the tier it claims. **The GR keystone (#1) is closed** in the honest sense: GR-I derives the weak-field Einstein metric + factor-of-two (conditional on the single postulate P-Commitment-Linear, which GR-III then *forces* from the P04 band law, modulo a named premise); GR-II fixes the class as **khronometric** (Lovelock + mode count, structural) with `c_T=c` and universal Lorentz-violation as F-independent shields; GR-III builds and runs the dynamical rule (Newtonian fixed point, `r_s∝M`, emergent frozen horizon, area law, Hawking scaling, `c_s=c` derived); and GR-IV closes the preferred-frame kill-switch **favorably** — **α₂=0 exactly** (both cones luminal, literature-verified) and `α₁=−4λ_local≲4×10⁻⁹³` (≥70 orders safe) because commitment is Zeno-forced sparse — while keeping it a live test. The **α₂=0 and `c_GW=c` are one causal-cone fact** (GR-IV §3), which is the load-bearing inseparability the untunability reading leans on. The **MOND/dark-matter line stands**: `G=c³ℓ_P²/ℏ` (form-derived, value-inherited), `a₀=cH₀/2π` (form-derived), the Combination Rule and BTFR slope-4, with **KM-I** unifying them as the khronon's deep field (single hinge "forced-given-030," lensing/ghost/PPN checks passing — *the standing result, not to be reinvented*), and **QuadraticStrain** sharpening 030 by recasting MOND as the off-diagonal interference cross-term (discharging P14 to one strain-reading postulate; residual = the constructive sign, now at measured tier). The honest BTFR distinction holds — slope-4 + tightness are MOND-shared; only the a₀ *normalization* is ED-distinctive. **KM-II** reduces the clusters/CMB debt to one sequestered regulator dial under five filters (orthogonality theorem clean) and pins `𝒲₀=−24π²Ω_Λ` at inherited tier with the ab-initio integral an open number-to-miss. The emergent metric is correctly held as **kinematic/static-linear** (the nonlinear regime is the distinctive/falsifiable frontier, OPEN), and exactly-3 as derived-conditional-on-the-holographic-count, not a forcing. **The propagation debt that dominated this folder is now closed:** the flagship a₀(z) evolution bet and its ~30σ 2026 confirmation, which had lived in only one paper (031 §8.8), were propagated on 2026-07-29 to 029 (new §8.8), 037 (preamble item 6), 038 §3.3 CO-3, Predictions_and_Falsifiers, 22_Ways #1, and Paper_101 (promoted to Tier-1 weapon row 2) — see Staleness #1 for the full list. What remains is the *lower-priority* second pass: the synthesis papers (KM-I, KM-II, One-Field, PhilPapers) still cite a₀ = cH₀/2π without the evolution confrontation. **Defensible headline:** *the GR keystone is closed (khronometric, α₂=0 exact, favorable preferred-frame kill-switch); Newton's G and the MOND scale are form-derived / value-inherited with KM-I the standing unification and QuadraticStrain the sharper foundation; Λ is form-identified with a force-vs-stipulate residual; the nonlinear metric and clusters/CMB are the named open frontier; and the confirmed-but-tense a₀(z) evolution has been propagated out of 031 §8.8 across the primary line (2026-07-29), leaving only the synthesis papers as a lower-priority second pass.*
