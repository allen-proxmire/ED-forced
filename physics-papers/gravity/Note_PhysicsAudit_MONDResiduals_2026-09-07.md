# Physics audit — the four "converged" open items are not four

**2026-09-07.** GPT's closing recommendation after four review rounds: *stop broad bookkeeping audits and move to a load-bearing physics audit of the threshold derivation, UFF, and the α=1 confrontation.* Done. **The first result is that the list itself is wrong, and I am the one who wrote it wrong.**

---

## What the Report currently says

§5's MOND residuals, **added by me this morning** in response to GPT's review item 5:

> **(i)** how the first-order horizon response generates the MOND **threshold**; **(ii)** the high-acceleration switch versus a smooth family; **(iii)** the universal-free-fall normalisation.

**I wrote that list from the reviewer's summary rather than from the corpus.** That is the failure `CLAUDE.md` names first, committed while acting on a review that was itself correct about something else.

---

## (i) The threshold — a candidate mechanism exists, dated 2026-09-04, and it answers the structural question

`gravity/Note_a0_ThresholdFromHorizonCompetition.md`. **The threshold is not in the response.**

`Paper_047_5` already holds the ingredient at **M3**: Rindler acceleration horizons and cosmological horizons are the *same* ED substrate object — the V5-saturated boundary where `Γ_cross` collapses — differing only in which axis parameterises them. A chain therefore has **two** saturation boundaries:

$$R_a = c^2/a \quad\text{(its own)}, \qquad R_H = c/H_0 \quad\text{(the cosmic one)},$$

and **only the nearer binds**, because nothing was getting through from beyond the nearer one anyway. The handover sits at `R_a = R_H`, i.e. **`a = cH₀`**.

- **`a > cH₀`** — the chain's own horizon is inside the cosmic one; cosmic content is already cut off. **Newtonian.**
- **`a < cH₀`** — the cosmic horizon binds; horizon content reaches the chain. **Deep-MOND.**

> **Nothing is nonlinear in `|a|`.** `Paper_029`'s linear response is untouched and describes behaviour *within* a regime. What changes at the threshold is **which boundary is saturating**. A linear response plus a handover in which horizon binds **is** a threshold — and it required **no new postulate**.

**And it is the third independent route to the scale.** Target #18's Route 2, from unrelated machinery (Rindler smoothness, de Sitter temperature), also gives `a₀ = cH₀` bare. **Three routes to the scale, none to the coefficient** — which is why the `2π` now looks inherited rather than derived.

**Status: CANDIDATE, correctly.** It has not been promoted, and this note does not promote it. But *"unresolved"* is not its state, and the Report says nothing about it.

---

## (ii) Switch versus smooth family — **this is not an open derivation, and the corpus already says so**

`gravity/Note_TheSwitchIsDerived_TheShapeIsInherited.md` §4, same day. The threshold note originally said the handover *"changes **discontinuously**"* and is *"a **discrete switch**, not a smooth response."* **The discreteness was withdrawn within hours**, by deriving the handover from the corpus's own `Γ_cross` construction (`Paper_039` §3.1) and finding it **shape-inherited**.

The conclusion, verbatim:

> **`μ` cannot be derived without deriving the V5 envelope shape, and `Paper_090` §7.2 lists that shape as value-layer inherited. `μ` is not an open derivation. It is an inherited quantity that has been mis-filed as an open one.**

**So the switch-versus-smooth question is not open work. It is an inherited value-layer quantity, and the arc already carries the analogous disclaimer for the other kernel** — `Paper_026`'s preamble item 3 says the V1 envelope shape is likewise not derived (`Paper_089` §4.4, value-layer).

> **That also closes (i)'s residual.** The threshold's *location* is derived three ways; its *shape* is the interpolation function, which is inherited by design. **There is no open derivation hiding between them.**

---

## (iii) Universal free fall — **genuinely open, and the only one of the three that is**

Unchanged and correctly stated in §14: the substrate rule `a_C = −∇_adj Σ_C` carries no inertial normalisation, so metric reproduction alone does not deliver composition-independent acceleration if the rule is bandwidth-dependent. **ED has inertia and does not have a demonstration that it cancels out of gravitational acceleration.** The proposed fix amends a primitive rather than supplementing one, which is correctly flagged as the expensive kind.

---

## (iv) The `α = 1` confrontation — open, and blocked on nobody

Status after the a₀(z) work: **UNTESTED**, not confirmed and not refuted. The survey's `~30σ` is under a coherent systematics challenge whose effect sizes check out arithmetically, and the survey's own text concedes the molecular-gas omission at `~0.2 dex` against a `0.334 dex` trend. **The catalogue is public**, so the decisive test is executable rather than requestable, and is deferred by AP's decision rather than blocked.

---

## The corrected list

| # | item | actual status |
|---|---|---|
| (i) | horizon response → MOND threshold | **candidate mechanism exists (2026-09-04), answers it structurally, no new postulate; location derived three ways** |
| (ii) | switch vs smooth family | **NOT an open derivation** — `μ`'s shape is value-layer inherited by design, and is mis-filed |
| (iii) | universal free fall | **genuinely open** — the real one |
| (iv) | `α = 1` confrontation | **untested**, executable, deferred by decision |

> **One of the four is a live open derivation.** One is a candidate that has not been promoted, one is inherited by design and mis-filed, and one is an executable observational test nobody has run.

**Four review rounds converged on this list because they were reading the Report, and the Report carried my error.** *A reviewer cannot correct a summary against a corpus they cannot see; that is the job of the person who can, and I skipped it.*

---

## What this does and does not change

**Does not change:** the `2π` remains disputed (#18); UFF remains unproven; the `α = 1` test remains unrun. **No physics moved.**

**Does change:** the arc's open-work list, which was overstating its debts by two items while understating the maturity of a third. **An arc that reports more open questions than it has is not being conservative — it is being inaccurate**, and it hides which question actually deserves the next session.

**The next session's target is (iii), universal free fall.** It is the only genuine open derivation of the four, and unlike the others nothing upstream blocks it.
